//! The closed `sessionRollup` projection kind — one declared row per composited
//! chat `sessionId`, carrying only documented facts (Spec #2896, ST-3). The
//! frontend qualification rule stays on the frontend. Implemented by ST-3.
