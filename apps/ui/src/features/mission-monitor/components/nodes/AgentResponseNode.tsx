/**
 * Replaced by ChatNode as part of the mission-monitor redesign.
 * This re-export preserves backward compatibility for existing NODE_TYPES imports.
 * Once MissionMonitorPanel is updated to use ChatNode directly, this file can be deleted.
 */
export { ChatNode as AgentResponseNode } from './ChatNode';
