## Task: Add Ninja generator config for llama-cpp-sys-2 on Windows

### What to Do

1. Determine the best approach for setting `CMAKE_GENERATOR=Ninja` when building `llama-cpp-sys-2` on Windows. The recommended approach is to add `[env] CMAKE_GENERATOR = "Ninja"` to `apps/tauri/src-tauri/.cargo/config.toml`.

2. Check if Ninja is already available on the system (`where ninja`). If not, document the prerequisite in the project README or build docs.

3. Create or edit `apps/tauri/src-tauri/.cargo/config.toml`:
   ```toml
   [env]
   CMAKE_GENERATOR = "Ninja"
   ```
   
   OR if a target-specific approach is needed:
   ```toml
   # Windows only — use Ninja instead of Visual Studio for CMake builds
   [target.x86_64-pc-windows-msvc]
   rustc-env = ["CMAKE_GENERATOR=Ninja"]
   ```

4. Clean the stale build artifacts:
   ```powershell
   Remove-Item -Recurse -Force apps\tauri\src-tauri\target\debug\build\llama-cpp-sys-2-*
   ```

5. Run `cargo build` from `apps/tauri/src-tauri/` and verify it succeeds with zero errors.

6. If `cargo build` still fails, investigate alternative approaches:
   - Check if the `CMAKE_GENERATOR` env var is actually being read by the `llama-cpp-sys-2` build script
   - Try setting it via `[build]` rustflags or in a wrapper `build.rs`
   - Check `llama-cpp-sys-2` build script source to confirm it reads `CMAKE_GENERATOR`

### Files

| File | Action | Notes |
|------|--------|-------|
| `apps/tauri/src-tauri/.cargo/config.toml` | Create or edit | Set CMAKE_GENERATOR=Ninja |
| `apps/tauri/src-tauri/.cargo/` | Possibly create dir | If .cargo doesn't exist |

### Patterns to Follow

- Use `.cargo/config.toml` for build configuration (Cargo standard)
- Test on Windows specifically since this is a Windows-only bug
- Do not change `llama-cpp-2` crate version or features

### Requirements Covered

- REQ-1: CMake uses Ninja generator on Windows
- REQ-3: No global env var changes needed
- REQ-4: llama-cpp-2 version unchanged

### Done When

- [ ] `apps/tauri/src-tauri/.cargo/config.toml` exists with CMAKE_GENERATOR setting
- [ ] `cargo build` from `apps/tauri/src-tauri/` succeeds with zero errors
- [ ] The config is scoped to the project (not global)

---
*Generated by @fredo-spec-arch*