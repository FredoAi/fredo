# oss-launch — Exploratory Probes

Unscripted edge/failure probes beyond the functional script. A confirmed finding promotes to `functional.md` as a new `F-` row (keep the origin note). Seed probes for #2773 (docs/config-only):

- [ ] E-1: "NEW workflow" scope confirmation — enumerate all workflow files before/after (`git diff <fork-sha>..HEAD --name-status -- .github/workflows/`). If the spec adds no brand-new workflow beyond re-enabling validate.yml + editing audit.yml, confirm AC4d's SHA-pin check applies to all `uses:` in touched files; report the resolved scope as a finding.
- [ ] E-2: Placeholder scrub — grep the full diff for leftover template artifacts: `INSERT`, `TODO`, `yourusername`, `your-username`, `example.com`, `<placeholder>`, `Lorem`. Expected: zero hits in shipped docs (except the intentional CoC placeholder pattern already filled).
- [ ] E-3: CoC hidden byte diffs — compare the CoC with line-ending/BOM awareness (`git diff --no-index` on normalized copies; check for UTF-8 BOM, trailing whitespace, smart-quote substitutions). Verbatim means byte-level, not render-level.
- [ ] E-4: GITHUB_SETUP click-path drift — spot-check 2–3 click-paths against the live GitHub Settings UI naming (or the most current docs.github.com pages). GitHub renames Settings pages periodically; a stale page name makes the doc's "alone" guarantee fail.
- [ ] E-5: Badge rendering — confirm badge URLs are well-formed shields.io / GitHub Actions badge URLs pointing at THIS repo (correct owner/name/workflow path); a badge pointing at the template project it was copied from is a classic launch bug.
- [ ] E-6: Apache-2.0 text canonicality — diff `LICENSE-APACHE-2.0` against the canonical Apache-2.0 text (apache.org/licenses/LICENSE-2.0.txt) — verify it is the full unmodified text, not a paraphrase or an MIT file mislabeled.
- [ ] E-7: dependabot silent-drop check — malformed `directory` values (e.g. cargo pointing at `/` instead of `apps/tauri/src-tauri`) do not error in GitHub UI; Dependabot just skips the ecosystem. Verify directory paths resolve to real manifests (`pnpm-lock.yaml` at root, `Cargo.lock` at `apps/tauri/src-tauri`).
- [ ] E-8: config.yml semantics — with `contact_links` absent, confirm `blank_issues_enabled` (if present) is deliberate and issue templates still surface correctly on the "New issue" page (form templates require correct top-level `name`/`labels`/`body` keys).
- [ ] E-9: Relative-link edge forms — links with `./` prefixes, `../` escapes out of docs/, anchors with special chars, and image embeds inside GITHUB_SETUP.md; each must resolve via `Test-Path` (case-sensitive comparison where possible — Windows filesystem is case-insensitive but GitHub is case-sensitive, so `Test-Path` alone can mask case mismatches; compare against `git ls-files` output).
- [ ] E-10: README section anchor integrity — GitHub generates anchors by slugifying headings; any in-README `#section` links must match the slugified heading (emoji/ampersand rules differ from naive lowercase).
