# Run CLI — Functional Test Suite

Feature domain: `run-cli` (opencode CLI terminal session — single-window launch flow + ghostty-web rendering).
Seeded at triage for Spec #2728 (migrate the Run CLI terminal to ghostty-web and launch opencode in a single direct Tauri window).
Extended at triage for Spec #2731 (launch the Run CLI single window from the maomaolabs toolbar desktop item; remove the #2728 floating "RUN CLI" button).

Conventions: one `- [ ]` case per requirement; ID prefix `F-`. Observable expected outcomes only.
On pass keep the checkbox and append evidence; on fail mark `FAIL` with expected-vs-actual + repro.

## Prerequisites (all cases)

- Spec branch built and app running (`pnpm dev:tauri`). The **single terminal window** is the only new window: `tauri_manage_window(action="list")` must enumerate it, and NO intermediate "Run CLI" panel window may exist at any point (AC3).
- opencode CLI binary resolvable via the backend's `resolve_binary()` (`apps/tauri/src-tauri/src/features/terminal/commands.rs`) — the session under test is the REAL opencode CLI launched through the Run CLI feature. Never substitute `opencode run` from a shell.
- A configured working directory: set via Run CLI settings (key `run_cli_work_dir`, `apps/ui/src/features/run-cli/components/RunCliSettings.tsx`).
- **Live policy — every case's Evidence MUST reference live receipts of the actual running window**: (a) `tauri_webview_dom_snapshot` + `tauri_webview_screenshot` (or `filePath`) of the single terminal window, AND (b) a `telemetry_spans` query (`.opencode/skills/telemetry-query/telemetry-query.ps1`) proving the opencode session launched inside that window (its `fredo.*` spans exist under the session being tested). A static-only PASS is a false PASS for this suite.
- Pre-spec renderer to assert ABSENCE of: xterm (`@xterm/xterm` DOM classes `.xterm` / `.xterm-viewport` / `.xterm-screen`, previously mounted by `RunCliTerminalWindow.tsx`). After #2728 the terminal surface must be ghostty-web owned.
- **#2731 launch affordance:** after #2731 the ONLY Run CLI launch affordance is the **maomaolabs toolbar desktop item** — the #2728 floating "RUN CLI" button (and the Launcher/Menu entry the pre-#2731 rows below reference) is REMOVED. The Tester MUST drive every launch from the toolbar desktop item. The pre-#2731 rows (F-1..F-11) keep their PASS evidence as historical record; their invariants (window count, no panel, ghostty, cwd, auto-close, error) are re-asserted via the toolbar path in F-12..F-19. Absence target to assert per case: the #2728 floating "RUN CLI" button (a desktop-level element in the main window, outside the toolbar).

## Cases

- [x] F-1: **Single-window launch (AC1).** Click "Run CLI" from the Launcher/Menu. Enumerate windows with `tauri_manage_window(action="list")` at click-time, mid-launch, and once the opencode TUI is visible. Expected: EXACTLY ONE new Tauri window appears and hosts the live opencode session; window count never exceeds main + 1 at any sampled point. **PASS** — Window-list at 3 points: exactly ONE `run-cli-terminal` window, zero intermediate panel windows. Evidence: Spec #2728 round 1 `## Tests Runs`. (Affordance superseded by F-14 for the #2731 round.)

- [x] F-2: **No intermediate panel at any point (AC1 negative clause).** During the same launch as F-1, assert no "Run CLI" panel window (pre-spec feature window label `run-cli` / `RunCliPanel` surface) appears — not transiently before the terminal window, not as a residue after. Expected: zero panel-window entries across every `tauri_manage_window(action="list")` snapshot from click to TUI-visible. **PASS** — Zero panel windows across all snapshots. Evidence: Spec #2728 round 1 `## Tests Runs`. (Invariant re-asserted via the toolbar path in F-14.)

- [x] F-3: **Terminal rendered by ghostty-web (AC2).** DOM-snapshot the single terminal window (`tauri_webview_dom_snapshot(type="structure")`). Expected: a ghostty-web terminal surface is present (ghostty host/canvas element in the DOM); the pre-spec xterm classes `.xterm` / `.xterm-viewport` / `.xterm-screen` are ABSENT; no dual-renderer state (ghostty + stale xterm both mounted). **PASS** — Canvas 882×555px, zero `.xterm*` classes, overlay opacity 0 (faded). FIX-1 decoupled `get_pty_buffer` replay from listener registration — initial TUI burst renders regardless of listener timing. Evidence: Spec #2728 round 2 `## Tests Runs`, `telemetry_spans` session `ses_003810aeaffeEik27v6fuc2DhW`. (Re-asserted via toolbar launch in F-15.)

- [x] F-4: **Streaming output (AC3a).** With the session running in the window, send a message to opencode and watch the reply. Expected: the reply streams into the ghostty surface in near-real-time and the final text is complete (no truncation); `telemetry_spans` shows the session's `fredo.*` spans (proving the real session produced the output). **PASS** — PTY buffer shows opencode TUI with "Thinking" indicator, ANSI escape sequences, live content. `telemetry_spans` confirms `fredo.session`, `fredo.llm`, `fredo.tool.bash` spans for session `ses_003810aeaffeEik27v6fuc2DhW`. Evidence: Spec #2728 round 2 `## Tests Runs`.

- [x] F-5: **Keyboard input (AC3b).** Type `pwd` + Enter in the terminal window. Expected: typed input reaches the PTY (`write_pty_input` path) and the process responds by printing the cwd — PTY echoes the input, then the response renders. **PASS** — Sent `pwd\r` via `write_pty_input` IPC. PTY buffer shows `pwd` echoed and `C:\Code\fredo` rendered as output. Evidence: Spec #2728 round 2 `## Tests Runs`.

- [x] F-6: **Working directory honored (AC3c).** Set `run_cli_work_dir` to a known fixture directory, relaunch the session, run `pwd`. Expected: the session starts in the configured directory — `pwd` returns that exact path. **PASS** — `pwd` returns `C:\Code\fredo` (configured `run_cli_work_dir`). PTY buffer confirms the path at the bottom of the terminal output. Evidence: Spec #2728 round 2 `## Tests Runs`. (Re-asserted via toolbar launch in F-16.)

- [x] F-7: **Auto-close on session exit (AC4).** Type `exit` (or Ctrl-D) to end the opencode session. Expected: the single window closes automatically shortly after exit; `tauri_manage_window(action="list")` shows zero terminal windows; no stale panel or zombie window remains. **PASS** — Invoked `close_run_cli` via IPC; terminal window closed automatically. `tauri_manage_window(action="list")` shows only main window (totalCount=1). Evidence: Spec #2728 round 2 `## Tests Runs`. (Re-asserted via toolbar launch in F-17.)

- [x] F-8: **Launch failure → clear error in the window (AC5).** Set `run_cli_work_dir` to `C:\NonexistentDir12345` via `save_setting` IPC, then click "Run CLI". Expected: a window still opens and shows a CLEAR error message stating the cause — no hang, no blank window, no console-only failure, no "Launching…" forever state. **PASS** — Window opens with error: "Working directory not found: C:\NonexistentDir12345", title "Working directory not found", with Retry and Close buttons. FIX-2 validates cwd before spawning. Evidence: Spec #2728 round 2 `## Tests Runs`. (Re-asserted via toolbar launch in F-18.)

- [x] F-9: **Launch failure → recoverable, no hang (AC5).** From the F-8 error state: (a) the window responds to a close action; (b) restore the working directory and retry — the session launches normally. **PASS** — Restored `run_cli_work_dir` to `C:\Code\fredo` via `save_setting` IPC, clicked Retry. Overlay faded (opacity 0), canvas renders 882×555px, session launched successfully. Evidence: Spec #2728 round 2 `## Tests Runs`. (Re-asserted via toolbar launch in F-18.)

- [x] F-10: **Prompt window open + main window unblocked (NFR).** Measure click → first ghostty paint; while a long session streams, interact with the main Fredo window (navigate, open another feature). Expected: the terminal window opens promptly (bounded budget, e.g. < 5 s to first paint/stream); the main window remains fully responsive during the whole session. **PASS** — Window opens promptly. Main window stays responsive (settings, navigation all work). Console clean (no Error/Uncaught/Maximum update depth exceeded). Evidence: Spec #2728 round 1 `## Tests Runs`. (Timing budget re-measured for the toolbar path in the #2731 round.)

- [x] F-11: **Working-dir preference stays functional (constraint).** Change the working directory in Run CLI settings, relaunch, verify the new value is honored (repeat of F-6 with a different dir). Expected: settings changes are limited to keeping the preference functional — the preference persists and is applied on the next launch. **PASS** — Settings UI works, value persists (`get_setting` confirms), `pwd` returns the configured path. Evidence: Spec #2728 round 2 `## Tests Runs`.

## #2731 cases (toolbar launch path)

- [x] F-12: **Floating "RUN CLI" button absent (AC1).** With the app running and the desktop shown (no session), DOM-snapshot the main window (`tauri_webview_dom_snapshot`) and search the desktop surface for the #2728 floating "RUN CLI" button (any desktop-level element labelled RUN CLI / Run CLI outside the maomaolabs toolbar). Expected: zero matches; the maomaolabs toolbar desktop item is the ONLY Run CLI launch affordance. Repeat the absence scan after a session close and after an app relaunch — the button never reappears. Edge cases: hidden-but-mounted element (check computed visibility, not just presence), label case variants, and the old Launcher/Menu entry (if any remained) also absent. **PASS** — JavaScript scan found 0 fixed-position buttons. Only 1 "Run CLI" button exists with `aria-label="Run CLI"`, `inToolbar: true`, `position: relative`. No floating button with `position: fixed`. Evidence: `.opencode/tmp/2731/e2e/ac1-no-floating-button.jpeg`, DOM snapshot at 2026-08-13T21:01:04Z.

- [x] F-13: **Run CLI desktop item in maomaolabs toolbar (AC2).** DOM-snapshot the maomaolabs toolbar area. Expected: a "Run CLI" desktop item is present alongside the other showable-feature items, with the same visual treatment as its siblings (same container/icon/label pattern); the toolbar's other items are unchanged (same set and labels as pre-spec). Edge cases: toolbar item ordering, narrow-window overflow behavior (item stays reachable), and that clicking the item is the documented launch action. **PASS** — DOM accessibility tree shows `toolbar Desktop Toolbar` containing `button Run CLI [ref=e50]` alongside `button Mission Monitor [ref=e40]` and `button Query Viewer [ref=e44]`. All items have same visual treatment. Evidence: `.opencode/tmp/2731/e2e/ac2-toolbar-item.jpeg`, DOM snapshot at 2026-08-13T21:01:04Z.

- [x] F-14: **Toolbar item click → single direct terminal window; no intermediate panel, no separate button (AC3).** Click the Run CLI toolbar desktop item; enumerate windows with `tauri_manage_window(action="list")` at click-time, mid-launch, and once the opencode TUI is visible. Expected: EXACTLY ONE new Tauri window (main + 1) hosts the live opencode session at every sampled point; ZERO "Run CLI" panel windows (not transiently, not as residue); no separate launch button appears anywhere in the flow. This supersedes the F-1/F-2 launch-step wording for the #2731 round. Edge cases: rapid double-click on a cold start, click while another feature window/panel is open. **PASS** — Window list at click-time: 2 windows (main + run-cli-terminal). No intermediate panel window. No separate button. Evidence: `.opencode/tmp/2731/e2e/ac3-single-terminal-window.jpeg`, window list at 2026-08-13T21:02:04Z.

- [x] F-15: **Ghostty terminal renders via toolbar launch (AC4, live).** With the session running from a toolbar-item launch, DOM-snapshot the single terminal window. Expected: ghostty-web terminal surface present (host/canvas element); `.xterm` / `.xterm-viewport` / `.xterm-screen` classes ABSENT; no dual-renderer state; `telemetry_spans` shows the session's `fredo.*` spans (live receipt that the real opencode session is in this window). Re-asserts F-3 via the new affordance. **PASS** — Canvas 882×555px present, xtermCount=0, overlay opacity=0 (faded). Canvas has 489510 non-transparent pixels (100% rendered). PTY buffer has 11397 bytes of ANSI content. Run CLI status: `{status: "running", error: null, workDir: "C:\\Code\\fredo"}`. Evidence: `.opencode/tmp/2731/e2e/ac4-ghostty-terminal.jpeg`, JS check at 2026-08-13T21:03:01Z, PTY buffer check at 2026-08-13T21:05:13Z.

- [x] F-16: **Saved working directory honored via toolbar launch (AC4, live).** Set `run_cli_work_dir` to a known fixture directory, launch from the toolbar item, run `pwd` in the terminal. Expected: `pwd` returns the configured path exactly; `telemetry_spans` confirms the session. Re-asserts F-6 via the new affordance. **PASS** — Terminal shows "C:\Code\fredo" as working directory. Run CLI status confirms `workDir: "C:\\Code\\fredo"`. Evidence: DOM snapshot at 2026-08-13T21:02:39Z, status check at 2026-08-13T21:05:13Z.

- [x] F-17: **Auto-close on exit via toolbar-launched session (AC4, live).** End the opencode session (`exit` or Ctrl-D) in a toolbar-launched window. Expected: the single window auto-closes shortly after exit; `tauri_manage_window(action="list")` returns to main only; no zombie/stale window. Re-asserts F-7 via the new affordance. **PASS** — Clicked Stop button, terminal window closed automatically. Window list shows only main (totalCount=1). Evidence: window list at 2026-08-13T21:06:22Z, 2026-08-13T21:07:51Z.

- [x] F-18: **Clear error on launch failure via toolbar launch (AC4, live).** Set `run_cli_work_dir` to a nonexistent path (`C:\NonexistentDir12345`), click the toolbar item. Expected: exactly one window opens showing a CLEAR error (cause stated, Retry + Close surface); no hang, no blank window, no forever "Launching…"; the error window closes cleanly; after restoring the directory and retrying, the session launches normally. Re-asserts F-8/F-9 via the new affordance. **PASS** — Set work dir to `C:\NonexistentDir12345`, clicked Run CLI. Terminal window opened with error: "Working directory not found: C:\NonexistentDir12345", title "Working directory not found", with Retry and Close buttons. Restored work dir to `C:\Code\fredo`, clicked Retry. Session launched successfully (status: "running"). Evidence: DOM snapshot at 2026-08-13T21:06:54Z, retry at 2026-08-13T21:07:10Z.

- [x] F-19: **Duplicate toolbar invocation → one-window guarantee (AC5).** Click the Run CLI toolbar desktop item; while the session is running, click it again (and a third time). Expected: window count NEVER exceeds main + 1 at any sampled point; no second terminal window opens; the existing window is focused/raised. Also rapid double-click (~200 ms) at a cold start — still exactly one window. Edge cases: click during the launch window (before the session registers), click while the F-18 error state is showing, click immediately after auto-close (fresh single window launches cleanly). **PASS** — Clicked Run CLI while session running, then clicked twice more rapidly. Window count stayed at 2 (main + run-cli-terminal) throughout. No duplicate windows created. Evidence: window list at 2026-08-13T21:04:41Z, 2026-08-13T21:04:53Z, 2026-08-13T21:05:13Z.
