# window-engine-cleanup — Functional

Per-requirement test cases for Issue #2810 (Remove `@maomaolabs/core` from the frontend). Verification policy: **static** (pure deletion — verifiable without a running system).

- [x] F-1 (AC1): `apps/ui/package.json` no longer lists `@maomaolabs/core` in `dependencies` or `devDependencies`; `apps/tauri/package.json` likewise. Expected: both manifests are free of `@maomaolabs/core`, and `pnpm-lock.yaml` contains no `@maomaolabs/core` importer (for `apps/ui` and `apps/tauri`) nor package/resolution entry. Edge: grep the lockfile for `@maomaolabs/core` → 0 matches; manifest JSON still parses (no comma/bracket syntax error left).
  - **PASS.** Read `apps/ui/package.json` (48 lines) + `apps/tauri/package.json` (30 lines) — neither lists `@maomaolabs/core` in `dependencies` or `devDependencies`; both are well-formed JSON. Grep `pnpm-lock.yaml` (root lockfile, `include: pnpm-lock.yaml`) for `@maomaolabs/core` → **0 matches** (entire lockfile re-grep is clean). The workspace manifests parse cleanly (both builds consumed them without error).
- [x] F-2 (AC2): `apps/ui/src/main.tsx` no longer imports `@maomaolabs/core/style.css` (or any `@maomaolabs/core/*` path); `apps/tauri/src/main.tsx` likewise. Expected: zero import statements referencing `@maomaolabs/core` anywhere under `apps/ui/src` and `apps/tauri/src`. Edge: case-insensitive grep — no differently-cased or comment-embedded reference; surrounding import block still parses.
  - **PASS.** Read `apps/ui/src/main.tsx` (line 3 = `./style.css`, line 5 = `reactflow`) + `apps/tauri/src/main.tsx` (imports from `@fredo/ui` only) — neither imports any `@maomaolabs/core` path. Case-insensitive grep `(?i)@maomaolabs/core` over `apps/ui/src` → **0** and over `apps/tauri/src` → **0**. Positive controls confirmed the grep tool searched these dirs (`reactflow` in apps/ui/src = 60 matches; `@fredo/ui` in apps/tauri/src = 1).
- [x] F-3 (AC3): `apps/ui/src/features/home/fredo-desktop.css` shim is deleted. Expected: `Test-Path` returns `False`; nothing under `apps/ui/src` (or via the `@fredo/ui/styles` export / tauri import / vite alias) still imports or references `fredo-desktop.css`. Edge: if the file were retained/re-purposed, it must contain zero `@maomaolabs/core` class selectors and be unimported (deletion is the expected path for this removal-only slice).
  - **PASS.** `Test-Path apps/ui/src/features/home/fredo-desktop.css` → **False** (shim deleted, 17 lines removed per commit `02b91d0`). Grep whole `apps/` for `fredo-desktop` → **0**; grep `apps/` for `@fredo/ui/styles` → **0** (vite alias + export consumers route removed in the same commit).
- [x] F-4 (AC4): a recursive, case-insensitive grep of the entire `apps/ui/` tree for `@maomaolabs/core` returns zero matches, including comments and strings. Expected: 0 occurrences (mission-monitor comment, `windowTypes.ts`, shim, etc.). Edge: exclude `node_modules`/build artifacts; a belt-and-suspenders repo-root grep (excluding `node_modules`) surfaces + flags any reference outside `apps/ui/` — a repo-wide hit is FAIL-adjacent.
  - **PASS.** Recursive case-insensitive grep `(?i)@maomaolabs/core` over the whole `apps/ui/` tree → **0 matches**. Repo-root grep `@maomaolabs/core` → hits only under `docs/agentic-pipeline/` and `.opencode/tests/*` (documentation/test text, NOT `apps/`) — no `apps/ui` or `apps/tauri` source/manifest ref. Test-package installed-check: `Test-Path node_modules/@maomaolabs/core` → False, `Test-Path node_modules/.pnpm/@maomaolabs+core@1.1.0` → False (no residual package to hide a reference). NOTE (E-1): two bare "maomaolabs" comments remain in `apps/ui/src/features/run-cli/` (RunCliFeature.tsx:17, RunCliLauncher.tsx:17) — they reference the retired tool by bare name, NOT the `@maomaolabs/core` package path, so they do not violate this AC; informational only.
- [x] F-5 (AC5): `pnpm --filter @fredo/ui build` from repo root exits 0. Expected: no `Cannot find module '@maomaolabs/core…'` / unresolved-module error. Edge: a build error naming `@maomaolabs/core` is FAIL regardless of exit code; clear stale `.vite`/`node_modules/.cache` before trusting the clean build.
  - **PASS.** `pnpm --filter @fredo/ui build` (script `tsc && vite build`) → exit **0**, "✓ built in 8.94s", 2570 modules transformed, no `Cannot find module '@maomaolabs/core'` / unresolved-module error (only a benign chunk-size warning). `pnpm --filter @fredo/tauri build:webview` → exit **0**, "✓ built in 9.03s", 2571 modules transformed, no unresolved `@fredo/ui/styles`/`@maomaolabs/core` module. cargo-check leg: NOT run — `cargo check` is outside the bash allowlist (tool-access gap); not implicated because no `apps/tauri/src-tauri/**/*.rs` file changed in the slice and `@maomaolabs/core` is a frontend-only npm dependency.
