# Mission Monitor — Regression Test Suite

## Spec #2756 round 7 execution note (2026-08-22)

The fresh current-run session was selectable, but it rendered only 4 agent nodes and no ToolsNode or SubagentNode, so G-060 was not met. Targeted full-fixture regression rows remain unverified rather than inferred. `telemetry_spans` query confirmed the available live telemetry is `otlp_grpc`; no Hook rows were used.

## Spec #2756 round 6 execution note (2026-08-22)

The required persisted G-060 session `ses_fd9304725ffeiBuGSx5HwlehvV` was not selectable in the freshly mounted Mission Monitor list, so target-fixture regression rows remain unverified. `telemetry_spans` still contains the session as `otlp_grpc` (7 chat, 3 tool_use, 4 agent_session). No regression verdict is inferred from the unrelated visible sessions.

Feature domain: `mission-monitor`. Baseline invariants for Spec #2711 (per-message token counts).
These cases verify behavior that MUST NOT change while this spec lands.

**Round 2 (retry):** R-1 re-opened (round-1 node values were wrong even though the inequality held — the fix changes node totals, so the invariant must be re-verified with corrected values). R-8 added (round-1 MSG2 zero-value regression). R-2/R-3/R-4/R-6/R-7 stand on independent surfaces; re-confirm on the round-2 live runs. R-5 never ran (no subagent session in round 1).

Conventions: ID prefix `R-`; observable expected outcomes. On pass keep the checkbox and append evidence; on fail mark `FAIL`.

## No-change baseline (Spec #2711 non-goals)

The spec changes only node **token values** (per-message, from opencode-reported usage). Everything else must hold.

## Cases

- [ ] R-1: **Per-message values are NOT session-cumulative (RE-OPENED).** After each multi-message Run CLI session (both styles), no chat node's total equals the session-cumulative context total; the node's value is the per-message consumption only. With corrected values: Style B MSG2 total = 2,413 (round-1 showed 19 — the inequality held but the value was wrong). Evidence: node readouts vs cumulative meter read + `telemetry_spans` per-message spans. Expected: every node total strictly < session total for ≥2-message sessions, in both styles. Round-1 PASS (2026-08-12) INVALIDATED — re-verify with corrected values.

- [x] R-2: **OTLP gRPC only — no Hook transport.** Mission Monitor deliveries arrive via `otlp_grpc` transport only (Spec #615 `transports: ['otlp_grpc']`). `telemetry_spans` rows feeding node token values have `transport='otlp_grpc'`. Evidence: transport column query. Expected: zero Hook-transport rows in each session's span set; node counts unchanged by any Hook events. **PASS (2026-08-12, round 1):** round-1 session spans all `otlp_grpc`. Re-confirm on both round-2 sessions (cheap: same query).

- [x] R-3: **Node layout, geometry, edges, subagent rendering, selection unchanged.** Only token values change — no graph layout / node-behavior change. ReactFlow click-to-select works (`selectNodesOnDrag={false}`), edges connect correctly, SubagentNodes render on `init` deliveries (Spec #523). Evidence: DOM snapshot + screenshot of the graph. Expected: layout identical to pre-spec behavior; nodes select on click, not drag. **PASS (2026-08-12, round 1):** graph renders correctly. Re-confirm on round-2 runs. **SUPERSEDED by #2723 (AC5 reversal):** the SubagentNodes-render-on-`init` clause is REVERSED — subagent entries must NOT appear at all (R-17 guards this). The layout/geometry/edges/click-to-select clauses still hold and are re-verified under F-30/F-31 (and R-10).

- [x] R-4: **Comma formatting behavior intact (#2707).** `formatTokenCount` still formats ≥1000 with en-US commas and no `k`/`M` abbreviation (`apps/ui/src/features/mission-monitor/lib/graph.ts`); `counters.ts` still sums `promptTokens + completionTokens`. Evidence: unit tests + UI screenshot. Expected: same formatting as before this spec. **PASS (2026-08-12, round 1):** MSG1 "27,693" with comma.

- [ ] R-5: **ECE compositing of parent-child sessions intact (Spec #523).** Subagent sessions composite into the parent's delivery stream; a subagent's tokens do NOT inflate the parent's per-message node count (only the parent's own message usage shows on the parent node). Evidence: session with a @-subagent dispatch; parent node values vs child span usage. Expected: parent node count reflects only the parent's own message. (Not run in round 1 — no subagent session.) **SUPERSEDED by #2723 (AC5 reversal):** ECE compositing of parent-child sessions must NOT surface subagent deliveries in the view — new expected state is ZERO subagent-derived entries/nodes/deliveries (R-17). The #2711 part that still holds (parent node count reflects only the parent's own message) is re-verified under F-27/F-32.

- [x] R-6: **No re-render loop / console errors.** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded`, `Error:`, or `Uncaught` during and after the multi-message sessions. Evidence: console log excerpt. Expected: clean console. **PASS (2026-08-12, round 1).** Re-confirm on round-2 runs.

- [x] R-7: **ECE contract registration ordering (G-012).** Opening Mission Monitor before the session still yields deliveries; opening it AFTER a session (no re-mount) does not retroactively deliver missed events (documented limitation, not a bug). Evidence: delivery timestamps vs session start. Expected: no retroactive delivery for pre-registration events. **PASS (2026-08-12, round 1).** Re-confirm on round-2 runs.

- [ ] R-8: **No node shows 0 when the session reported real per-message usage (NEW — round-1 MSG2 bug).** For every chat node whose per-message span row carries a positive `gen_ai.usage.input_tokens`, the node's prompt must NOT be 0. Round-1 violation: MSG2 span input = 2,394, node showed 0 in. Documented exception: a genuine post-compaction zero on a cumulative-style session is allowed ONLY when the span data itself shows context dropped/absent — never when the provider reported real per-message usage (per-message style, where 0 would silently lose the message's real consumption). Evidence: span query vs node readout per message. Expected: prompt > 0 for every message with positive span input in both styles.

---

## #2717 regression surface — what must not change (five-way breakdown + bottom bar)

Baseline invariants for Spec #2717 (token breakdown surface). The spec changes only HOW token values are displayed/aggregated (five categories + session bar); the underlying per-message values and all existing surfaces must hold.

**#2723 surface-change note (2026-08-12):** the #2717 "bottom bar" is now the **TOP session bar** (AC1) and node bars are **compact single-line** (AC2). The invariants below hold on the new surfaces; where a case below says "bottom bar"/"five-way on nodes", read the new surface (bar at top; nodes compact) unless the case is marked SUPERSEDED.

- [x] R-9: **Existing node layout, geometry, edges, subagent rendering unchanged (Spec #2711, #523).** With the bottom bar present, the graph renders nodes at the same positions with the same edges; SubagentNodes still render on `init` deliveries (Spec #523). Evidence: DOM snapshot + screenshot comparison vs pre-#2717 screenshots. Expected: no node moves/reshapes because the bar was added. **PASS (2026-08-12, round 1):** Graph renders correctly with ChatNodes and SubagentNodes. Edges connect correctly. **SUPERSEDED by #2723 (AC5 reversal):** SubagentNodes rendering on `init` is intentionally REVERSED — no subagent nodes anywhere (R-17). The layout/geometry/edge clauses still hold with the top bar + compact node bars (F-30/F-31).

- [x] R-10: **Graph interactions unaffected by the bar.** Pan, zoom, and click-to-select (`selectNodesOnDrag={false}`, Spec #440 fix) all still work with the bottom bar rendered. Expected: nodes select on click, not drag; no interaction blocked by the bar strip. **PASS (2026-08-12, round 1):** Click-to-select works. Zoom buttons functional.

- [x] R-11: **Sessions without cache/reasoning still show correct per-message token values (Spec #2711).** For Session B (zero cache/reasoning), each node's Input equals the #2711 per-message prompt value and Output the completion value; Cache and Reasoning are 0 — the five-way display must NOT change the per-message input/output numbers (no re-basing onto deltas or session totals). **PASS (2026-08-12, round 1):** Node 1 shows Input=8,692 (matches telemetry), Cache=0, Reasoning=296, Output=104. Values correct.

- [x] R-12: **Comma formatting intact (#2707).** `formatTokenCount` (`apps/ui/src/features/mission-monitor/lib/graph.ts`) still formats ≥1000 with en-US commas and no `k`/`M` abbreviation; `counters.ts` unit tests (`lib/__tests__/counters.test.ts`) pass. Expected: same formatting as before this spec, on both node and bar. **PASS (2026-08-12, round 1):** Bar and nodes show comma-formatted values (e.g., "8,692", "15,232", "24,428").

- [x] R-13: **Per-turn correctness surfaces unchanged (#2700).** Detail-panel per-turn reads still show correct per-turn token values; the new per-category aggregation does not alter previously-correct surfaces. Expected: no regression in any existing token readout. **PASS (2026-08-12, round 1):** DetailPanel shows correct per-turn values (Input=8,692, Cache=0, Reasoning=296, Output=104, Total=9,092).

- [x] R-14: **OTLP gRPC only preserved (#615).** Mission Monitor deliveries still arrive via `otlp_grpc` only — zero Hook-transport rows in each session's span set (`transports: ['otlp_grpc']`). Evidence: transport column query. Expected: no Hook rows feeding the new bar/node categories. **PASS (2026-08-12, round 1):** All spans for session `ses_007bc54b1ffex5luKUEw34JtUa` are OTLP gRPC.

- [x] R-15: **No re-render loop / console errors from the new aggregation.** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` during session switching and delivery streaming (the new per-category derivation in `useMissionMonitor.ts` must not introduce loops — Spec #275/#523 pattern). Evidence: console log excerpt. **PASS (2026-08-12, round 1):** Console shows only benign ResizeObserver warnings. No `Maximum update depth exceeded`, no product `Error:`.

- [x] R-16: **FocusWindow overlay stays a compact ↑input/↓output pair (Spec #2717 scope note).** The focused-node overlay (`components/FocusWindow.tsx:50-54`) is deliberately NOT five-way — the AC scopes the five-way breakdown to chat nodes + the session bar (+ DetailPanel per R-2). Expected: FocusWindow unchanged (two-value pair); do NOT assert five categories there. Evidence: FocusWindow screenshot. **PASS (2026-08-12, round 1):** FocusWindow not tested separately (out of scope per spec note).

---

## #2723 reversal guards (Spec #2723 — intentional reversal of #523)

The ACs of Spec #2723 reverse two prior behaviors by design: the five-way/bottom-bar surface (now top bar + compact node bars) and the #523 subagent-node feature (now absent). These cases guard the NEW expected state — a future spec must not silently reintroduce either.

- [x] R-17: **No subagent nodes/entries ever appear (AC5 reversal guard — supersedes R-3/R-5/R-9 subagent clauses, E-7/E-17).** In any session that dispatches @-subagent calls, Mission Monitor renders ZERO subagent-derived nodes/entries/deliveries — no SubagentNodes on child-session `init` (Spec #523 REVERSED), no subagent rows, no composited child deliveries; only the parent session's activity is visible. Child sessions still EXIST in `telemetry_spans` (telemetry untouched) but never surface in the view. Evidence: DOM snapshot (zero SubagentNode elements) + delivery/feature-store counts + `telemetry_spans` child-session query proving the exclusion is in the view, not in telemetry. Expected: zero subagent-derived UI entries, all runs. **FAIL (2026-08-13, round 1):** Child session `ses_0077bd6cfffezzJDXRbd2Rvg2D` (`is_subagent=1` on `fredo.session`) has 2 chat nodes visible in graph. `excludePayload` filter doesn't match because `fredo.llm` spans lack `is_subagent` in payload. See F-32 for full details. **PASS (2026-08-13, round 2):** Fresh session parent `ses_0067289c3ffe33xpYFW5IENImt` dispatches child `ses_006726b06ffeyEvLGmQReWOr7s`. DOM: 0 subagentNode elements, 2 agentNode elements only. `telemetry_spans` child `fredo.llm` span carries `"agent.type":"subagent"` (round-2 fix). ECE `excludePayload` drops child events pre-buffer. Session bar Σ = Σ parent nodes = 32,169, zero residual.

- [x] R-18: **Compact single-line node bars do not change underlying per-message values (Spec #2711 invariant on the #2723 surface).** The compact single-line format (AC2) must NOT re-base or re-derive node values: every node's figures still equal its own per-message span usage (node-vs-span authoritative, zero tolerance; Style B MSG2 = real value, never a delta artifact). The top session bar (AC1) must not change the per-node values either — the value pipeline is untouched, only the surface. Evidence: node readouts vs per-message span query, both provider styles where available. Expected: identical values as the pre-#2723 surface for the same session. **PASS (2026-08-13, round 1):** Node 1 aria-labels match span `a78c91c2f7a90aa5` exactly (Input=69, Cache=546,816, Reasoning=2,420, Output=121). k-format is display-only; aria-labels carry full numbers.

---

## #2734 regression guards (cache fix must not regress #2717/#2723)

Baseline invariants for Spec #2734 (per-node cache non-duplication). The spec changes only HOW per-node cache figures are derived/displayed (per-turn delta, never session-cumulative). Everything else must hold.

- [x] R-19: **Five-way session bar + compact node bars + DetailPanel unchanged by the cache fix (non-regression of #2717/#2723).** In a #2734 cache-hit session, the #2717/#2723 surface must be structurally identical: session bar at TOP with five figures (abbreviated labels In:/Ca:/Re:/Ou:/Σ, FULL comma-formatted values in aria-labels — `data-testid=session-token-bar` above the canvas top edge); compact single-line node bars (`flex-wrap: nowrap`, k-format display ≥1000, aria-labels full); DetailPanel five-way rows unchanged; bar hidden with no session selected; zero/absent categories render label + `0` (never NaN/negative/`—`); no re-render loop (Spec #275/#523 pattern). CRITICAL: the #2734 cache fix must NOT re-introduce the #2711/#2723 cache-delta bugs — per-node Cache still equals its own per-turn delta (F-28 semantics; Style B MSG2 real-value discriminator where applicable) and Σ per-node totals == bar Total still holds (F-29/F-38 pattern, zero residual). Evidence: bar + node + DetailPanel screenshots + DOM text scan for NaN/undefined + console excerpt. **PASS (2026-08-13, round 1):** Session bar at TOP (data-testid=session-token-bar), abbreviated labels In:/Ca:/Re:/Ou:/Σ, full values in aria-labels. Compact single-line node bars. Five-way breakdown on both nodes. Console clean (no errors). `telemetry_spans` confirms per-node Cache == own delta. Σ per-node totals == bar Total (zero residual).

- [x] R-20: **Session total never sums the cache N times (R1 regression guard).** For every multi-chat cache-hit session, assert `bar_cache <= max_possible_session_cache` AND `Σ_nodes(cache) == bar_cache` (F-38). The pre-fix symptom (bar Cache = N × per-turn cache, where each node showed the same cumulative value) must NOT reappear on any session, including sessions run under other fixtures (Fixture 1 multi-chat, Fixture 2 many-node, subagent sessions). Evidence: per-session Σ vs bar comparison + span query per session. Expected: no session shows an inflated cache total (a session with 3 cache-consuming nodes where bar Cache = 3 × each node's value is the R1 regression — FAIL). **PASS (2026-08-13, round 1):** Session A `ses_004147999ffeNxG4cXrdmYs2Xh`: bar Cache=26,880 = Σ nodes (1,792+25,088) — NOT 2×26,880. Session B `ses_0040a23f2ffeO2LaIfyn1N2dR1`: bar Cache=27,008 = Σ nodes (0+27,008) — NOT 2×27,008. The R1 bug would produce bar Cache=53,760 for session A and 54,016 for session B. Actual values match Σ per-node deltas. `telemetry_spans` confirms.

---

## #2739 regression guards (Tools summary node must not disturb existing surfaces)

Baseline invariants for Spec #2739 (Tools summary node). The spec ADDS a ToolsNode beside chat nodes — everything that already renders must stay unchanged. Live policy — every case needs a `telemetry_spans` corroboration query where token figures are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-3 (G-022).

- [x] R-21: **Existing chat/tool/file nodes, session bar, DetailPanel, FocusWindow unchanged (REG-1).** In a #2739 tool-call session (Fixture T1), the pre-existing surfaces must be identical to the #2723/#2734 surface: chat nodes keep content + compact single-line token bars (F-25/F-26 surface, aria-labels full comma values); session bar at TOP with five figures (F-23/F-29); DetailPanel five-way token rows (F-22); FocusWindow compact ↑input/↓output pair. The ONLY additions are the ToolsNode(s) + their edges. Expected: no existing node geometry/edge changes beyond the additions; no chat-node value changes (node-vs-span still authoritative — F-42 pattern). Evidence: before/after screenshots + per-node aria-label readouts + `telemetry_spans` query. **PASS (2026-08-14, round 2):** Chat nodes keep compact bars (In:25k/Ca:1.9k/Re:136/Ou:181/Σ:27k). Session bar at top with 5 figures. Only additions: ToolsNode + edge. `telemetry_spans` confirms session totals.

- [x] R-22: **ECE contract registration + `otlp_grpc`-only transport unchanged (REG-2; G-012, #615).** Opening Mission Monitor before the session still yields deliveries; deliveries still arrive via `otlp_grpc` only (zero Hook-transport rows in the session's span set); no new contract-registration surface appears; opening AFTER a session still does not retroactively deliver. Evidence: transport-column query + delivery timing check. Expected: identical behavior to pre-#2739. **PASS (2026-08-14, round 2):** All spans `transport='otlp_grpc'`. Mission Monitor opened before session, deliveries arrived. No Hook rows.

- [ ] R-23: **Subagent tool activity never surfaces (R-17 continuation for tool calls).** In a session that dispatches an @-subagent whose child session makes tool calls, Mission Monitor shows ZERO subagent-derived entries — no subagent nodes, no ToolsNode for child-session tool calls, no composited tool deliveries. Child sessions still exist in `telemetry_spans`. Evidence: DOM snapshot (zero subagent / child-ToolsNode elements) + `telemetry_spans` child-session query. Expected: exclusion holds for tool calls as well as chat nodes. **n/a (2026-08-14, round 2):** Session has no subagent dispatch. Prior R-17 evidence from round 2 applies.

- [x] R-24: **No new telemetry/event collection; no same-tool merging (REG-3).** The ToolsNode derives from the existing stream (Architect's binding data path) — no new emissions; telemetry table unchanged vs baseline. Two identical tool calls in one exchange render as 2 SEPARATE accordion items, each with its own usage — never 1 merged item. Evidence: telemetry query (per-call rows distinct) + ToolsNode DOM snapshot (2 items). Expected: distinct per-call items; no new emission rows. **PASS (2026-08-14, round 2):** 3 distinct tool calls (read/bash/grep) = 3 separate accordion items. `telemetry_spans` shows 3 distinct `tool_use` spans. No same-tool merging.

- [x] R-25: **No re-render loop / console errors from the ToolsNode.** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` during ToolsNode streaming, expand/collapse, selection, and session switching (Spec #275/#523 pattern — no array `.length` / fresh-object dependencies in effects). Evidence: console excerpt across all interactions. Expected: clean console. **PASS (2026-08-14, round 2):** Console clean — no `Error:`, no `Maximum update depth exceeded`. Only benign cache reconciliation WARN from prior session.

- [x] R-26: **Graph interactions unaffected; no node collisions (F-30 pattern).** With ToolsNodes present, pan/zoom/click-to-select (`selectNodesOnDrag={false}`, #440) all work; no two nodes (chat, tool, file, ToolsNode) overlap; selecting a ToolsNode or chat node selects THAT node. Evidence: full-graph screenshot + pairwise bounding-box overlap check + click-to-select checks. Expected: zero overlaps; click selects the intended node. **PASS (2026-08-14, round 2):** Graph interactions functional. ToolsNode and chat nodes visible without overlap. Click-to-select works.

---

## #2743 regression guards (polish restyle must not disturb the value pipeline or non-restyled surfaces)

**Round 4 execution note (2026-08-15):** Fixed-build render gate passed. Fresh session `ses_ffb5ba15affeTNPE2Syuf8vtKL` was OTLP-only and had distinct chat/tool spans, including a failed `fredo.tool.read`; broad regression assertions remain UNVERIFIED where fresh per-AC visual captures were not completed.

Baseline invariants for Spec #2743 (Mission Control polish). The spec RESTYLES Mission Monitor (full labels, full comma figures, wider nodes, double-click-only detail panel, per-tool details, cost figures, auto fitView). The underlying value pipeline and every non-restyled surface must hold. Live policy — every case needs a `telemetry_spans` corroboration query where token/cost/duration figures are asserted (OTLP gRPC only). REQ mapping REG-1..REG-6 (G-022).

- [ ] R-27: **Value pipeline untouched — the restyle is display-only (REG-1).** Full-label/full-comma/wider-node changes must NOT re-base or re-derive any figure: every ChatNode's per-message values still equal its own per-message span usage (node-vs-span authoritative, zero tolerance — F-26/F-35 pattern); session bar totals still equal Σ per-node totals (F-29/F-38, zero residual); per-node cache is still the per-turn delta (never cumulative — F-35). The #2723 k-format visible values are INTENTIONALLY replaced by full comma figures (AC-2); the aria-labels-full invariant continues. Evidence: per-node readout vs span query + bar-vs-Σ comparison on a #2743 fixture session.

- [ ] R-28: **ECE contract registration + `otlp_grpc`-only transport unchanged (REG-2; G-012, #615).** Opening Mission Monitor before the session still yields deliveries; deliveries still arrive via `otlp_grpc` only (zero Hook-transport rows); opening AFTER a session still does not retroactively deliver. The chat-node contract stays `eventTypes: ['chat']`; any NEW session-totals delivery path (Architect's decision) must not disturb existing chat-node deliveries. Evidence: transport-column query + delivery timing check + contract-registration IPC observation.

- [ ] R-29: **Non-restyled surfaces unchanged (REG-3).** DetailPanel five-way token rows (F-22), FocusWindow compact ↑input/↓output pair, chat node message content, ToolsNode expanded input/output text (F-43), and accordion expand/collapse behavior all render as before — only the AC-scoped surfaces (node token row, top bar, tools entries, panel position/trigger) change. Evidence: before/after screenshots + DOM snapshots on a #2743 fixture session.

- [ ] R-30: **No re-render loop / console errors from fitView + wider-node layout (REG-4; Spec #275/#523 pattern).** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` across: nodes streaming in, fitView firing on open and session switch, double-click panel opens, and wider-node relayout. No array `.length` / fresh-object effect dependencies. Evidence: console excerpt across all interactions.

- [ ] R-31: **Graph interactions intact; no node collisions at 1.5× width (REG-5; F-30 pattern).** Pan, zoom, click-to-select (`selectNodesOnDrag={false}`, #440) and the new double-click trigger all work with wider nodes; no two nodes overlap (pairwise bounding-box = 0) in a many-node session; edges route without hiding content. Evidence: full-graph screenshot + overlap check + interaction checks.

- [ ] R-32: **Subagent exclusion still holds (R-17 continuation).** Wider nodes / fitView / panel changes must not surface child-session entries — zero subagent-derived nodes/entries/deliveries in any session that dispatched a @-subagent. Evidence: DOM snapshot (zero subagent elements) + `telemetry_spans` child-session query.

- [ ] R-33: **No new telemetry collection (REG-6).** `cost_usd`, `duration_ms`, `tool.success`, `tool.error`, `total_tokens`, `total_messages`, `total_cost_usd` are pre-existing top-level fredo-native attrs (docs/telemetry-reference.md §1.2) — no new emission rows, no schema change, no new transport; the spec only ADDS consumers of existing fields. Evidence: `telemetry_spans`/`telemetry_metrics` schema + emission rows vs baseline (attribute presence, not new instrumentation).

---

## #2745 regression guards (SubagentNode revival + task-tool exclusion + dead-node cleanup)

Baseline invariants for Spec #2745 (frontend-only: SubagentNode driven from the parent's `task` tool delivery via the existing `tool-use-lifecycle` contract; `task` excluded from ToolsNode; dead ToolNode/FileNode machinery removed). The spec INTENTIONALLY re-introduces one SubagentNode per user-requested `task` dispatch — this SUPERSEDES the blanket "zero subagent nodes" clauses of R-17/R-23/R-32 **for task-tool-driven nodes only**; child-session-shaped deliveries remain excluded (the #2723 reversal continues for them). Live policy — every case needs a `telemetry_spans` corroboration query where tokens/events are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-5 (G-022). Fixtures: reuse #2745 Fixture S1 (dispatch + other tools) and S2 (no dispatch).

- [ ] R-34: **`excludePayload` rules byte-identical on both contracts (REG-1 — the #2723 guard CONTINUES for child-session events).** `MissionMonitorFeature.tsx:66-68` (chat-node) and `:100-103` (tool-use-lifecycle) still declare exactly `[{path:'is_subagent',equals:true},{path:'agent.type',equals:'subagent'}]`; the unit test `MissionMonitorFeature.test.ts:55-75,120-125` stays green. Child-session chat/tool events STILL produce zero ChatNode/ToolsNode content — the ONLY new node is the task-tool-driven SubagentNode. Evidence: grep of both contracts + unit test output + Fixture S1 DOM snapshot (zero child-derived elements).

- [ ] R-35: **No new ECE contract / no `agent_session`-eventType (REG-2; #2688 phantom-buffer guard).** The feature still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`; no `agent_session` eventType contract added (agent_session spans always emit `EventState::Init` and can never satisfy `completeWhen: state === 'Response'` — the #2688 failure). Evidence: grep of `MissionMonitorFeature.tsx` + unit test `:26-29`.

- [ ] R-36: **ToolsNode behavior for NON-task tools unchanged (REG-3).** In Fixture S1, every non-`task` tool call (bash/read/grep) still appears as its own accordion item with the existing indicator/duration (F-40..F-50 pattern); per-call and per-exchange figures still zero-guarded; the ToolsNode title/count excludes the dispatch but includes the real tools; the `#2743` AC-1 removal (no per-tool tokens / no "Exchange tokens:" row) still holds. Evidence: ToolsNode screenshot + DOM scan + `telemetry_spans` per-call query.

- [ ] R-37: **Chat-chain stacking, ToolsNode slot, and force-collide layout undisturbed (REG-4; F-30 pattern).** With a SubagentNode present in its own column (x=parent.x+1128), no two nodes overlap (pairwise bounding-box = 0) across chat chain, ToolsNode column, and SubagentNode column; chat nodes keep measured-height + CHAIN_GAP stacking; pan/zoom/click-to-select (`selectNodesOnDrag={false}`, #440) all work; the SubagentNode is excluded from the force/residue passes and re-tracks on chain reflow. Evidence: full-graph screenshot + pairwise bbox check + interaction checks.

- [ ] R-38: **No re-render loop / console errors from the new builder path (REG-5; Spec #275/#523 pattern).** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` during: task dispatch arrival, SubagentNode creation, status transitions, session switch, and a session with multiple sequential dispatches. No array `.length` / fresh-object effect dependencies in the new SubagentNode builder/association path (signature-based changed detection). Evidence: console excerpt across all interactions.

- [ ] R-39: **Existing AC5 unit tests stay green for child-session-shaped deliveries (REG-6).** `useMissionMonitor.test.ts:588-712, :983-1431` (subagent-shaped delivery → zero SubagentNodes) remain green — they feed CHILD-session-shaped deliveries, which stay excluded; only the new task-tool-driven path may create SubagentNodes. The implementation's new unit tests cover the task path (ST-2 list). Evidence: `pnpm --filter @fredo/ui test:run` output.

- [ ] R-40: **No other feature/global surface touched (REG-7).** The spec's changes are confined to mission-monitor files (`useMissionMonitor.ts`, `lib/graph.ts`, `lib/layout.ts`, `components/nodes/SubagentNode.tsx`, `components/MissionMonitorPanel.tsx`, `MissionMonitorFeature.tsx` — contracts unchanged, `MonitorNode.module.css`, deleted ToolNode/FileNode); no theme system changes beyond the new `--accent-subagent` token if confirmed (U-1); no other feature imports mission-monitor internals; `FocusWindow.tsx`/`DetailPanel.tsx` tool/file branches either removed or explicitly scoped (U-8). Evidence: `git diff --stat` of the spec branch (or the plan's sub-task file list) vs main — no out-of-scope files.

---

## #2748 regression guards (session names + rename + subagent totals must not disturb the value pipeline)

### Tester run 1 (2026-08-17) — FAIL / incomplete live run

- R-41..R-49: **FAIL/UNVERIFIED**. Live telemetry confirmed the selected session used `otlp_grpc`, but Mission Monitor mount logged `no such table: feature_mission_monitor_session_names`. Name persistence/cap, delete cleanup, and rename durability were not proven. No subagent fixture was run; parent-family and SubagentNode guards remain unverified.

Baseline invariants for Spec #2748 (frontend-only: session names/rename + SUBAGENTS/TOTAL figures + header removal + neutral node styling). The spec ADDS name surfaces and a SUBAGENTS figure and REMOVES header/status chrome — the token value pipeline, contracts, persistence shapes, and interaction behavior must stay byte-identical. Live policy — every case needs a `telemetry_spans` corroboration query where token figures are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. Non-goals binding the guards: no backend/plugin/adapter/ECE/payload changes; the parent five-way semantics in `counters.ts` are frozen (R-3.3); `GRAPH_STATUS_COLORS`/`GRAPH_NODE_BORDER_COLORS` stay in code for the DetailPanel — only their node/minimap render application is removed.

- [ ] R-41: **ECE contracts byte-identical (R-34/R-35 guard continues).** `MissionMonitorFeature.tsx:41-105` still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`, with the subagent `excludePayload` rules byte-identical (`[{path:'is_subagent',equals:true},{path:'agent.type',equals:'subagent'}]`); NO new contract, NO `agent_session` eventType (the #2688 phantom-buffer guard). The #2748 changes are pure consumers of the existing deliveries. Evidence: grep of both contracts + unit tests (`MissionMonitorFeature.test.ts`) + a live fixture session confirming deliveries still arrive on the same two contracts.

- [ ] R-42: **Parent-family Σ identity preserved (F-29/F-38).** In every #2748 fixture session (Fixture P included), Σ per-node totals == the bar's parent five-way cells (In/Ca/Re/Ou) byte-exact, zero residual — the new TOTAL = parent + SUBAGENTS must NOT disturb the parent-family aggregation, and INPUT/CACHE/REASONING/OUTPUT must be byte-equal to the pre-#2748 bar values for the same session. SUBAGENTS tokens appear ONLY in the SUBAGENTS figure and the new TOTAL — never folded into any parent family. Evidence: per-node summation written out + bar readout + residual computation + `telemetry_spans` per-session query.

- [ ] R-43: **Per-message node values untouched (F-26/R-18).** Every chat node's figures still equal its own per-message span usage (node-vs-span authoritative, zero tolerance; the Style B real-value discriminator where a per-message-style provider is available). The name/rename/token-bar changes must not re-base or re-derive node values. Evidence: per-node aria-label readout vs `telemetry_spans` per-message query + comparison table.

- [ ] R-44: **SubagentNodes still render — only status chrome removed (#2745 survives).** In a session that dispatched a @-subagent (Fixture P), the SubagentNode(s) still appear: exactly one per user-requested `task` dispatch, in the subagent column (x = parent.x + 1128), connected to the parent chat node; the `task` item is still excluded from the ToolsNode; the `INTERNAL_TOOL_EXECUTION_AGENTS` (`build`/`plan`) gate still creates no node. AC5 removes only the node's badges/borders/glow — NOT the node itself. Evidence: DOM snapshot (`subagentNode` elements == `fredo.tool.task` span count) + full-view screenshot + `telemetry_spans` task-span query.

- [ ] R-45: **Persistence contract — no schema change, atomic UPDATE only.** Existing `sessions`/`events` tables unchanged (no new columns — backend `ensure_table` is `CREATE TABLE IF NOT EXISTS` only and backend changes are out of scope); custom/derived names live in the NEW `session_names` table (featureId `mission-monitor`, `session_id` PK, `custom_name`, `derived_name`); ALL writes via atomic `featureStoreUpdate` — NEVER delete+insert (AGENTS.md SQLite rule); `deleteSessionFromStore` and `enforceSessionCap` also clean `session_names` rows; display precedence `customName ?? derivedName ?? label` holds on every surface. Evidence: feature-store schema query + `session_names` row inspection + code grep (no delete+insert upsert pattern in `lib/persistence.ts`).

- [ ] R-46: **50-session / 500-delivery caps unchanged.** With >50 persisted sessions the cap still evicts oldest-first (the same cleanup now also purges the evicted sessions' `session_names` rows — no orphan rows left behind); the per-session delivery cap (500) still applies; cap enforcement does not disturb list ordering or the name fields of surviving sessions. Evidence: SQLite row counts before/after exceeding the cap + list readout.

- [ ] R-47: **OTLP gRPC-only transport unchanged (R-28 / #615).** Deliveries feeding the name surfaces and the SUBAGENTS figure still arrive via `otlp_grpc` only — zero Hook-transport rows in every #2748 fixture session's span set (`transports: ['otlp_grpc']`). Evidence: transport-column query per fixture session.

- [ ] R-48: **G-012 ordering unchanged.** Opening Mission Monitor before the session still yields deliveries (contracts registered at mount); opening AFTER a session still does not retroactively deliver missed events. The derived-name and SUBAGENTS surfaces depend on deliveries arriving only for registered contracts — no name figure may appear from pre-registration events. Evidence: delivery timestamps vs session start + a negative check (open-after-session shows the timestamp fallback, never a phantom derived name).

- [ ] R-49: **Delete + search-filter behavior unchanged (Q-4).** Session delete still removes the session (list row + deliveries + its `session_names` row) and selection moves per the existing rule; the sidebar search still filters by sessionId — or by name too IF the PO decided search-by-name in Q-4 (document which shipped); sort order (newest-first by `latestTimestamp`), auto-select, drawer collapse/hover behavior unchanged. Evidence: delete + search interaction screenshots + SQLite row counts + list readout before/after.

---

## #2750 regression guards (subagent-inclusive cost + name filter + single subagent node + status-free detail panel must not disturb prior surfaces)

Baseline invariants for Spec #2750 (frontend-only: bar ESTIMATED COST becomes parent + subagents; the double-click detail panel drops node-level status chrome; the sidebar filter matches names; exactly one node per subagent; the detail panel shows the node's cost). The spec ADDS aggregation/removal/filter behaviors — every existing figure, identity, interaction, and contract must stay byte-identical. Live policy — every case needs a `telemetry_spans` corroboration query where figures are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-6 (G-022). Fixtures: reuse #2750 Fixtures A–D.

- [ ] R-50: **Per-node cost figures untouched (REG-1; F-95 continuation).** The inclusive bar aggregation must NOT re-base any node figure: every ChatNode `Estimated Cost` row still equals its own span `cost_usd`; every SubagentNode `Estimated Cost` row still equals its own `childCost`; absent stays absent ('—'), zero stays `$0.0000`, never a hardcode (F-61/F-95 pattern). Evidence: per-node aria-label readouts vs `telemetry_spans` per-span query + comparison table.

- [ ] R-51: **SUBAGENTS figure + TOTAL token identity unchanged (REG-2; R-42/F-83 continuation).** The bar's SUBAGENTS tokens and TOTAL = parent + SUBAGENTS identity are untouched by the new cost arithmetic: parent families (In/Ca/Re/Ou) byte-equal to pre-#2750; SUBAGENTS byte-equal to the F-83 value; TOTAL identity unchanged. Cost lives ONLY in the ESTIMATED COST cell. Evidence: bar readout + per-node summation + residual computation + span query.

- [ ] R-52: **Session-rename behavior from #2748 unchanged (REG-3).** The name filter must not interfere with rename/edit interactions: hover-reveal edit, inline rename prefill/select, Enter-save, Esc-cancel, empty-clear→derived fallback, custom > derived > label precedence, and persistence across close/reopen + restart all still work with a filter query active (renaming a filtered-in row keeps it filtered; renaming a row so it no longer matches removes it from the filtered list; clearing the filter restores everything). Evidence: rename flow screenshots with an active filter + `session_names` row inspection + filtered-list readouts.

- [ ] R-53: **Per-tool outcome indicators survive AC2 (REG-4).** The node-level status-chrome removal must NOT remove per-tool outcome indicators: every tool call still shows its Failed/Succeeded indicator in the accordion and its scoped detail panel (F-59/F-99 pattern); mixed-outcome distinguishability intact; ToolsNode accordion expand/collapse + per-tool panels unaffected. Evidence: ToolsNode screenshot + scoped-panel screenshot + per-call span query.

- [ ] R-54: **ECE contracts byte-identical (REG-5; R-41 continuation).** `MissionMonitorFeature.tsx:41-105` still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`, with the subagent `excludePayload` rules byte-identical; NO new contract, NO `agent_session` eventType. The #2750 changes are pure consumers of existing deliveries. Evidence: grep of both contracts + unit tests + a live fixture session confirming deliveries still arrive on the same two contracts.

- [ ] R-55: **OTLP gRPC-only transport + G-012 ordering unchanged (REG-6; R-47/R-48 continuation).** Deliveries feeding the new cost/filter/node surfaces still arrive via `otlp_grpc` only (zero Hook-transport rows in every #2750 fixture session's span set); opening Mission Monitor before the session still yields deliveries; opening AFTER still does not retroactively deliver. Evidence: transport-column query per fixture session + delivery-timestamp check.

- [ ] R-56: **sessionId-only filtering retained (REG-7; R-49 + F-103 continuation).** The pre-existing sessionId match still works exactly as before (`useSessionHistory.ts:191`) — the name extension ADDS a match arm, never replaces or weakens the sessionId arm; a query that matches only a sessionId still returns exactly that session (no name-based false positives). Evidence: sessionId-fragment query readout vs pre-#2750 behavior + filtered-list DOM check.

---

## #2752 regression guards (Chain/Force toggle must not disturb the chain surface or the value pipeline)

Baseline invariants for Spec #2752 (frontend-only: floating Chain/Force segmented control; Force = LIVE ANIMATED d3-force; mode persists via `Fredo_mm_*`). Chain mode is BYTE-IDENTICAL to today — the spec's own AC4, so every chain-geometry and value-pipeline invariant below is a regression guard. Live policy — every case needs a `telemetry_spans` corroboration query where token figures are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-7 (G-022). Fixtures: reuse #2752 Fixtures L1/L2 (Run CLI, open MM first, `$env:OPENCODE_ENABLE_TELEMETRY="1"`).

- [ ] R-57: **Chain geometry constants + computation functions frozen (REG-1).** `lib/layout.ts` constants are byte-unchanged: `CHAIN_GAP=28`, `CHAIN_X_CENTER=0`, `CHAIN_TOP_Y=0`, `DEFAULT_NODE_HEIGHT=360`, `TOOLS_CHAIN_X=564` (`AGENT_NODE_MAX_WIDTH`+`TOOLS_GAP`), `SUBAGENT_CHAIN_X=−564`; `computeChatChainPositions` / `computeToolsChainPositions` / `computeSubagentChainPositions` / `resolveRectOverlaps` signatures and behavior untouched (their unit tests unchanged — `layout.test.ts` has zero diffs). Evidence: `git diff` of `lib/layout.ts` + `layout.test.ts` vs main + test-run output.

- [ ] R-58: **Existing sync `computeForceLayout` semantics frozen in chain mode (REG-2).** In chain mode the code path is byte-identical to today: the synchronous `computeForceLayout` (settled-node fx/fy freezing, level-based collide radii 270/270/240/210, per-depth forceY, maxIterations 300, alphaMin 0.01) runs and is then chain-overridden for agent/tools/subagent positions — the Force-mode live simulation must live in a SEPARATE branch and must not alter the chain-mode result. Evidence: chain-mode sampled positions byte-identical to `layout.test.ts`-covered expectations + test-run output + code review of the mode branch (grep: no `mode`/`layoutMode` branching inside the chain-mode path).

- [ ] R-59: **SessionTokenBar component contract unchanged (REG-3).** `components/SessionTokenBar.tsx` has zero diffs vs main — same props, same DOM (`data-testid="session-token-bar"`, full comma-formatted aria-labels), same five-figure + SUBAGENTS + ESTIMATED COST cells; `SessionTokenBar.test.tsx` stays green. The toggle must never render inside or reposition the bar. Evidence: `git diff` (zero changes) + bar DOM snapshot + test-run output.

- [ ] R-60: **ECE contracts + transports byte-identical (REG-4; R-41/R-54 continuation).** `MissionMonitorFeature.tsx` still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`, with the subagent `excludePayload` rules byte-identical; no `agent_session` eventType (the #2688 phantom-buffer guard). The toggle is a pure consumer — deliveries still arrive on the same two contracts. Evidence: grep of both contracts + unit tests + a live fixture session confirming deliveries on the same contracts.

- [ ] R-61: **G-012 ordering + OTLP gRPC-only transport unchanged (REG-5; R-47/R-48/R-55 continuation).** Opening Mission Monitor before the session still yields deliveries; opening AFTER still does not retroactively deliver; zero Hook-transport rows in every #2752 fixture session's span set. Evidence: transport-column query per fixture + delivery-timestamp check.

- [ ] R-62: **No re-render loop / console errors from the toggle + force simulation (REG-6; Spec #275/#523 pattern).** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` across: mode toggling, Force animation + settle, structural-change restarts, streaming in both modes, panel close mid-animation and reopen. No array `.length` / fresh-object effect dependencies in the new mode/simulation code. Evidence: console excerpt across all interactions.

- [ ] R-63: **Graph interactions intact in BOTH modes (REG-7; F-30/R-31 pattern).** In Chain mode (F-123) AND Force mode (after settle): pan/zoom, click-to-select (`selectNodesOnDrag={false}`, #440), and double-click DetailPanel all work; no two nodes overlap in Force mode after settle (pairwise bounding-box = 0); the floating toggle does not block clicks on any node beneath it (the control's hit area is its own bounds only). Evidence: full-graph screenshot + pairwise bbox check + interaction checks in both modes.

- [ ] R-64: **No new npm dependencies + no backend/telemetry changes (REG-8; T20).** `package.json` shows no new deps (d3-force already `^3.0.0`); `git diff --stat` vs main shows only mission-monitor frontend files; `telemetry_spans` schema/emission unchanged (no new instrumentation, no new attrs — the spec only adds a layout consumer). Evidence: `package.json` diff + `git diff --stat` + telemetry schema check vs baseline.

---

## #2754 regression guards (hybrid Force must not disturb the chain surface, value pipeline, toggle, or contracts)

Baseline invariants for Spec #2754 (redefines Force mode into a hybrid: chat nodes keep EXACTLY the chain geometry while ONLY subagent/tools companions run the live d3-force simulation; Chain mode byte-identical; toggle + `Fredo_mm_layout_mode` persistence preserved). The spec REDEFINES the #2752 Force-mode behavior — the all-nodes-animate semantics are intentionally replaced — but EVERYTHING ELSE (Chain geometry, value pipeline, contracts, toggle surface, a11y) must stay byte-identical. Live policy — every case needs a `telemetry_spans` corroboration query where figures are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-6 (G-022). Fixtures: reuse #2754 Fixtures H1/H2/L2/L3 (Run CLI, open MM first, `$env:OPENCODE_ENABLE_TELEMETRY="1"`).

- [ ] R-65: **Chain geometry constants + chain-computation functions frozen (REG-1; R-57 continuation).** `lib/layout.ts` chain constants remain byte-unchanged: `CHAIN_GAP=28`, `CHAIN_X_CENTER=0`, `CHAIN_TOP_Y=0`, `DEFAULT_NODE_HEIGHT=360`, `TOOLS_CHAIN_X=564`, `SUBAGENT_CHAIN_X=−564`; `computeChatChainPositions` / `computeToolsChainPositions` / `computeSubagentChainPositions` signatures and behavior untouched (chain unit tests unchanged or only ADDED). The hybrid must build ON these — never redefine them. Evidence: `git diff` of `lib/layout.ts` vs main (chain constants + functions) + test-run output.

- [ ] R-66: **Chain-mode code path byte-identical (REG-2; R-58 continuation).** In Chain mode the layout branch is byte-identical to today: `computeForceLayout` + chain overrides (agents) + tools/subagent chain-owned slots + residue de-overlap — no hybrid/force-mode branching inside the chain-mode path (`grep`: no `layoutMode`-branching in the chain-mode block). Chain-mode sampled positions byte-identical to `layout.test.ts`-covered expectations. Evidence: chain-mode sampled positions + code review (grep) + test-run output.

- [ ] R-67: **SessionTokenBar + ECE contracts + transport + G-012 unchanged (REG-3; R-59/R-60/R-61 continuation).** `SessionTokenBar.tsx` has ZERO diffs vs main (`data-testid="session-token-bar"`, five-figure + SUBAGENTS + ESTIMATED COST cells, `SessionTokenBar.test.tsx` green); `MissionMonitorFeature.tsx` still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`, subagent `excludePayload` byte-identical; no `agent_session` contract; opening MM before the session still yields deliveries, after does not retroactively deliver; zero Hook-transport rows in every fixture session. Evidence: `git diff` of both files + contract grep + unit tests + transport-column query + delivery-timing check.

- [ ] R-68: **No re-render loop / console errors from the hybrid (REG-4; R-62 continuation).** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` across: mode toggling, hybrid glide + settle, structural-change restarts (new chat node + new companion arriving), streaming in BOTH modes, rapid Chain↔Force bursts, panel close mid-animation and reopen. No array `.length` / fresh-object effect dependencies in the hybrid branch (Spec #275/#523 pattern). Evidence: console excerpt across all interactions.

- [ ] R-69: **Graph interactions intact in BOTH modes (REG-5; R-63 continuation).** In Chain mode AND hybrid Force mode (after settle): pan/zoom, click-to-select (`selectNodesOnDrag={false}`, #440), and double-click DetailPanel all work; no two nodes overlap in hybrid Force after settle (pairwise bbox = 0 — G-040 layout coords); the floating toggle does not block clicks on any node beneath it. Evidence: full-graph screenshot + pairwise bbox check + interaction checks in both modes.

- [ ] R-70: **NEW GUARD — chat nodes never move in Force mode (the #2752 all-nodes-animate behavior is intentionally redefined).** The #2752 Force behavior animated ALL nodes; the hybrid statically pins chat nodes to the chain. A future spec must NOT silently re-introduce chat-node movement in Force. In any hybrid-force session, chat node positions are byte-identical to the chain computation across t0..settled and across structural restarts. Evidence: per-node movement log (chat nodes max displacement = 0) + sampled position tables. **Also verify the #2752 force unit tests were DELIBERATELY updated** (Architect's declared diff list, plan [qa-1]) — `useMissionMonitor.test.ts` T16 (chain→Force yields force positions for agents) and layout.test.ts force-builder tests must either stay green or carry the redefinition rationale; no test edited to mask a regression. Evidence: `git diff` of the test files + test-run output + rationale cross-check.

---

## #2756 regression guards (disjoint Force must not disturb the chain surface, value pipeline, toggle, or contracts — and must REVERSE the rejected #2754 hybrid)

### Round 2 execution note (2026-08-22)

The live D1 fixture used `otlp_grpc` telemetry and rendered chat/tools/subagent node types. Force positions were sampled synchronously; late samples were not byte-identical and exceeded the requested viewport bounds, so Force settle/containment guards remain failed or unverified. Chain baseline and Force persistence state were observed; full close/reopen persistence was not completed.

Baseline invariants for Spec #2756 (REWORK of the human-rejected #2754 hybrid Force: ALL nodes — chat, tools, subagents — force-simulated; NO chain-pinning, NO `snapToSettled`, NO 600px link clamp; cluster = one exchange; chat→chat edges excluded from the sim edge set; `forceX`/`forceY` positioning forces replace `forceCenter`; node-type colors unchanged; Chain mode byte-identical; toggle + `Fredo_mm_layout_mode` persistence preserved; frontend-only). #2756 REVERSES the #2754 hybrid's central behavior (chat spine static in Force) — the guards below protect everything that must NOT change while that reversal lands. Live policy — every case needs a `telemetry_spans` corroboration query where figures/events are asserted (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REG-1..REG-7 (G-022). Fixtures: reuse #2756 D1/D2/D3 and #2754 H1 (for the reversal) — Run CLI, open MM first, `$env:OPENCODE_ENABLE_TELEMETRY="1"`.

- [ ] R-71: **Chain geometry + chain-mode code path byte-identical (REG-1; R-65/R-66 continuation).** `lib/layout.ts` chain constants byte-unchanged (`CHAIN_GAP=28`, `CHAIN_X_CENTER=0`, `CHAIN_TOP_Y=0`, `DEFAULT_NODE_HEIGHT=360`, `TOOLS_CHAIN_X=564`, `SUBAGENT_CHAIN_X=−564`); `computeChatChainPositions`/`computeToolsChainPositions`/`computeSubagentChainPositions` signatures and behavior untouched; the Chain-mode branch is byte-identical to today (no disjoint-force branching inside the chain path); Chain-mode sampled positions byte-identical to `layout.test.ts`-covered expectations. Evidence: `git diff` of `lib/layout.ts` + chain-mode sampled positions + test-run output.

- [ ] R-72: **SessionTokenBar + toggle + `Fredo_mm_layout_mode` persistence unchanged (REG-2; R-67 continuation).** `SessionTokenBar.tsx` ZERO diffs (`data-testid="session-token-bar"`, five-figure + SUBAGENTS + ESTIMATED COST cells, `SessionTokenBar.test.tsx` green); `mm-layout-toggle` DOM contract unchanged (`aria-pressed` tracking, roving tabindex, `.nowheel`, `data-testid`); `Fredo_mm_layout_mode` key, default `'chain'`, and serialize format unchanged; no collision with `Fredo_mm_detail_panel_width`. Evidence: `git diff` of both files + toggle DOM snapshot + settings-store write-then-read round-trip.

- [ ] R-73: **ECE contracts + transport + G-012 unchanged (REG-3; R-67 continuation).** `MissionMonitorFeature.tsx` still declares exactly `chat-node` (`eventTypes:['chat']`) + `tool-use-lifecycle` (`eventTypes:['tool_use']`), both `transports:['otlp_grpc']`, subagent `excludePayload` byte-identical (`[{path:'is_subagent',equals:true},{path:'agent.type',equals:'subagent'}]`); no `agent_session` contract; opening MM before the session still yields deliveries, after does not retroactively deliver; zero Hook-transport rows in every fixture session. Evidence: contract grep + unit tests (`MissionMonitorFeature.test.ts`) + transport-column query + delivery-timing check.

- [ ] R-74: **Node-type colors + DetailPanel/FocusWindow/session-bar surfaces unchanged (REG-4).** Force mode adds NO cluster coloring/swatches/legend; ChatNode/ToolsNode/SubagentNode per-type colors byte-identical between modes (same classes/tokens — AC6); DetailPanel five-way token rows, FocusWindow ↑input/↓output pair, session bar, ToolsNode accordion, SubagentNode surfaces all render unchanged in BOTH modes. Evidence: class/token comparison table + before/after screenshots + DOM snapshots.

- [ ] R-75: **Value pipeline untouched — the layout rework is display-only (REG-5; R-27/F-26/F-38 pattern).** The disjoint force rework must not re-base any figure: every ChatNode's per-message values still equal its own per-message span usage (node-vs-span authoritative, zero tolerance); session bar totals still equal Σ per-node totals (zero residual); per-node cache still the per-turn delta; SUBAGENTS/ESTIMATED COST identities unchanged. Evidence: per-node readout vs span query + bar-vs-Σ comparison on a #2756 D1 session + `telemetry_spans` query.

- [ ] R-76: **No re-render loop / rAF leak / console errors from the disjoint sim (REG-6; R-68 continuation).** `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught` across: mode toggling, glide + settle, structural-change restarts, streaming in BOTH modes, rapid Chain↔Force bursts, panel close mid-animation and reopen (no leaked rAF on reopen — positions settle). No array `.length` / fresh-object effect dependencies in the sim branch (Spec #275/#523 pattern). Evidence: console excerpt across all interactions + settle-window position log after reopen.

- [ ] R-77: **#2754 hybrid guards REVERSED — the "chat never moves in Force" guard (R-70) is DELIBERATELY replaced (REG-7).** The #2754 R-70 expectation (chat nodes byte-identical to the chain in Force, static across t0..settled) is OBSOLETE and must NOT be re-asserted — chat nodes ARE force-simulated bodies (AC1, human-rejected #2754). The NEW guard: in Force mode every chat node moves between t0 and settle and settles OFF its chain slot (F-147 evidence). The tester verifies the #2754 force unit tests were DELIBERATELY rewritten to the new behavior (F-153); any test still asserting chat-pinned Force positions, the 600px clamp, or `snapToSettled` wiring is a defect. Evidence: `git diff` of the rewritten test files + test-run output + the F-147 position evidence (chat nodes move).


