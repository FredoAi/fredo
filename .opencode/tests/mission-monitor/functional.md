# Mission Monitor functional tests

- [ ] FM-9 FIX-CHAT-TOOLS: Run a unique-marker Run CLI session that produces at least two tool calls and a final answer. Verify DOM and screenshot order USER → TOOLS → RESPONSE, with THINKING (if present) between USER and TOOLS; reconcile the rendered nodes to the marker-resolved telemetry_spans session tree.
- [ ] FM-10 FIX-CHAT-NOTOOLS: Run a unique-marker zero-tool session. Verify DOM and screenshot show USER immediately followed by RESPONSE with no TOOLS header, empty box, or orphan divider; reconcile to telemetry_spans.
- [ ] FM-11 FIX-L3: Run a unique-marker three-level delegation fixture with sibling subagents. Verify screenshot and DOM bounding boxes show every SubagentNode to the right of chats, non-overlapping, and sibling gaps ≥24px; reconcile node set to telemetry_spans.
- [ ] AC5 FIX-L4: Run a four-to-five-level fixture and verify rightward progression, no collisions, pan/zoom/select interaction, and telemetry_spans receipt.
- [ ] FM-12 (#2768 AC1) Collector attribution on completed trees: select F5 (`ses_fb1432de1ffePbfJDDXCpGwtT4`) / F4B (`ses_fb111fbbdffehKcKwhVbma0rGL`) → NO `unattributed` chip; every SubagentNode shows the embedded `── TOOLS (N) ──` (F5 1+1, F4B 1+1+1). Verified round 2 on the fixed collector (4a9361e), historically AND on two live drives (R2A 1+1, R2B seam 1).
- [ ] FM-13 (#2768 AC5) S4 seam UI: with `FREDO_SUPPRESS_PARENT_ROUTING=1` (legacy-shaped child spans), the SubagentNode still renders with TOOLS data and chip hidden (R2B live: node `subagent-ses_faf4b16cbffefWVYwzXt1GTqwO_1`, TOOLS (1), 2 nodes + 1 edge).
- [ ] FM-14 (#2768 AC4) Hydration of a pruned session: after `contract_events` pruning, a listed session renders its retained (FeatureStore) graph with zero errors and chip hidden (F5 after full contract-row prune — identical node-id set, 2 edges).
- [ ] FM-15 (#2770 AC1 / R-1, R-2, R-3) Nested distinction at a glance — fixture A (parent → L1 → L2 live): inspect nested (depth ≥ 2) card vs L1 card on BOTH distinction axes — accent color (R-1/R-2: title-bar icon, title text, INSTRUCTION label, instruction/output box borders → `var(--accent-nested-subagent)` + the 3px inset tier stripe) AND placement (R-3: nested card y = parent y + `NESTED_TIER_INDENT_Y` = 64px per nesting level below L1, in layout space; L1 flush with its parent chat node's y) — with the `[L{depth}]` chip ignored as the distinguishing signal. Repeat under both active themes turbo and classic (G-050: no light/dark toggle exists in-product). FAIL if distinction is color-only, the 64px-per-level offset is absent, or the L1 card's appearance drifts from pre-#2770. Edge: L3+ compact variant (fixture B) — distinction present via color + stripe at minimum (R-2); card collapsed vs expanded. Round-1 (FIXA): PASS live classic+turbo (nested y−parent y = 64 exactly, computed rgb(245,158,11) vs L1 rgb(99,102,241), stripe in boxShadow, border/handles neutral). Round 2 (FIXB2, spec/2770 @ 6aea81c): nested distinction re-verified live under TWO different parents — fixture-l3 cards (depth 2, aria `Subagent (nested) · fixture-l3 · level 2`) at (1128,64) and (2256,64), each = its fixture-l2 parent's y (0) + 64; icon/title/INSTRUCTION computed rgb(245,158,11); boxShadow `inset 3px 0 0 var(--accent-nested-subagent), 0 2px 8px var(--border-color)33`; L1 cards unchanged (`Subagent · fixture-l2 · level 1`, no stripe). STILL OPEN: the compact (depth ≥ 3) variant edge — card-depth counter counts delegation hops from the driven root (useMissionMonitor.ts:1195-1200), so the FS-1 minimal chain root→fixture-l2→fixture-l3 tops out at depth 2; the isCompact path (SubagentNode.tsx:137 `(depth ?? 0) >= 3`) needs a 4-session chain (add fixture-l4, widen fixture-l3's task map) — see FM-20. Round 3 (spec/2770 @ 4fbb061): TWO complete depth-3 drives (FIXB3 root `ses_fab7f30abffe6pAYuh9yEC2Uku`, FIXB3b root `ses_fab6cec64ffepXjp0c6u385Nz0`, both 7-session trees root→2×l2→2×l3→2×l4, free model, $0, store-gated all-OK, Start-Sleep receipts in all 4 leaves) rendered ZERO compact cards — see FM-20 for the blocker.
- [ ] FM-16 (#2770 AC2 / R-4, R-5) Token compliance: [U] static — `accentNestedSubagent` defined through the 3-file theming chain (`types/theme.ts` → `ThemeProvider.tsx` → `system.ts` `accent.nestedSubagent`) mapped to `--accent-nested-subagent` with the value for both active themes; grep changed frontend files for newly added `#`hex / `rgba(` literals (allowed: transparent/inherit/currentColor/none + data/terminal palettes). [L] live — switch turbo ↔ classic in Appearance while Mission Monitor shows fixture A: distinction re-renders correctly, computed styles resolve `var(--accent-nested-subagent)` (never a raw hex), no stale color without remount. Edge: each active theme's value leg individually; ST-1's divider tokenization shows only token-equivalent visual deltas.
- [ ] FM-17 (#2770 AC3 / R-6, R-7, R-8) Zero collisions — fixture B (deep tree: ≥ 3 sibling branches, L2/L3 nesting, consecutive chat nodes each with subagent cards taller than their parent chat node). Three live legs: (a) initial render; (b) streaming arrivals + measured-height reflow while the drive is in flight (R-8: subagent height changes bump the reflow epoch, `prev !== h` no-op kept); (c) full rebuild via session switch away/back and restore-from-persistence. MEASUREMENT GUARDRAILS: node positions read from `.react-flow__node` inline `style.transform` (`translate(xpx, ypx)` — the layout position, unscaled by zoom; each element carries `data-id`) per G-055; node sizes measured in LAYOUT space (`offsetWidth`/`offsetHeight`), never `getBoundingClientRect()` (zoom-scaled) per G-040; fit-view verification honors the minZoom-clamp and `zoomOnDoubleClick` records. Compute pairwise axis-aligned bounding boxes (parent↔nested, siblings, same-lane cards of neighboring chat nodes): zero overlap on either axis with ≥24px clearance (graph-coordinate px) in ALL three legs. Edge: streaming burst landing mid-reflow; panel resize during reflow; widest branch; unmeasured fresh cards reserving `SUBAGENT_CARD_FALLBACK_HEIGHT` (640) on initial render (R-7).
- [ ] FM-18 (#2770 AC3b / R-9) Determinism: completed fixture B — capture every node transform + layout size at rebuild 1 and rebuild 2 (session switch away/back), text-diff the position sets: identical (same inputs → same positions, no drift). Edge: rebuild after app restart; rebuild at different panel size then same size.
- [ ] FM-19 (#2770 AC4 / R-10) Flat parity — no-nesting session (historical reconciliation first): (a) [U] `layout.chain-parity.test.ts` goldens pass UNMODIFIED (absent-`companionExtent` degenerate case); (b) [L] live DOM geometry inspection confirms flat-session node positions/geometry/aria-labels unchanged. Verified via chain-parity + DOM geometry ONLY — never pixel-diff against any unmaterialized screenshot/baseline artifact (G-068). Edge: flat session with a single L1 subagent; flat chat-only session; flat session with a tall L1 card (legitimately gets extended pitch per the converged AC3-vs-AC4 contract — only card RENDERING + degenerate geometry are byte-identical).
- [ ] FM-21 (#2770 round 6 / R-4 real-path depth-3) L3 compact card on the REAL delivery path — ONE Run CLI drive, marker in FIRST prompt, chain root(build) → product-owner → self-improver → developer (leaf bash wait), Mission Monitor mounted BEFORE launch, EARLY root-attribution gate, completion store-gated. Expected: exactly one depth-3 SubagentNode per real nested session; compact anatomy (single summary line, no INSTRUCTION section); `[L3]` chip; aria `Subagent (nested) · <name> · level 3`; e-calls edge sourced from the L2 card; staircase y = L2 y + 64 (layout space); tools > 0 (leaf's own tool activity); node set 1:1 with telemetry sessions; 0 duplicate node/edge ids; 0 unattributed chip. Round 6 (2026-08-31, marker `fredo-2770-L3REAL-0831`, root `ses_fa8b7aecaffeKZxFowCV8vhvgR`): PASS — L3 card `Subagent · developer [L3] 13.4s 1 tools · 0 nested · 29k tok` at (1692,128) off L2 (1128,64), 4 nodes/3 edges, receipts in the round-6 Tests Runs. Sub-legs with named blockers: P-1 live-arrival camera reveal UNOBSERVED (drive session was not the selected MM view during arrival — restore-path framing + 2 R-9 unit cases cover it); P-2 live warm-up awaiting state UNOBSERVED (drive completed ~90s; 3 R-10 unit cases cover it); both are presentation edges, not the R-4 creation assertion.
---

## RTDB migration (#2788) — canonical-tier cases

> Post-migration the frontend consumes typed rows/patches (insert/update/remove + seq) instead of contract deliveries. Existing FM-9..FM-21 legs remain the parity baseline and are re-run on the row path (see FM-28 / R-2788-4).

- [ ] FM-22 (#2788 AC1) Late-arrival patch delivery: drive whose child-subagent completion (childSessionId, tokens) lands >300s after dispatch start (compression: ≥6-min leaf via chained `node -e "setTimeout(...)"` waits, or `fredo emit` synthetic late patch corroborated by a real late completion — see QA Plan). Mission Monitor mounted before launch. Expected: patch delivered regardless of elapsed time; SubagentNode shows correct attribution + child tool activity/tokens; `unattributed` counter does not grow; no timed-out-buffer discard. Edge: patch after app restart (replay surfaces it); patch inside a flush window; S-2770 root-attribution preflight if depth ≥3. **ROUND 1 (spec/2788 @ 02c013a, 2026-09-01): BLOCKED by named environment/product defect — MM mount wedges the webview (see F-33). The live-drive observation could not start; no fixture was driven. Unit half green (`lateCompletion.test.ts` 3/3).**
  **ROUND 3 (spec/2788 @ f3eac6f, 2026-09-01): PASS (live).** Real ≥6-min leaf drive (marker `fredo-2788-AC1R3-0901`, root `ses_fa0cda737ffeQMlQvpvHodBOkw`, child `ses_fa0cd74e1ffeF9nYHkT1Mc5myg`, free model muse-spark-1.2-contributor-free, MM mounted BEFORE launch, root llm-span preflight clean): task span 22:59:03→23:05:48Z = **405s (>300s legacy window)**; in-page observer recorded the UI timeline — chip `⚠1→2→3 unattributed` while the child ran, then at T+405s the completion patch DELIVERED AND APPLIED: SubagentNode `Subagent · general 6m 45s` + INSTRUCTION (405s = 6m45s exact), counter → 0, chip unmounted. Child rows composited under the parent with `parent_session_id` + `composited_child_session_id` stamps (R-1e). Replay edge: patch state survived the boot-4→boot-5 restart (same node ids re-rendered from replay). Child echo `R3-LATE-DONE` verified in rows.
- [ ] FM-23 (#2788 AC1-b) Correlation-state preservation across ingest: verify per-field merge rules (last-wins / keep-first / last-nonzero) against telemetry_spans at first-write → updates → completion. Expected: Init-time fields (userMessage, session metadata) survive all patches; zero/empty patch values never overwrite non-zero; #523 child re-key preserves parent linkage and creates the SubagentNode. **ROUND 1: PASS at the data layer (live).** Receipts: (a) LIVE gRPC session `ses_fa249f731ffe1PrBFxyPCvgOZg` (the tester's own agent session) derives per-turn chat rows `…_139/_141/_143/_145/_147` — one row per (session, turn), per-turn prompt deltas, seq monotonic; (b) F5 backfill rows show KeepFirst `userMessage` (init-time field survives later patches), LastNonZero cache-read delta EXACT (child2 turn2 crt=22784 = span 22912 − baseline 128, #2723), zero-delta turns clamp to null (child1 turn2 in=18488 < baseline 22895 → pt=null); (c) #523 re-key rows carry parent_session_id + composited_child_session_id stamps (child turns composited under the parent key). MOCK-VS-REAL cross-check done: real span extraction (F5, gen_ai.*) vs mock emit extraction (top-level typed fields) both verified.
- [ ] FM-24 (#2788 AC2) Replay across restart: complete a drive, fully quit + relaunch, subscribe with `replay: true`. Expected: full-row snapshots as `kind: insert` first, then live patches; derived graph (nodes/edges, token rollups, orphan chip) matches pre-restart; no hydration path involved. Edge: replay while live events arrive (no gap/duplicate); empty session (zero rows, no error). **ROUND 1: PARTIAL — restart mechanics exercised twice (dev-env Down/Up, app healthy, subscriptions re-registered); the replay RENDER observation is blocked by F-33 (MM mount wedge). Replay snapshot DELIVERY verified via webview-invoked `subscribe_events(replay:true, flushMs:0)` → insert received (retention-1 + ac4b-2 rows).**
  **ROUND 3 (spec/2788 @ f3eac6f): PASS.** (a) Replay/live boundary leg (webview-invoked subscribe, replay+flushMs:0): subscribe returned in **4ms** (async command — registration only), 300-row snapshot drained in 52ms as inserts, terminal EMPTY marker envelopes (one per query, correct queryId, never before subscribe return, never on live batches); live emits at T+7.4s/T+8.3s crossed the boundary — new-key insert + snapshot-key update (seq 2→3) both delivered; 304 deliveries / 303 distinct keys / 0 duplicates / seq strictly monotonic. (b) Cross-boot graph parity: the ac4b session rendered IDENTICALLY on boot 3 (live ingest), boot 4 (replay) and boot 5 (replay) — same node id `agent-ac4b_c1`, same USER/RESPONSE chain, same token bar 10/0/0/20/30, chip absent; the drive session's attributed graph (agent + SubagentNode 6m45s) also re-rendered identically on boot 5 from replay.
- [ ] FM-25 (#2788 AC2-b) Backfill correctness: re-derive canonical rows for a pre-cutover session (F5 `ses_fb1432de1ffePbfJDDXCpGwtT4`) from `telemetry_spans`; diff against fresh-extraction of an equivalent live drive. Expected: field-for-field match on typed columns, `seq` order, `started_at`/`ended_at`, merge outcomes; replay renders the same graph as pre-cutover receipts. Edge: malformed/partial spans skipped-or-defaulted, never a crash; large batch (write-behind, no ingest stall). **ROUND 1: PASS at the data layer.** Receipts: backfill log `spans_read=15450 chat_spans=6763 tool_spans=8287 session_spans=400 skipped_malformed=0 skipped_unrecognized=0` (fredo.db, 16:03:00Z); F5 rows — 6 chat rows with per-turn PKs (`_2/_4/_5` suffixes, dupes=1 each), every child-row (started_at_ns, ended_at_ns) EXACTLY matches its child-session fredo.llm span pair, token deltas exact (#2723 crt 22784=22912−128), task spans ↔ tool rows (task×2 sub=0, bash+glob sub=1), 3 session rows (agent unknown/general×2); NFR-6 one-extract-rule proven by backfill ≡ live derivation (tester session rows derive identically). Replay-render leg blocked by F-33. **ROUND 3: PASS (render half).** The backfilled F5-family session renders in MM from replayed backfilled rows: 2 nodes (agent + SubagentNode `Subagent · general 20.3s` with INSTRUCTION text), 1 edge, chip ABSENT (R-2768-1 zero-state on the row path) — verified via DOM captures identical across THREE replay cycles (initial + close/reopen ×2, boot 4); screenshot capture blocked by renderer saturation (named in verdict).
- [ ] FM-26 (#2788 AC3/AC3-b) Schema-validated queries: register a typo query (`chat { promtTokens }`) and a type-mismatch query → HARD mount error naming field + type, never silent-empty; a valid query returns the matching row set. Edge: unknown event-type root; unknown arg key; args at payload depth. **ROUND 1: PASS (live, webview-invoked `subscribe_events`).** All edges reject with the exact hard named errors: typo → `chat has no field 'promtTokens' — in: chat(promtTokens>0) { userMessage }`; type mismatch → `chat field 'promptTokens' is number; cannot compare with string "abc"`; string ordering → `comparison '>' requires a number field`; unknown root → `unknown root type 'chatx' (expected chat, toolUse, or agentSession)`; cross-type field → `toolUse has no field 'promptTokens'`; mixed valid+invalid batch → rejected with ZERO partial registration; invalid flushMs −1 → u32 arg rejection. Valid query (`chat(sessionId = "e2e-2788-ac4b-2") { … }`, replay) registered queryId `2ad6965d…` and returned its row set (never silently empty). **ROUND 3 spot-check (spec/2788 @ f3eac6f, live): PASS — no regression.** All hard errors reproduce verbatim on the new build (typo / type mismatch / unknown root / cross-type field / mixed batch zero-partial); valid query `chat(sessionId = "e2e-2788-r3n1") { … }` (replay) registered queryId `749540b5…` and delivered its 3 matching rows.
- [ ] FM-27 (#2788 AC4/AC4-b) Ingest cutover QA flows: (a) OTLP gRPC :4317 / HTTP :4318 receivers feed canonical rows — same session via both transports = ONE row per (session_id, correlation_id); (b) full `fredo-cli-events` skill flow set produces the same UI-observable outcomes as pre-cutover. MOCK-VS-REAL RULE: never assume `fredo emit` mock payload fields exist in real OTLP-derived rows — verify row/patch extraction paths against `telemetry_spans` via the telemetry-query skill (real paths with mock fallbacks, never the reverse). Edge: malformed span dropped, receiver survives; metrics/logs still dropped; emit before any subscriber (row stored). **ROUND 1: PASS (live).** (a) gRPC: listeners verified UP (ports 4317/4318 owned by fredo.exe); the live tester session's spans land as canonical rows continuously (92 distinct keys = 92 rows, zero duplicates); HTTP: OTLP JSON POST to :4318/v1/traces created `e2e-2788-http-1_1` (pt=42 ct=7, started/ended from span timing); cross-transport dedupe: the SAME span (session.id + spanId) replayed over HTTP mapped to the SAME correlation `_141` → still exactly 1 row (n=1). Malformed JSON POST → receiver survives (subsequent posts 200). Metrics POST → 0 rows carry the bogus marker. (b) `fredo emit` flow set (chat init→response, tool_use init→response, agent_session init→response) → canonical rows with correct extraction (user_message/agent_reply/promptTokens/completionTokens/cacheReadTokens/model; tool_success/input/output/duration; agentName/totalTokens/totalMessages/totalCostUsd) and seq 1→2 per key. Tooling caveat: PowerShell 5.1 strips embedded double quotes → use `--file <payload.json>` (skill update candidate). Emit-before-subscriber edge: rows stored unconditionally (MM was closed during all emits). **ROUND 3 (spec/2788 @ f3eac6f): PASS (UI half now observed).** Full flow set re-emitted on boot 3 with MM OPEN (`e2e-2788-r3-ac4b-1`, chat+tool_use+agent_session init→response via `--file`): canonical rows landed with correct extraction (seq 1→2 per key); the session rendered in MM as the v1-equivalent node `agent-ac4b_c1` — Chat with `── USER ──`/`── RESPONSE ──` chain carrying the exact emitted texts + Token Usage bar INPUT:10 CACHE:0 REASONING:0 OUTPUT:20 TOTAL:30 (promptTokens 10 + completionTokens 20), chip absent; identical render re-verified on boot 4 and boot 5 (replay). MOCK-SHAPE NOTE: the mock tool row carries no span timing (started_at_ns null), so the time-window tool→chat association does not attach it — per the mock-vs-real rule this is a mock-shape artifact, not a pipeline defect (real OTLP tool spans carry timing; the live drive's root bash DID render as TOOLS(1)).
- [ ] FM-28 (#2788 AC5) Mission Monitor parity on rows/patches: reworked vitest suites feed rows/patches (NOT removed) + ONE live drive per suite conventions (G-080/G-073/G-074 doctrine). Expected: nodes/edges, time-window tool→chat association, depth, emission gates, orphan chip observably at parity; FM-9..FM-21 legs pass on the migrated frontend. Edge: streaming burst mid-reflow (E-2770-1); session switch away/back; deep-tree stress (FM-20); depth-3 leg (FM-21) with root-attribution preflight; StepperProbe after. **ROUND 1: PARTIAL — vitest HALF GREEN: 30 files / 606 tests PASS (incl. row-reworked rowPatchPipeline 10, rowDerivation 15, lateCompletion 3, corpusParity 3, useMissionMonitor.realCorpus 3, useSessionHistory 38, useEventRows 21; chain-parity goldens 7 UNMODIFIED per R-2770-1; deep-tree 20). Live-drive half BLOCKED by F-33 (no fixture driven; MM cannot mount).**
  **ROUND 3 (spec/2788 @ f3eac6f): PASS (live drive).** ONE real Run CLI drive per doctrine (marker `fredo-2788-AC1R3-0901` in FIRST prompt, MM mounted BEFORE launch, free model muse-spark-1.2-contributor-free, one fixture per launch, completion store-gated: 2/2 `fredo.session` spans OK+ended): derived graph at parity — 2 nodes/1 edge (agent node `build · muse-spark-1.2-contributor-free` with the full marker prompt + `── TOOLS (1) ──` via time-window tool→chat association; SubagentNode `Subagent · general 6m 45s` with INSTRUCTION), depth-1, emission gates correct (TOOLS(1) counted once, not inflated by the child's 4 tool calls), orphan chip → 0 after attribution and unmounted (R-2788-7). Node set 1:1 with the telemetry session tree (root + 1 child). Carried: vitest half green (developer receipt on 8a521fc: 46 files / 723 tests, goldens unmodified).
- [ ] FM-29 (#2788 retention, decisions 2+3) Retention eviction `kind: remove`, never mid-session: retention via config file (seconds-scale dev TTL or backdated `updated_at`); age a row past TTL with an active subscription. Expected: exactly one `kind: remove` delivery with key + seq; a row updated within TTL NEVER disappears from an active subscription (mid-session remove = FAIL). Edge: replay after eviction → row absent; eviction with no subscriber; eviction-vs-update race (E-2788-5). **ROUND 1: PARTIAL (backend receipts green; delivery observation blocked by F-33).** Knobs verified (`rtdb.retention_days`=7, `rtdb.max_rows`=100000 in settings KV, seeded at startup); two marker rows backdated to 2026-08-20 via bun:sqlite (no seconds-scale dev TTL knob exists — integer days only; backdate compression works); subscription `616dbc8f…` held the retention-1 row (insert delivery received at subscribe). The 60-min writer prune EVICTED both rows (0 remaining; they were the only rows past cutoff) and the within-TTL corpus stayed fully intact (18086 chat rows, 0 stale rows in any of the three tables — "never mid-session" holds). Replay-after-eviction: row absent from SQLite (storage half). The `kind: remove` DELIVERY itself was unobservable (webview wedged by F-33 at the 17:51:40Z prune); the routing is unit-proven (`eviction_routes_remove_to_matching_subscriber_only`, `remove_is_emitted_only_for_retention_evictions`). **ROUND 3 (spec/2788 @ f3eac6f): PASS (delivery half observed live).** Marker row `e2e-2788-r3-retention-1|r1` backdated to 2026-08-20 via bun:sqlite on boot 4; in-page recorder held subscription `f338fed7…` from the page (replay insert received at 22:46:33Z); the boot-4 writer prune (~60 min after boot, due 23:18:47Z) evicted the row (SQLite COUNT 1→0) and the recorder received **exactly ONE `kind: remove` delivery (cid r1, seq 1, state null) at 23:19:00Z** — no other removes, within-TTL corpus intact, replay-after-eviction row absent.
- [ ] FM-30 (#2788 flush) `flush: 0` opt-in vs ~30ms default: subscribe one query with `flush: 0`, one at default; drive a burst; record delivery timestamps. Expected: `flush: 0` patches arrive individually; default coalesces; per-key `seq` strictly monotonic in both. Edge: burst ordering under `flush: 0`; `flush: 0` + replay; invalid flush value rejected at subscribe. **ROUND 1: PASS (live).** Two queries over the same session (`e2e-2788-flush-1`), 6 distinct-value parallel emits on key c1: flush:0 delivered updates seq 3,4,5,6,7,8 INDIVIDUALLY ~10ms apart (t+29152…+29201); default coalesced the same mutations into `update@5` and `update@8` (highest seq per window). Burst 1 (18 emits, 3 key families): flush:0 delivered insert+update separately (12ms apart); default coalesced to one `insert@2` per key. Content-identical mutations correctly skipped (content-no-op gate — no delivery churn). `flush:0 + replay:true` snapshot delivered immediately (1 insert). Invalid `flushMs: -1` rejected at subscribe (u32). seq strictly monotonic per key in both queries.
- [ ] FM-31 (#2788 AC6/AC6-b) v1-deletion audit: after Phase 5, verify OpenCodeAdapter, EventBus raw `.emit` FE path, contract DSL (streamFields/completeWhen/excludePayload), ContractEventStore + hydration, `eventFilters` shims, and FE defensive fallbacks are GONE (FredoEvent survives only as `fredo emit` CLI wire format); `cargo check` + `pnpm --filter @fredo/ui build` zero warnings; `telemetry-query.ps1` still answers a marker-resolved session identically pre/post-deletion (`telemetry_spans` untouched). Edge: dead imports/IPC registrations; leftover CLI flags; FE fallback removal leaving untyped paths (zero `??` chains). **ROUND 1: FAIL BY DESIGN (strangler order — P5.1 deliberately pending, gated on this round's live parity verdict). Pre-deletion baseline captured for the AC6-b compare: telemetry_spans schema (16 cols) + full fingerprint sha256 `19347d6f7cdc9f1191ae5e3ffe3073e6e23c9856179f1cfd61748339a1dc6560` (15592 rows) + sampled 18-row snapshot sha256 `eebccb6c…` (.opencode/tmp/2788/integrity-snapshot.json); WAL mode verified.** **ROUND 4 (spec/2788 @ 12be946): PASS.** DELETION HALF: independent grep sweep over `apps/tauri/src-tauri/src/` + `apps/ui/src/` for the full deleted-symbol set (ContractEngine/ContractEventWriter/ContractEventStore/EngineInput/SubscriptionDelivery/registerEventContracts/eventContracts/contractHydration/contract_events_hydrate/GenericOtlpAdapter/OpenCodeAdapter/emit_delivery/req_2_3_process/req_6_sweep/completeWhen/excludePayload/streamFields/deferredFields/useDeliveryFilter/useStepperEvents/useLatestStepperEvent/deliveryCompat/SideStepper/AlertHandler/useAlertEvents/eventFilters/eventSubscriptions/processEvent) — zero production references; sole hits are historical doc comments and the documented test-fixture exception (`ContractDelivery` survives ONLY as the deprecated test-fixture input type + zero production imports). Deleted files verified absent on disk: `comm/contract/` (whole module), `adapters/opencode.rs`, `adapters/otlp.rs` (pure helpers relocated VERBATIM to `rtdb/attrs.rs` — NFR-6), `bus_tests.rs`, `contractHydration.ts`, `useDeliveryFilter.ts`, `SideStepper.tsx`, `AlertHandler.tsx`, `useAlertEvents.ts`, `EventTimeline.tsx`, `deliveryCompat.ts`. EventBus reduced to `emit_row_delivery_batch` (bus.rs 49 lines); no contract IPC commands in `generate_handler!`; CLI flags cleaned (emit.rs: `#[value(rename_all = "snake_case")]`, `default_value = "init"`, `--file` doc fixed, 2 new parse tests). Zero-warning builds per the dev's round-4 receipts (`cargo check` clean, `cargo check --tests` clean, `cargo test rtdb` 173/173, `pnpm --filter @fredo/ui build` clean, vitest 44 files/669 green, RTDB goldens unmodified) — the running dev binary IS the build (process-hygiene receipt). INTEGRITY HALF: `telemetry_spans` schema 16 cols in the exact banked order; `PRAGMA journal_mode` = wal; sampled 18-row re-hash EXACTLY matches the banked baseline sha256 `eebccb6c…f70da` (missing=0, field_drift=0 — bun re-hash script in `.opencode/tmp/2788/rehash-round4.ts`); row count 16,401→17,104 (round 3)→17,845 (dev receipt)→18,013 — appends only; telemetry-query skill answers the round-3 marker session `fredo-2788-AC1R3-0901` IDENTICALLY (root/child ids + task span 22:59:03→23:05:48Z = 405s match the round-3 receipt).
- [ ] FM-32 (#2788 AC7) Docs completeness: AGENTS.md, docs/ARCHITECTURE.md, references.md, `fredo-cli-events/SKILL.md`, `.opencode/tests/README.md` all updated in the same spec; no stale ECE/hydration/completeWhen references; documented query/hook examples match the shipped API. **ROUND 1: FAIL BY DESIGN (P6.1 deliberately pending).** **ROUND 4 (spec/2788 @ 12be946): FAIL — narrow, doc-only residuals (2 findings).** GREEN half: all five targets re-baselined to the RTDB row pipeline (AGENTS.md: zero stale matches on the full v1-symbol grep; SKILL.md "How It Works" rewritten to the classifier→rows path with the `--file` payload-only caveat + post-fix snake_case `--event-type`/`--state` values; tests/README.md clean; references.md carries v1 mentions ONLY inside SI-owned `### G-` guardrail records per the Fix Plan's no-edit rule; the five rewritten ARCHITECTURE.md sections are clean). The SKILL.md flow set was LIVE-verified end-to-end on the deletion build (chat/tool_use/agent_session init→response via `--file`, snake_case `--event-type` parsing, omitted `--state` defaults cleanly — nits b/c fixed). FINDING 1 (stale refs): `docs/ARCHITECTURE.md` retains 4 present-tense references to deleted components — :256 "when the ECE re-delivers events", :572 "The OTLP adapter propagates the parentSessionId relationship", :573 "task delivery payload + ECE relationship metadata — canonical keys projected by the OTLP adapter", :575 "RFC3339 UTC injected by the adapter" — all misattribute mechanisms now owned by the ingest classifier/`rtdb/attrs.rs`. FINDING 2 (doc-vs-API): SKILL.md Recipe 3 documents a snake_case agent_session payload (`{"total_tokens":1234,"total_messages":3,"total_cost_usd":0.01}`) but the classifier's pinned extraction (and live behavior) reads camelCase — emitted per the doc: typed totals NULL (payload only in `raw_json`); emitted `{"totalTokens":4321,"totalMessages":7,"agent":"opencode"}`: typed columns 4321/7/opencode. Repo-wide edge grep: FAQ.md/CLI_GUIDE.md/SECURITY.md/telemetry-reference.md (NOT in the five targets) still describe the deleted v1 pipeline in present tense + stale `.opencode/tests/event-persistence|otlp-genai` suites reference deleted modules — out of AC7 scope, flagged for the contract-trust cleanup direction.
  **ROUND 5 (spec/2788 @ f8add9a): PASS — AC7 closed.** Static half: the Fix Plan's exact stale-clause patterns (`ECE re-delivers`, `OTLP adapter propagates`, `projected by the OTLP adapter`, `injected by the adapter`, `all drains recover`) grep to ZERO hits across the repo's *.md; all five targets clean (AGENTS.md zero v1-symbol/ECE/adapter hits; ARCHITECTURE.md zero; SKILL.md zero snake_case payload hits; tests/README.md zero; references.md v1 mentions only inside SI-owned `### G-` historical records at :530). The five corrected ARCHITECTURE.md clauses verified in place (:114 softened E-2788-r3-1 note; :135 contract_events disposition sentence; :256 RTDB seq+merge idempotency; :572 relationship-registry attribution; :575 attrs-extractor + nsToIso attribution) and anchors spot-verified against shipped code: `ingest.rs:134-150` IS the relationship registry (child_to_parent/parent_to_children), `attrs.rs:533-564` IS the child-completion flat-attr projection, `ingest.rs:1458-1478` reads camelCase only. LIVE half (Recipe 3 receipt): emitted `agent_session` init→response via `--file` with the corrected camelCase payload (session `e2e-r5-lifecycle-1`) → `agent_session_rows` ONE row, seq 2 state=response, typed columns `total_tokens=1234` / `total_messages=3` / `total_cost_usd=0.01` / `agent_name="opencode"` all NON-NULL — the SKILL.md verify-line column names match the observed DB columns exactly.
- [ ] FM-33 (promoted from round-1 exploratory, F-33 / E-2788-7) MM mount with a large canonical corpus must NOT wedge the webview: opening Mission Monitor on the RTDB replay path floods the webview — MM's panel (`useEventRows('Chat', {}, {replay:true})` + `useEventRows('ToolUse', {}, {replay:true})`) and useSessionHistory's second full-chat subscription replay the ENTIRE canonical tables (~40k rows) as one RowDelivery per IPC event, and `applyRowDelivery` bumps the epoch + notifies listeners PER DELIVERY (StreamContext.tsx applyRowDelivery/bumpRowEpoch) → ~40k React renders with a growing graph derivation → the webview main thread wedges for 25+ minutes with NO recovery (observed 3/3: two fresh boots wedged within seconds of the MM click; eval/DOM/screenshot all time out; backend process healthy throughout). Expected: mount stays responsive — replay batched (client-side chunking/rIC) or epoch bumps coalesced per flush window. FAIL until fixed; blocks ALL MM-observation legs (FM-22, FM-24 render half, FM-25 render half, FM-28 live half, S-2766/S-2788-3 console reads). **ROUND 2 fix attempt (72c2150): NOT sufficient — human stopped the run: main-thread block (sync subscribe_events) froze the app ("Not Responding"). ROUND 3 (spec/2788 @ f3eac6f, fix 8a521fc: async subscribe_events + spawn_blocking drain + replayCompleteQueryId marker + ready-on-marker): PASS on the mount criterion.** Four MM mounts observed on the persisted 41.7k-row corpus: boots 2/3/4 — eval probes responded INSTANTLY at T+3..40s (0 nodes while the gate stayed parked = correct ready-on-marker semantics), bounded drain (renderer spin ~45-60s, 27-44 CPU-s, then idle), full render (sidebar 360+ rows, graph, chip); boot 5 — mount wedged the renderer ~7 min (23:29→23:36Z) then fully recovered (drain bounded; fredo.exe main thread Responding:True on ALL boots — the "Not Responding" signature is GONE; backend healthy throughout). Residual (new E-2788-r3-1, exploratory): with MM open on the full corpus, continuous live agent traffic re-triggers per-batch derivations — the renderer intermittently saturates (screenshots/DOM/console reads time out during bursts; quick evals slip through); a real user's lighter traffic is unlikely to hit it as hard, but it bounds MM's interactive smoothness on ~42k-row corpora under sustained agent activity.
  **ROUND 4 (spec/2788 @ 12be946, deletion build): PASS on the mount criterion — residual E-2788-r3-1 SEVERE under active agent traffic.** 2 fresh boots on the 45.4k-row corpus. Boot 1: MM mount drain bounded (~108s renderer spin, recovered without restart; main thread healthy throughout — process-hygiene + backend queries ran DURING the spin; single fredo.exe owned :9223/:4317/:4318), full render (sidebar 366 rows). Boot 2: mount drain ~185s window, sidebar 369 rows. HOWEVER, under the round-4 tester's own continuously-streaming opencode session, every mutation burst with MM open re-triggered multi-minute renderer saturations: session-selection spins of ~5 min (boot 1), ~5 min and ~11 min (boot 2), and ONE non-recovering ≥13-min wedge on boot 1 requiring the full G-046 Down→Up recovery (no WS_SERVER bridge errors; bridge responded but the JS thread never yielded). Webview eval/DOM/screenshot time out during spins; all recoveries complete (no data loss, console clean). The mount criterion (main thread never blocked, drains bounded, full render) HOLDS on the deletion build; the interactive-responsiveness residual under live traffic is materially worse than round 3's observations (45-60s typical) and is documented against the ARCHITECTURE.md known-limitation note (E-2788-r3-1 ROUND 4 receipt).
  **ROUND 5 (spec/2788 @ f8add9a, 47.8k-row corpus): PASS on the mount criterion — streaming aggravator persists.** 2 fresh boots this round. Boot 1 (dispatch-era instance): found WEBVIEW-WEDGED at round start (all JS evals timing out at the 2s bridge limit, Rust side healthy — window list instant), never recovered over ~2 min → G-046 Down→Up; MM was not open at the time (idle instance, no active streaming — a wedge WITHOUT the streaming aggravator, new observation). Boot 2 (fresh, quiet discipline): MM opened via toolbar; mount drain COMPLETED bounded (~10 min under near-zero traffic, no restart), full render — sidebar restored the whole persisted corpus (366+ rows incl. round-3 marker `fredo-2788-AC1R3-09…` and the round-5 emitted sessions), graph rendered the replayed `e2e-r5-fm34` session 1:1 (auto-follow selection, agentNode `rf__node-agent-e2e-r5-fm34_1`, USER/RESPONSE/token-bar 10/0/0/20/30, session bar TOTAL 30 / 1 msgs). Post-render interactive use under the tester's own continuously-streaming opencode session re-saturated the webview for multi-minute stretches (heavy evals/DOM/screenshots time out; `1+1` slipped through between bursts; backend healthy throughout — process-hygiene single fredo.exe on :9223/:4317/:4318; 1,682 spans/5min from the tester's own traffic during bursts). Closing MM unblocked the renderer instantly. FM-33's mount criterion HOLDS on the round-5 build; the E-2788-r3-1 interactive residual under live traffic persists (see exploratory.md).
- [ ] FM-34 (NEW round 4, #2788 P5.1 regression) Dev Mode's row-mutation stream viewer must render post-mount row mutations: `useDevModeStream` (apps/ui/src/features/dev-mode/hooks/useDevModeStream.ts:91-110) accumulates fresh entries in an effect keyed `[mutations]` where `mutations = getRowMutations()` returns the SAME module-scoped array reference on every render (`return rowMutationLog;` — StreamContext.tsx:179-181) → the deps never change identity → the effect runs only at mount → entries arriving after the panel opens NEVER accumulate. Expected: the viewer's count/event list tracks every applied store mutation (the log itself is sound — `recordRowMutation` runs in the same apply path that bumps the partition epoch). **ROUND 4: FAIL (live-proven, deletion build @ 12be946).** Stepper insert present at mount rendered ("1 event: #1 20:45:56 ToolUse:insert (ToolUse) Init"); SIX+ subsequent mutations (chat-2 insert, probe_live_test insert, stepper update, led-1 agent_session, led-2 chat init+response) never appeared while the SAME deliveries provably applied to the row store (Stepper Probe epoch 1→2 on the stepper update; MM rendered the led-2 response patch: RESPONSE section filled + token bar INPUT:10/OUTPUT:20/TOTAL:30). Fix direction: depend on the log VERSION primitive (the `useSyncExternalStore` value) instead of the stable array reference — mirror StreamStatus's pattern (StreamStatus.tsx:100-112, which is sound).
  **ROUND 5 (spec/2788 @ f8add9a): FIX VERIFIED LIVE in the below-cap regime — new cap-edge finding E-2788-r5-1 (exploratory, non-blocking).** The fix (version-driven recompute: `useSyncExternalStore` value bound at :83, `getRowMutations()` read fresh inside the effect at :97, deps `[version]` at :116) verified three ways: (1) pin test `useDevModeStream.test.tsx` 4/4 green on the served checkout (the second-delivery assertion is the fail-before/pass-after pin; dev receipt: 3 failed/1 passed with the fix stashed); (2) LIVE boot 1: viewer opened at 0 events ("Waiting for row mutations…"), counted up to **512 events** while the MM replay drain applied batches post-mount (screenshot receipt — the pre-fix code froze at 0); (3) LIVE boot 2: viewer at mount consumed the bounded log's current contents (512 events displayed with drain-tail rows #407-#512). **E-2788-r5-1 (NEW, round 5): the retained seenCount accumulator misses mutations once the log sits at its 512-entry cap** — `ROW_MUTATION_LOG_CAP = 512` (StreamContext.tsx:153) eviction `splice(0, length-512)` (:172-173) pins `length` at 512 while shifting contents, so the hook's `start = Math.min(seenCount, mutations.length)` (= 512 = length) loop never re-examines the array and the newest entry (shifted into index 511) is never consumed. LIVE-proven on boot 2: two stepper emits applied to the row store (Stepper Probe replayed rows 2→3, row-store epoch 42→43) while the viewer stayed pinned at exactly "512 events" (newest rendered row #512 @ 23:39:56 — a drain mutation predating both emits). Same visible symptom class as FM-34 but only AT the cap; the Fix Plan explicitly retained the accumulator logic as byte-identical, and the round-4 failing scenario (mount snapshot freeze, log below cap) is fixed. Debug-viewer-only: store semantics (epoch, rows, MM derivation, replay) unaffected. Fix direction for a follow-up: on version change, when `mutations.length === CAP && seenCount >= CAP`, re-scan backwards from the end by id (the id set already de-dupes) instead of relying on the seenCount offset.
- [ ] FM-20 (#2770 AC5 / R-11, R-12) Deep-tree stress + a11y — fixture B: ≥ 15 nodes, ≥ 3 sibling branches, ≥ 2 L3 compact cards. Re-run the FM-17 overlap check under full stress; read `aria-label` of nested vs L1 cards from the live DOM (nested = `Subagent (nested) · <name> · level N` per R-11; L1 = `Subagent · <name>[ · level N]` unchanged). FAIL if labels drift across re-renders or collisions appear at stress scale. Edge: 3+ levels deep mid-streaming; stress tree after full rebuild; accessible-name probe for both card types. Code-level support: `layout.deep-tree.test.ts` (fixture shape ≥15/≥3/≥2, three zero-overlap legs, determinism, width pin, tier pin). Round 1: R-11 + stress-overlap PASS on FIXB (24 nodes, aria 16× level-2 vs 6× level-1 stable across 3 re-renders); L3-compact leg UNVERIFIED (0 depth-3 cards). Round 2 (FIXB2, 5-session tree root→2×fixture-l2→2×fixture-l3, free model, store-gated all-OK): the L3-SESSION cards rendered live under different parents — nested aria `Subagent (nested) · fixture-l3 · level 2` (matches the R-11 unit pin: aria level = payload.depth = delegation hops; the Fix Plan's "level 3" literal was a chain-depth miscount, not a code deviation); pairwise AABB over the 5-node tree: 10 pairs, 0 overlaps, min clearance 24px, identical after a full session-switch rebuild; telemetry receipt: all 5 `fredo.session` spans OK+ended, task-span parent-child attrs, Start-Sleep bash spans in both leaves, every llm span `muse-spark-1.2-contributor-free`, $0 cost. STILL OPEN: ≥2 cards at CARD depth ≥ 3 rendering the isCompact summary variant — needs the fixture-l4 config addition (see FM-15). Round 3: the fixture-l4 config landed (spec/2770 @ 4fbb061, ba0ee02) and TWO depth-3 drives ran (FIXB3 + FIXB3b after a G-046 restart) — both completed cleanly at the telemetry store (7/7 `fredo.session` OK+ended, task-span chain root→2×fixture-l2→2×fixture-l3→2×fixture-l4 verified, 14/14 llm spans `muse-spark-1.2-contributor-free`, Σ cost $0, bash `Start-Sleep -Seconds 3` 3251/3206ms and 3230/3237ms in the l4 leaves) but ZERO compact cards rendered: a plugin-side span-attribution defect stamps `session.parent_id` (a grandchild fixture-l3 session id) onto the PRIMARY root's llm spans during concurrent 3-level delegation (FIXB3: 2/4 llm spans, FIXB3b: 2/2; round-2 depth-2 root: 0/3) → the adapter's `is_subagent_session` rule 2/3 (otlp.rs:969-983) correctly flags the root's chat events as subagent (`is_subagent: true`, injected at otlp.rs:596-610) → chat-node/tool-use-lifecycle `excludePayload` drop them pre-buffer (unit-pinned: otlp.rs:3892) → no AgentNode/chat nodes → no SubagentNodes → no compact cards, and the session never even appears in the sidebar (session rows require a non-child delivery — persistence.ts:396-443). Evidence the rest of the pipeline works: 82-530 composited `subagent-tool-activity` deliveries persisted per drive (ECE + delivery + FeatureStore all live), console clean. UNVERIFIED blocker is plugin-side (apps/opencode-plugin message handlers), NOT Mission Monitor code — product diff between the working round-2 commit and this round is tests/docs/config only (git log 6aea81c..4fbb061).
