# Mission Monitor functional tests

- [ ] FM-9 FIX-CHAT-TOOLS: Run a unique-marker Run CLI session that produces at least two tool calls and a final answer. Verify DOM and screenshot order USER → TOOLS → RESPONSE, with THINKING (if present) between USER and TOOLS; reconcile the rendered nodes to the marker-resolved telemetry_spans session tree.
- [ ] FM-10 FIX-CHAT-NOTOOLS: Run a unique-marker zero-tool session. Verify DOM and screenshot show USER immediately followed by RESPONSE with no TOOLS header, empty box, or orphan divider; reconcile to telemetry_spans.
- [ ] FM-11 FIX-L3: Run a unique-marker three-level delegation fixture with sibling subagents. Verify screenshot and DOM bounding boxes show every SubagentNode to the right of chats, non-overlapping, and sibling gaps ≥24px; reconcile node set to telemetry_spans.
- [ ] AC5 FIX-L4: Run a four-to-five-level fixture and verify rightward progression, no collisions, pan/zoom/select interaction, and telemetry_spans receipt.
- [ ] FM-12 (#2768 AC1) Collector attribution on completed trees: select F5 (`ses_fb1432de1ffePbfJDDXCpGwtT4`) / F4B (`ses_fb111fbbdffehKcKwhVbma0rGL`) → NO `unattributed` chip; every SubagentNode shows the embedded `── TOOLS (N) ──` (F5 1+1, F4B 1+1+1). Verified round 2 on the fixed collector (4a9361e), historically AND on two live drives (R2A 1+1, R2B seam 1).
- [ ] FM-13 (#2768 AC5) S4 seam UI: with `FREDO_SUPPRESS_PARENT_ROUTING=1` (legacy-shaped child spans), the SubagentNode still renders with TOOLS data and chip hidden (R2B live: node `subagent-ses_faf4b16cbffefWVYwzXt1GTqwO_1`, TOOLS (1), 2 nodes + 1 edge).
- [ ] FM-14 (#2768 AC4) Hydration of a pruned session: after `contract_events` pruning, a listed session renders its retained (FeatureStore) graph with zero errors and chip hidden (F5 after full contract-row prune — identical node-id set, 2 edges).
- [x] FM-15 (#2770 AC1 / R-1, R-2, R-3) Nested distinction at a glance — **PASS (round 1, live)**. Fixture A marker `fredo-2770-FIXA-20260830` (`ses_fabe4ecffffeaWF2HQftbvwijI`, root → self-improver mid `ses_fabd6d81bffe…` → developer leaf `ses_fabd6bbd9ffe…`, all spans OK in telemetry_spans). Live DOM (classic AND turbo): nested (depth 2) card icon/title/INSTRUCTION label = `var(--accent-nested-subagent)` (computed rgb(245,158,11)), instruction+output box borders `1px solid var(--accent-nested-subagent)28`, container boxShadow `inset 3px 0 0 var(--accent-nested-subagent), 0 2px 8px var(--border-color)33`; L1 card unchanged (`var(--accent-subagent)`, no stripe, aria `Subagent · self-improver · level 1`); nested placement measured `y = parent y + 64px` exactly (parent (564,1885) → nested (1128,1949)); L1 flush with parent chat y. Chain-pitch extension live: chat 2 y = 498 = card 470 + CHAIN_GAP 28 (pre-#2770 would be 383). Screenshots: ac1-nested-card-closeup-classic, ac1-nested-distinction-classic (both themes in AC2 row). FAIL conditions did not occur. Drive recipe: pipeline-agent nesting (root build → task self-improver → task developer) — see smoke.md S-2770 note.
- [x] FM-16 (#2770 AC2 / R-4, R-5) Token compliance — **PASS (round 1, static + live)**. [U] 3-file chain verified in diff: theme.ts `accentNestedSubagent` typed + `#f59e0b` for turbo (:109) and classic (:148); ThemeProvider `setProperty('--accent-nested-subagent', …)` in the one-pass base-theme effect; system.ts `'accent.nestedSubagent': { value: 'var(--accent-nested-subagent)' }`. Zero NEW hex/rgba in changed feature files (git diff added-lines: only removals → `var(--border-color)`/`var(--text-secondary)`; ST-1 tokenized DetailPanel 5×, SessionHistoryDrawer 4×, FocusWindow 2× dividers + text). `pnpm --filter @fredo/ui build` clean. [L] Appearance Base Theme select classic→turbo→classic while MM showed fixture A: `--accent-nested-subagent` recomputed `#f59e0b` in both, nested icon computed rgb(245,158,11) vs L1 rgb(99,102,241) in BOTH themes, no stale color without remount. Screenshot ac2-theme-switch-turbo.
- [x] FM-17 (#2770 AC3 / R-6, R-7, R-8) Zero collisions — **PASS (round 1, live, with a shape deviation recorded under FM-20)**. Fixture B marker `fredo-2770-FIXB-20260830` (`ses_fabcf1e07ffeDpHMeSWXayFT8R`; root + 5 self-improver mids + 13 developer leaves, ALL spans OK in telemetry_spans). Leg (a) initial render: fallback-height reservation observed (cards at 640 reserved space before measure). Leg (b) streaming+reflow: MID-FLIGHT sample recorded (4 cards at interim force-ish positions, heights 406/227/227/227, chat node pending) — transient, not failed per E-2770-1; screenshot ac3-leg-b-streaming-midflight. Settled tree: 24 nodes (7 agent + 17 subagent), positions from `.react-flow__node` style.transform (G-055), sizes offsetWidth/offsetHeight (G-040): **276 pairs, 0 overlaps, all ≥24px clearance**. Extent-fed pitch live: chat-2 y = 434 = card 406 + 28. Leg (c) rebuild: session switch away/back ×2 → zero overlap held. R-8: no render-loop console errors (only pre-era framer-motion deprecation WARN).
- [x] FM-18 (#2770 AC3b / R-9) Determinism — **PASS (round 1, live)**. FIXB settled: THREE full rebuilds (session switch away→FIXA→back, ×3) each yielded a byte-identical position set (24/24 entries, text-diff diffCount 0 across rebuilds 1→2 and 1→3; id-sorted `data-id@transform` comparison per G-055). No drift. (Restart-rebuild edge not exercised this round.)
- [x] FM-19 (#2770 AC4 / R-10) Flat parity — **PASS (round 1, static + live historical)**. (a) [U] `layout.chain-parity.test.ts` 7/7 PASS UNMODIFIED (git: file untouched by spec/2770 — goldens unedited); layout.test.ts 40/40; useMissionMonitor.test.ts 101/101; full suite 641/641 across 37 files. (b) [L] Historical reconciliation: #2762-era deep-tree sessions exist in telemetry_spans (ses_fb8c56e47 → fixture-l2 → fixture-l3, completed) but are NOT UI-reachable (no FeatureStore rows) — used #2768 R2A `ses_faf58aae4ffeA5TEEA31QbFMni` (parent + 2 L1 subagents, no nesting) instead: live DOM geometry = legacy grammar exactly (chat (0,0,h429); L1 cards (564,0,h379) and (1128,0,h379) — flush y, lane step 564, 24px horizontal clearance), L1 rendering byte-parity (`var(--accent-subagent)` icon, no nested var, no stripe, border `1.5px solid var(--border-color)`, aria `Subagent · general`), zero unattributed chip. Chat-only flat session ("hi" `ses_faf92c470…`): 1 node at chain origin (0,0), zero cards. Chain-parity + DOM geometry ONLY (G-068 — no pixel-diff). Screenshot ac4-flat-parity-r2a.
- [ ] FM-20 (#2770 AC5 / R-11, R-12) Deep-tree stress + a11y — **PARTIAL (round 1): R-11 + stress-overlap PASS live; L3-compact stress leg UNVERIFIED (named blocker)**. PASS legs: FIXB (24 nodes ≥ 15; 3+ sibling branches; every session completed in telemetry_spans — root `ses_fabcf1e07ffeDpHMeSWXayFT8R` + 5 mids + 13 leaves all OK) re-ran the AC3 overlap check settled: 276 pairs, 0 overlaps ≥24px, stable across 3 rebuilds; aria sweep from live DOM: 16× `Subagent (nested) · developer · level 2` vs 6× `Subagent · self-improver · level 1` — depth classes distinguished, labels stable across re-renders. Screenshot ac3-ac5-stress-settled-fixb. UNVERIFIED leg: live tree maxes at depth 2 — **zero L3+ compact-variant cards rendered live**. BLOCKER (named): driving depth-3 requires dedicated fixture subagent types with inter-agent task permissions (the #2762 pattern, defined in `opencode.json` at commit 3f0ec0c); the tester sandbox denies writing `opencode.json` and `.opencode/agents/**` (writable: `.opencode/tmp/**`, `.opencode/tests/**`, references.md only); 4 prompt-only nesting attempts with Muse Spark 1.2 Free failed to self-nest (children ran the leaf work themselves — spans in telemetry_spans sessions ses_fabe4c9c1/ses_fabe144b2/ses_fabdb7763/ses_fabd9f3ca show no task spans); the OTLP span injector is forbidden (G-080 human decision). L3-compact geometry IS covered at unit level (layout.deep-tree.test.ts 20/20 incl. depth-3/4 tier pins + compact-class assertions). Round-2 fix: SI restores the fixture agents in opencode.json (env prep), re-test ONLY the L3 legs (FM-15 edge + FM-20 L3 + E-2770-4).
