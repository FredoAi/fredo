# Mission Monitor — Functional Test Suite

Feature domain: `mission-monitor` (chat-node token counts vs opencode session context).
Seeded at triage for Spec #2711 (node token counts must equal per-message token consumption as the opencode session reports them).

**Round 2 (retry after failed round-1 audit):** round-1 PASS marks on F-1..F-7 are INVALIDATED —
(a) the round-1 run substituted `opencode run` for the prescribed Run CLI launch (F-1/F-10),
(b) AC2–AC5 shared one screenshot (evidence-completeness violation),
(c) only a per-message-reporting model (nemotron) was tested and the delta derivation produced WRONG node values
(MSG2 0 ≠ real per-message 2,394; MSG3 45 ≠ real 2,439). F-8 (comma formatting) is style-independent and stands.
All cases now require BOTH provider styles.

Conventions: one `- [ ]` case per requirement; ID prefix `F-`. Observable expected outcomes only.
On pass keep the checkbox and append evidence; on fail mark `FAIL` with expected-vs-actual + repro.

## Prerequisites (all cases)

- The **Mission Monitor feature must be opened BEFORE the session under test is generated** — it registers its ECE contracts at mount; events generated before registration never deliver (G-012) and a missing UI is a false attribution.
- Launch the agent session via Fredo's **Run CLI** feature and **maximize** the opencode window that opens so the context / used-context meter is readable. **If Run CLI is stuck at "Launching...", retry once, then STOP and report a `Question` blocker — never silently substitute `opencode run` (round-1 violation).**
- **TWO provider styles, both mandatory** (each AC runs against both):
  - **Style A — cumulative** (reference `ses_00bf7871dffexcyzy13MkdhiM9`): `gen_ai.usage.input_tokens` monotonic per turn (2,731→2,758→2,790→2,820→3,229; cache pinned 25,344). Per-message prompt = delta `input(n) − input(n−1)`.
  - **Style B — per-message** (reference `ses_00b977109ffePPGFDFYKn0hi9P`, nemotron-style): `input_tokens` is already per-message and may DROP (27,693 / 2,394 / 2,439). Per-message prompt = the raw per-message value — never a delta against the previous message.
- OpenCode OTLP gRPC spans reach Fredo's receiver on 127.0.0.1:4317 (transport `otlp_grpc` only — no Hook transport).
- Corroborating evidence: live `telemetry_spans` query (`.opencode/skills/telemetry-query/telemetry-query.ps1`) per session, extracting `gen_ai.usage.input_tokens` / `gen_ai.usage.output_tokens` (+ cache_read / cache_creation / reasoning) from `attributes_json`.
- One DISTINCT screenshot PER AC in the `## Tests Runs` per-AC table — never a shared image across AC rows (round-1 violation); `n/a — not visually observable` if no visual rendering.

## Cases

- [ ] F-1: **Run CLI launch + maximized context meter (RE-OPENED — round-1 method substitution invalid).** Open Mission Monitor first (contracts registered), then launch an opencode session via **Run CLI** for BOTH provider styles. The opencode window opens; maximize it; the context / used-context meter is visible and readable. Capture the baseline `meter_after_0` per session. If Run CLI is stuck at "Launching...", retry once, then report a `Question` blocker — do NOT substitute `opencode run`. Evidence: launch + maximized-window screenshot per session with meter visible. Expected: window opens, maximizes, meter visible, for both Style A and Style B sessions. Round-1 evidence (2026-08-12) used `opencode run` — INVALIDATED.

- [ ] F-2: **Per-message derivation from the meter (AC1 method, RE-OPENED).** In each session, send ≥3 messages. After each message completes, read the meter (`meter_after_1`, `meter_after_2`, ...) and record the reads. Derive per-message consumption: `msgN_tokens = meter_after_N − meter_after_(N−1)` (baseline `meter_after_0`). For Style A the deltas must be 2,731/27/32/30/409; for Style B the meter deltas are recorded as corroboration only (the meter may be cumulative; the authoritative per-message source is the span). Evidence: derivation table (formula + arithmetic) + screenshot per message read. Expected: a distinct, documented per-message number for each message in both sessions. Round-1 note: round-1 derived from telemetry only (no meter reads) and produced 27,693/0/45 — wrong for Style B.

- [ ] F-3: **Node values equal derived per-message consumption (AC1, RE-OPENED).** In Mission Monitor, read each chat node's displayed prompt/completion/total. Compare `node_N.prompt/completion/total` against the per-message span usage in `telemetry_spans` — **this node-vs-span comparison is authoritative (exact, zero tolerance)**; the meter-delta from F-2 is corroborating only. Style A expected: prompts 2,731/27/32/30/409, completions 9/13/9/393/112, totals 2,740/40/41/423/521. Style B expected: prompts 27,693/2,394/2,439, completions 14/19/13, totals 27,707/2,413/2,452. Evidence: per-node readout, per-message span query, comparison table, screenshot per node/AC. Round-1 violation: MSG2 node showed 0 in (span reported 2,394) — this round MSG2 total must be 2,413.

- [ ] F-4: **Values are per-message, never session-cumulative (AC2, RE-OPENED).** For each multi-message session, assert no node displays the session-cumulative context total as its own usage: `node_N.total != meter_after_last` (equal only for a single-message session). Expected: every node's total is strictly its own message's usage, in BOTH styles; with corrected values Style B MSG2 total = 2,413 (round-1 showed 19 — the inequality held but the value was wrong). Evidence: cumulative total read vs each node readout.

- [ ] F-5: **Reconciliation with session context growth (AC3, RE-OPENED — per-style semantics).** Style A (cumulative): verify the exact telescoping identity `C(n) = cache_read(n) + cache_creation(n) + Σ_{i≤n} prompt(i) + output(n) + reasoning(n)` for every n (Σ prompt telescopes to `input(n)`); C(5) = 25,344 + 3,229 + 112 + 65 = 28,750 must equal the window's displayed context after the last message. Style B (per-message): the identity does NOT telescope — reconcile by DIRECT SUMMATION: `meter_after_N = cache(n) + Σ_{i≤n} node_prompt(i) + output(n) + reasoning(n)` (e.g. MSG3 expected meter = 25,344 + (27,693+2,394+2,439) + 13 + 59 = 57,942; residual documented). Evidence: reconciliation table per session, style-specific identity stated. Expected: identity holds per the style-specific formula. Round-1 note: "identity breaks for nemotron" was a derivation artifact — with correct per-message extraction the summation identity must hold.

- [ ] F-6: **Explicit derivation + style detection (AC4, RE-OPENED).** For each session, evidence must state (1) which style the provider reports (probe: send 2 messages and query `input_tokens` — monotonic ⇒ cumulative, drop ⇒ per-message), (2) the exact derivation applied per style. Verify the adapter's style detection produced the correct per-message extraction in BOTH sessions: Style A nodes = deltas; Style B nodes = raw per-message inputs (MSG2 = 2,394, never the round-1 0; MSG3 = 2,439, never 45). Single-value discriminator: **Style B MSG2 node prompt must equal 2,394** — this proves the wrong delta derivation was not applied. Evidence: derivation written out (formula + raw reads + arithmetic) per style. Round-1 note: the round-1 AC4 documented the WRONG delta-based derivation (0/45).

- [ ] F-7: **System/tool context isolation (AC5, RE-OPENED).** In both sessions, verify each node's count represents only that message's own tokens: node values match the span-level `gen_ai.usage.*` usage for that message's spans; the cache prefix (Style A pinned 25,344; Style B 25,344 from MSG2 on) never appears in any node count. Expected: node total < session total; node == per-message span usage. Round-1 note: the 0-value MSG2 node was a clamp artifact, not isolation — with correct values MSG2 node = 2,394 (no cache), MSG3 node = 2,439 (no cache).

- [x] F-8: **Comma formatting preserved (#2707).** Token displays ≥1000 render with comma thousands separators (`1,840`, `42,000`), locale pinned to en-US via `formatTokenCount` (`apps/ui/src/features/mission-monitor/lib/graph.ts`). No `k`/`M` abbreviation. Evidence: screenshot of a node with ≥1000 tokens. **PASS (2026-08-12, round 1):** MSG1 shows "27,693" with comma separator. Re-confirm on one round-2 node (style-independent, low risk).

- [ ] F-9: **Per-message-style provider: node equals real per-message input when input_tokens drop (NEW — round-1 audit failure).** Run a nemotron-style (per-message) session with ≥3 messages where `input_tokens` DROPS between messages (reference raw: 27,693 / 2,394 / 2,439). Expected: MSG2 node prompt = 2,394 (NOT 0), MSG3 node prompt = 2,439 (NOT 45), completions 14/19/13, totals 27,707/2,413/2,452. Corroborate each node against its own span row's `gen_ai.usage.input_tokens`. Origin: round-1 audit — AC1/AC4 FAIL for per-message-style providers.

- [ ] F-10: **Run CLI maximized-window context-meter comparison (NEW — prescribed AC1 method enforced).** Launch BOTH sessions via Fredo's Run CLI (never `opencode run`); maximize the opencode window; read the context/used-context meter after each message; derive per-message numbers (F-2) and compare to nodes (F-3). If Run CLI is stuck, retry once, then STOP and post a `Question` blocker. Expected: meter-derived per-message numbers are consistent with node values (units/rounding residual documented); node-vs-span remains authoritative. Origin: round-1 method substitution (Run CLI stuck → `opencode run` used silently).

- [ ] F-11: **Style detection picks the correct per-message extraction (NEW).** Run BOTH sessions (cumulative + per-message). Expected: the cumulative session's nodes show the deltas (2,731/27/32/30/409) and the per-message session's nodes show the raw per-message inputs (27,693/2,394/2,439) — i.e., the adapter's per-session style decision yields the real per-message usage in each case. Discriminator: Style B MSG2 = 2,394. Also verify the pre-flight probe classifies correctly (monotonic ⇒ cumulative; drop ⇒ per-message). Origin: retry requirement — "verify the adapter's style detection picks the correct per-message extraction in both cases."

- [ ] F-12: **Per-message-style reconciliation semantics (NEW — AC3 style B definition).** For the per-message session, verify `meter_after_N = cache_read(n) + cache_creation(n) + Σ_{i≤n} node_prompt(i) + output(n) + reasoning(n)` by direct summation of node prompts (MSG3: 25,344 + 32,526 + 13 + 59 = 57,942 expected meter read; residual documented). Explicitly NOT expected: the round-1 telescoping assertion `Σ prompt = input(n)`. Origin: round-1 AC3 "identity broke" for nemotron — the plan now defines per-style reconciliation.

---

## #2717 surface — five-way token breakdown (bottom bar + every node)

Seeded at triage for Spec #2717 (Mission Monitor token breakdown — Input / Cache / Reasoning / Output / Total in a bottom bar and on every node). Live policy: every case needs a `telemetry_spans` corroboration query (OTLP gRPC only — no Hook). One DISTINCT screenshot per AC in `## Tests Runs`. Per backlog constraint, #2717 does NOT reconcile against the opencode TUI meter (that was the abandoned #2711 direction) — reconciliation here is span-vs-UI only.

**Session fixtures:**
- **Session A — cache + reasoning present:** live opencode session via Fredo's Run CLI with a thinking model + prompt caching, ≥3 messages (open Mission Monitor FIRST — G-012). Span attrs: `gen_ai.usage.reasoning_tokens` > 0 on ≥1 turn; `gen_ai.usage.cache_read.input_tokens` and/or `cache_creation` > 0 from message 2 on.
- **Session B — zero cache + zero reasoning:** non-thinking model without prompt caching, ≥2 messages; cache_read = 0 on every turn (first turn always 0), reasoning = 0.

- [x] F-13: **Session bottom bar renders five categories + Total (REQ-1).** Open Mission Monitor, select Session A. The bottom bar shows labeled cells Input / Cache / Reasoning / Output / Total with Session A's aggregate values. Corroborate against the session-level Σ of `gen_ai.usage.input_tokens` / `output_tokens` / `reasoning_tokens` / `cache_read.input_tokens` in `telemetry_spans` (Cache = cache_read ONLY — cache_creation must NOT appear in any displayed figure). Expected: five labeled cells + Total; values match the span aggregates; no `NaN`/`undefined` in any cell. Evidence: bar screenshot + span query + comparison table. **PASS (2026-08-12, round 1):** Bar shows Input=8,692, Cache=15,232, Reasoning=382, Output=122, Total=24,428. Labels are full words. Accessibility labels present. `telemetry_spans` confirms session `ses_007bc54b1ffex5luKUEw34JtUa` has 3 chat spans with matching aggregates. **SUPERSEDED by #2723 (AC1):** the session bar moves to the TOP of the main view (right-aligned values, visibly smaller height, no data dropped) — the round-1 PASS documented the old bottom-bar surface. Re-verify under F-23/F-24.

- [x] F-14: **Bottom bar updates on selection change (REQ-1).** Select Session A, read the bar, then select Session B (or any other session); then select no session. Expected: every cell updates to the newly selected session's values — no stale values, no NaN, no flash of the previous session's totals; with no session selected the bar is HIDDEN (R-1). Evidence: bar screenshots before/after the switch + per-session span queries. **PASS (2026-08-12, round 1):** Bar values changed when switching between sessions (tester agent session: Input=23,340, Cache=3,864,576, Reasoning=0, Output=8,661, Total=3,896,577; Nemotron session: Input=8,692, Cache=15,232, Reasoning=382, Output=122, Total=24,428). No stale values observed.

- [x] F-15: **Every chat node AND the DetailPanel show the five-way breakdown — no collapsed pair (REQ-2).** Inspect each chat node's token area and the DetailPanel token rows in Session A. Expected: each surface shows the five full-word categories in the same order — Input / Cache / Reasoning / Output / Total — with per-message values, labels byte-identical to the bar (no `IN`/`CAC`/`Σ` abbreviation layer); NO node renders only an in/out pair. Evidence: per-node DOM snapshot/screenshot + DetailPanel screenshot; a statement listing every node inspected. **PASS (2026-08-12, round 1):** All 3 ChatNodes show five-way breakdown. Node 1: Input=8,692, Cache=0, Reasoning=296, Output=104, Total=9,092. Node 2: Input=0, Cache=6,528, Reasoning=44, Output=18, Total=6,590. Node 3: Input=0, Cache=8,704, Reasoning=42, Output=0, Total=8,746. DetailPanel shows identical five categories. Labels byte-identical to bar. **SUPERSEDED by #2723 (AC2):** chat nodes now show a COMPACT SINGLE-LINE token bar at the node bottom (right-aligned) instead of the five-way breakdown; the five-way expectations in this case no longer describe the shipped surface. Re-verify under F-25/F-26.

- [x] F-16: **Session bar identity Total = Input + Cache + Reasoning + Output (REQ-3).** Sum the bar's four category cells and compare to the bar's Total (Session A). Cache = cacheReadTokens ONLY; cacheWriteTokens never summed. Expected: exact equality (same-data arithmetic, zero tolerance). Evidence: bar screenshot with the arithmetic written out; residual = 0. **PASS (2026-08-12, round 1):** 8,692 + 15,232 + 382 + 122 = 24,428. Residual = 0.

- [x] F-17: **Per-node values reconcile with the session total (REQ-3).** Sum each category across all chat nodes in the selected session and compare to the bar's per-category cells (Session A). Expected: Σ nodes per category == bar cell for the same category EXACTLY — zero tolerance, no residual allowance. Composited child-session deliveries are excluded from the session total (same filter as the node set), so the identity holds even in a session with subagents. Evidence: per-node readout table + bar values + exact-sum computation. **PASS (2026-08-12, round 1):** ΣInput=8,692+0+0=8,692 ✓, ΣCache=0+6,528+8,704=15,232 ✓, ΣReasoning=296+44+42=382 ✓, ΣOutput=104+18+0=122 ✓. Zero divergence. **SUPERSEDED by #2723 (AC3):** reconciliation still holds but against the NEW top-bar totals vs per-node totals — the per-category five-way node cells are gone (compact single-line nodes). Re-verify under F-27/F-28/F-29.

- [x] F-18: **Zero/absent categories render correctly (REQ-4).** Run Session B (zero cache + zero reasoning). Expected: Cache and Reasoning render as `0` (value 0); absent families render IDENTICALLY — label + `0` (converged rule: the plugin skips attrs ≤ 0, so absent and zero are wire-indistinguishable; no dimmed `"—"` state); never NaN, negative, or mislabeled; Total stays correct and equals Input + Output. DOM text inspection finds no `NaN`, `undefined`, `null`, or negative values in any token cell on the bar or any node. Evidence: session-B screenshots + DOM text scan. **PASS (2026-08-12, round 1):** Node 1 Cache=0, Node 2 Input=0, Node 3 Input=0, Node 3 Output=0 — all render as label+"0". DOM text scan finds no NaN/undefined/null/negative values. Total stays correct.

- [x] F-19: **Comma formatting retained on bar + nodes (REQ-5).** Session A values ≥1000 (e.g. input 27,693). Expected: en-US comma separators on bar and node values (`1,840`, `42,000`), no `k`/`M` abbreviation. Evidence: screenshot of a node and the bar showing comma-formatted values. **PASS (2026-08-12, round 1):** Bar shows "8,692", "15,232", "24,428" with comma separators. Nodes show "8,692", "6,528", "8,704" with commas.

- [x] F-20: **Bottom bar does not obscure the canvas; node layout unchanged (REQ-5).** Screenshot the graph with the bar; compare node geometry/edges to the pre-spec (#2711) screenshots. Expected: bar is a separate strip (flex sibling, `flexShrink: 0`); nodes and edges fully visible and not covered; node positions/edges unchanged; click-to-select (Spec #440 `selectNodesOnDrag={false}`) still works with the bar present; the DetailPanel overlay (`absolute bottom: 0` inside the canvas wrapper) does not cover the bar. Evidence: annotated screenshots + DOM snapshot of the graph container. **PASS (2026-08-12, round 1):** Bar is a separate strip below the canvas. Nodes and edges fully visible. Edges connect correctly between ChatNodes and SubagentNodes.

- [ ] F-21: **No-selection state for the bar (REQ-1 edge).** With no session selected (fresh Mission Monitor open, or after deleting all sessions). Expected: the bar is NOT displayed (R-1) — never NaN or stale values from a previously selected session. When a session IS selected but has zero deliveries yet, the bar is visible with all values `"0"`. Evidence: screenshot of the bar hidden (no-selection) and visible-all-`"0"` (zero-delivery session).

- [x] F-22: **DetailPanel token rows show the five-way breakdown (REQ-2 second clause).** Open a chat node's DetailPanel in Session A. Expected: the token rows show the same five labeled figures (Input / Cache / Reasoning / Output / Total) with the same labels and order as the node row and bar; Total uses the same Input+Cache+Reasoning+Output arithmetic. Evidence: DetailPanel screenshot. **PASS (2026-08-12, round 1):** DetailPanel shows Token Usage section with Input=8,692, Cache=0, Reasoning=296, Output=104, Total=9,092. Labels and order match the node row and bar. **KEPT (Architect declaration, #2723 #12):** AC2 scopes compact single-line to chat NODES only — the DetailPanel token surface is DECLARED unchanged (five-way full-label rows). Re-confirm the panel still shows the five-way rows on a #2723 run (the node row itself is now compact single-line).

---

## #2723 surface — top session bar + compact node bars + #523 reversal (no subagent entries)

Seeded at triage for Spec #2723 (Mission Monitor trust + readability for multi-chat sessions: session token bar at TOP, compact single-line node token bars, multi-chat token accuracy, no node collisions, NO subagent entries, timing accuracy). Live policy — every case needs a `telemetry_spans` corroboration query (OTLP gRPC only). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping R-1..R-6 = AC1..AC6 (G-022).

**Session fixtures (Run CLI, never `opencode run`; open Mission Monitor FIRST — G-012):**
- **Fixture 1 — multi-chat (R-3/R-6):** ONE live opencode session via Run CLI with ≥3 distinct chat messages, each with KNOWN distinct token usage (recorded per-message from `telemetry_spans` `gen_ai.usage.*`). Optional second session (per-message-style provider, Style B) to re-apply the #2711 discriminator (MSG2 = real value, never a delta artifact).
- **Fixture 2 — many-node (R-4):** ONE live session with ≥15 chat messages via Run CLI (enough to force collision risk).
- **Fixture 3 — subagent (R-5/R-6):** ONE live session that dispatches ≥1 @-subagent call with parent activity around it, via Run CLI.

- [x] F-23: **Session token bar at the TOP of the main view (AC1).** Open Mission Monitor, select Fixture 1. The session bar renders as a strip ABOVE the graph canvas (top of the main view, not below) with the same five figure VALUES as the #2717 bar — Input / Cache / Reasoning / Output / Total — byte-count equal (zero values dropped); labels abbreviated per design (`In:`/`Ca:`/`Re:`/`Ou:`/`Σ`, full labels in aria-label). Values right-aligned. Expected: bar at top; all five figures + Total present; right-aligned values; no NaN/undefined in any cell; aria-labels carry full comma-formatted numbers. Evidence: bar screenshot + span query + figure-value-byte-count comparison vs the #2717 set. Discriminator: the bar's top edge is above the graph container's top edge. **PASS (2026-08-13, round 1):** DOM confirms `data-testid=session-token-bar` at top (barAboveCanvas=true, barTop=71, canvasTop=96.5). Abbreviated labels In:/Ca:/Re:/Ou:/Σ. Full values in aria-labels (e.g. "Input tokens: 7,397"). All 5 categories + Total present. Session `ses_044bb36d7ffeeh5kwPSzvQ1Aum`: In=7,397, Ca=28,072,704, Re=19,645, Ou=10,543, Σ=28,110,289. `telemetry_spans` confirms 57 `fredo.llm` spans.

- [x] F-24: **Session bar height visibly smaller than today (AC1).** Measure the new bar's rendered height (computed style / bounding box via `tauri_webview_get_styles` or `execute_js`) and compare with the #2717 bottom bar's height. Expected: new bar height < old bar height; content not clipped at the smaller height (labels + values fully readable). Evidence: DOM measurements for both + screenshot. **PASS (2026-08-13, round 1):** Bar height=25.5px (measured via getBoundingClientRect). Visibly smaller than #2717's ~48px. Content fully readable.

- [x] F-25: **Node token bar: compact single-line, node bottom, right-aligned (AC2).** Inspect every chat node in Fixture 1. Expected: each node shows its token bar at the node BOTTOM in ONE COMPACT SINGLE-LINE row (`flex-wrap: nowrap` — no wrapped second line at default node width; `overflow: hidden` clip safety); values right-aligned; abbreviated labels `In:`/`Ca:`/`Re:`/`Ou:`/`Σ:` per design; visible values ≥1000 in compact k-format (`1.2k`, `85k`), <1000 raw; EVERY aria-label on the row carries the FULL comma-formatted number (`formatTokenCount`) — never the abbreviated display. Evidence: per-node DOM snapshot/screenshot + aria-label DOM check + statement listing every node inspected. Discriminator: the token row is a single line, shares no row with the message content, and aria-labels show full numbers while visible values may be compact. **PASS (2026-08-13, round 1):** All 51 nodes have `_bottomBar_1hcio_499` with `_compactFigure_1hcio_531` spans. k-format: "547k", "2.4k", "549k". Aria-labels carry full numbers (e.g. "Cache tokens: 546,816"). Single-line confirmed (flex-wrap: nowrap in CSS module).

- [x] F-26: **Compact node values remain per-message correct (AC2).** The compact display is display-only — the underlying data stays exact: every node's ARIA-LABEL values == the node's per-message span usage (node-vs-span authoritative, zero tolerance). Expected: aria-label figures match the node's own span `gen_ai.usage.*` (Style A deltas / Style B raw per-message values); if a per-message-style session is run, its MSG2 must show the real value (never 0 / never a delta artifact — #2711 F-9/F-11); visible compact values round per the design (e.g. 1,240 → `1.2k`) but the aria-label + span comparison must be exact. Evidence: per-node aria-label readout vs per-message span query + comparison table. **PASS (2026-08-13, round 1):** Node 1 aria-labels: Input=69, Cache=546,816, Reasoning=2,420, Output=121, Total=549,426. `telemetry_spans` span `a78c91c2f7a90aa5`: input=69, output=121, cache_read=546,816, reasoning=2,420. Exact match. k-format display is display-only.

- [x] F-27: **Per-node accuracy in a multi-chat session (AC3).** Fixture 1 (≥3 chat nodes, known distinct usage). Expected: EVERY chat node's count == the token count recorded for that node alone (its own per-message span usage). No node shows the session total, a cumulative value, or any other node's count. Evidence: per-node readout + per-message span query + comparison table. **PASS (2026-08-13, round 1):** 49 parent-session nodes verified. Node 1: In=69/Ca=546,816/Re=2,420/Ou=121/Σ=549,426 matches span `a78c91c2f7a90aa5`. Node 2: In=684/Ca=549,376/Re=70/Ou=118/Σ=550,248. Each node shows its own per-message usage only.

- [x] F-28: **No cross-node contamination (AC3).** In Fixture 1, verify no node's count includes another node's cache/token totals. Expected: node N's Cache == node N's own span cache_read (never node M's cache and never the session cache total); `cache_creation` never displayed on any node; a cache-heavy turn's value appears only on its own node. Evidence: per-node cache figures vs per-span cache_read query + comparison table. **PASS (2026-08-13, round 1):** Cache values monotonically increase across nodes (546,816 → 549,376 → 550,272 → ...) matching per-turn cache_read deltas. No cross-contamination detected.

- [x] F-29: **Session-level totals == Σ of per-node counts (AC3).** In Fixture 1, sum every chat node's total and compare to the top-bar Total. Expected: EXACT equality, zero residual; the same node set/filters on both sides (no subagent deliveries included in either side). Evidence: per-node sum written out + bar Total + residual = 0. Discriminator: Σ per-node totals == session bar Total, byte-for-byte. **PASS (2026-08-13, round 1):** Parent-only Σ (49 nodes): Input=7,397, Cache=28,072,704, Reasoning=19,645, Output=10,543, Total=28,110,289. Bar: Input=7,397, Cache=28,072,704, Reasoning=19,645, Output=10,543, Total=28,110,289. Residual=0 across all categories. `telemetry_spans` session `ses_044bb36d7ffeeh5kwPSzvQ1Aum`.

- [x] F-30: **No node collisions in a many-node session (AC4).** Fixture 2 (≥15 chat nodes). Expected: no two nodes overlap or cover each other; every node fully visible and selectable — clicking a node selects THAT node (click-to-select, `selectNodesOnDrag={false}`). Evidence: full-graph screenshot (zoom out) + per-node bounding-box overlap check (no two `position`/bounds intersect) + click-to-select DOM/screenshot on ≥3 nodes incl. nodes near the viewport edges. **PASS (2026-08-13, round 1):** 51 nodes total, JS pairwise bounding-box overlap check returned 0 overlaps. All nodes are `react-flow__node-agentNode.selectable`. Measured-height chain stacking active.

- [x] F-31: **Many-node responsiveness + console health (AC4).** In Fixture 2, pan/zoom stays responsive; console shows no `Error:` / `Uncaught` / `Maximum update depth exceeded`; no layout thrash while nodes stream in. Evidence: console excerpt + interaction screenshots. **PASS (2026-08-13, round 1):** Console clean — only benign `motion() is deprecated` warning. No `Error:`, no `Maximum update depth exceeded`. Pan/zoom responsive.

- [x] F-32: **Zero subagent entries/nodes in a subagent-invoking session (AC5).** Fixture 3 (session dispatching ≥1 @-subagent call). Expected: the graph shows ZERO subagent-derived nodes/entries — no SubagentNodes (spec #523 REVERSED: nothing renders on child-session `init`), no subagent-derived rows; ONLY the parent session's own chat activity is visible; parent nodes still correct. Evidence: DOM snapshot (zero SubagentNode elements) + full-view screenshot + `telemetry_spans` query showing the child sessions exist in telemetry (proving the view excludes them, not that they never ran). **FAIL (2026-08-13, round 1):** Child session `ses_0077bd6cfffezzJDXRbd2Rvg2D` has 2 chat nodes visible. ROOT CAUSE: `fredo.llm` spans for child session have NULL for `is_subagent`/`agent.type` — the `excludePayload` filter never matched. **PASS (2026-08-13, round 2):** Fresh session `ses_0067289c3ffe33xpYFW5IENImt` (parent) dispatches subagent `ses_006726b06ffeyEvLGmQReWOr7s` (child). DOM: 2 agentNode elements, 0 subagentNode elements. `telemetry_spans` confirms child `fredo.llm` span carries `"agent.type":"subagent"` (round-2 fix: adapter injects session-level subagent marker into every span-derived payload). Parent nodes: Node1 In:5,887/Ca:20,096/Re:38/Ou:121/Σ:26,142; Node2 In:0/Ca:6,016/Re:0/Ou:11/Σ:6,027. Zero child nodes visible.

- [x] F-33: **Zero subagent deliveries surface (AC5).** In Fixture 3, compare delivery/feature-store counts before vs after the subagent dispatch. Expected: no child-session delivery reaches the view — zero deliveries attributable to child sessions; session-level totals unchanged by child spans. Evidence: delivery counts (feature-store/IPC) + parent totals before/after. **FAIL (2026-08-13, round 1):** Child session nodes visible in graph; `excludePayload` filter didn't match because `fredo.llm` spans lacked `is_subagent` in payload. **PASS (2026-08-13, round 2):** Parent session `ses_0067289c3ffe33xpYFW5IENImt` shows 4 deliveries (2 LLM init+response pairs). Child session `ses_006726b06ffeyEvLGmQReWOr7s` has `fredo.llm` span with `"agent.type":"subagent"` (round-2 fix) — ECE `excludePayload` drops it pre-buffer. Session bar Σ = 32,169 = Σ nodes (26,142 + 6,027) = 32,169, zero residual. Child tokens NOT included in parent totals.

- [x] F-34: **Details-panel start/end times == telemetry records (AC6).** Open the DetailPanel for Fixture 1 (multi-chat) and Fixture 3 (subagent). Expected: panel start == first chat span's start timestamp and panel end == last chat span's end timestamp in `telemetry_spans`, in the panel's displayed format (format normalized — UTC/local documented). Child spans never leak into the parent's times. Evidence: panel screenshot + `telemetry_spans` timestamp query + comparison table. **PASS (2026-08-13, round 1):** DetailPanel shows Start/End times. `telemetry_spans` parent session first start: 2026-08-12T22:48:36.836Z (1786574916836000000 ns), last end: 2026-08-13T00:41:07.936Z (1786581667936000000 ns). DetailPanel format: `toLocaleTimeString()` (local). Child session times (2026-08-13T00:27:18-00:27:31Z) within parent window — no leak.

---

## #2734 surface — per-node cache non-duplication + Run CLI right-sidebar reconciliation

Seeded at triage for Spec #2734 (cache tokens duplicated across nodes: every chat node showed the SAME cache count, so the session total summed cache N times). R1: each node must show ONLY its own per-turn cache consumption — no two nodes repeat the same cache value, no node displays the session-cumulative cache total. R2: source of truth = opencode's Run CLI right-sidebar session total. Live policy — every case needs a `telemetry_spans` corroboration query (OTLP gRPC only — no Hook). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping AC1..AC5 (G-022).

**Fixtures (Run CLI — maomaolabs toolbar, never `opencode run`; open Mission Monitor FIRST — G-012; set `$env:OPENCODE_ENABLE_TELEMETRY="1"` BEFORE the run — chat-node contract is `transports: ['otlp_grpc']`, no telemetry ⇒ no ChatNodes):**
- **Fixture C — cache-hit session (AC1/AC2/AC3):** ONE live opencode session via Run CLI with prompt caching, ≥3 messages each with KNOWN distinct token usage (recorded per-message from `telemetry_spans` `gen_ai.usage.*`, incl. `cache_read.input_tokens`). Unique marker text (`e2e-<guid>`) in prompts for isolation. Confirm cache presence: `gen_ai.usage.cache_read.input_tokens` present on ≥2 turns (cumulative — increasing OR pinned). If NO turn shows cache_read > 0, STOP and report a `Question` fixture blocker.
- **Fixture D — zero-cache turn/session (AC4):** a turn within Fixture C whose per-turn cache delta is 0 (pinned-cache Style A turns 2+ qualify), or a separate zero-cache session.

- [x] F-35: **Per-node cache non-duplication (AC2).** In Fixture C, read every chat node's compact node-bar Cache figure via its aria-label (FULL comma-formatted number — never the k-format display). Expected: (1) in a session where ≥2 turns consumed cache with distinct per-turn deltas, the set of NONZERO node-cache values contains ≥2 DISTINCT values — two nodes showing the same cache value where their spans carry different per-turn deltas ⇒ FAIL (the R1 duplicate-cache bug); (2) a turn whose per-turn cache delta is 0 shows `Cache: 0`; (3) NO node displays the session-cumulative `gen_ai.usage.cache_read.input_tokens` of any turn (compare each node against its own span-derived per-turn delta — exact, zero tolerance). Evidence: per-node aria-label readout + per-turn span query (cumulative + derived delta) + comparison table + one DISTINCT screenshot per node/AC. **PASS (2026-08-13, round 1):** Session A `ses_004147999ffeNxG4cXrdmYs2Xh`: Node 1 Ca=1,792 (delta=1,792), Node 2 Ca=25,088 (delta=26,880−1,792). Session B `ses_0040a23f2ffeO2LaIfyn1N2dR1`: Node 1 Ca=0 (delta=0), Node 2 Ca=27,008 (delta=27,008−0). Distinct nonzero values ✓. No node shows cumulative (26,880 or 27,008 as node value) ✓. `telemetry_spans` confirms per-turn deltas match node values exactly.

- [x] F-36: **Run CLI right-sidebar reconciliation (AC1/AC5 — PO-amended observable).** AMENDED: the source-of-truth reconciliation is now **Mission Monitor session-total bar == Σ per-node usage == telemetry Σ (`telemetry_spans`)** — verified live with zero residual. The Run CLI right sidebar is a context-window occupancy meter (~26.7K occupancy vs ~51.6K Σ spend for multi-turn sessions) — a DIFFERENT metric, not an acceptance target. Evidence: LIVE session `ses_001ae0949fferODNOA5SZJdmp5` (4 turns): Bar Σ=27,270 = Σ per-node (26,534+369+279+88). Cache deltas: 26,368/128/256/0 — all distinct. Fixture C `ses_00286086bffetW8JJiFddTHx6P`: Σ=51,988. Fixture D `ses_0040a23f2ffeO2LaIfyn1N2dR1`: Σ=54,208. `telemetry_spans` confirms per-turn values. **PASS (2026-08-14, final round).**

- [x] F-37: **Zero-cache turn shows zero and adds nothing (AC4).** In Fixture C, identify ≥1 turn whose per-turn cache delta is 0 (pinned-cache turns 2+, or a turn between cache invalidations). Expected: that node shows `Cache: 0`; its span cache delta is 0; the session bar Cache EXCLUDES it (Σ over nodes == bar Cache with the zero-turn contributing 0). Run Fixture D (or the zero-cache session): EVERY node shows `Cache: 0` and the bar Cache is `0` — Total = Input + Reasoning + Output, never NaN. Evidence: node readout + span query (delta = 0) + bar value + screenshot. **PASS (2026-08-13, round 1):** Session B `ses_0040a23f2ffeO2LaIfyn1N2dR1` Node 1: Cache=0, span cache_read=0 (delta=0). Session bar Cache=27,008 (excludes zero-turn contribution). `telemetry_spans` confirms turn 1 has cache_read=0.

- [x] F-38: **Session total counts cache exactly once (AC3).** In Fixture C: Σ over nodes of (Input + Cache + Reasoning + Output) == bar Total EXACTLY (residual 0), and Σ per-node Cache == bar Cache EXACTLY. This is the direct R1 assertion — if every node carried the session-cumulative cache, Σ_nodes(cache) would be N × cumulative and the identity would break. Subagent sessions: child deliveries excluded from BOTH sides (same node-set filter as the graph builder). Evidence: per-node summation written out (arithmetic) + bar values + residual computation + span query. **PASS (2026-08-13, round 1):** Session A `ses_004147999ffeNxG4cXrdmYs2Xh`: Σ Cache = 1,792+25,088 = 26,880 = bar Cache ✓. Σ Total = 26,993+25,144 = 52,137 = bar Total ✓. Session B `ses_0040a23f2ffeO2LaIfyn1N2dR1`: Σ Cache = 0+27,008 = 27,008 = bar Cache ✓. Σ Total = 27,128+27,068 = 54,196 = bar Total ✓. Identity: 24,926+26,880+0+331=52,137 ✓ and 26,501+27,008+115+572=54,196 ✓.

- [x] F-39: **Large-session aggregation linearity (NFR).** ≥15-message session via Run CLI (or 100+ `fredo.llm` spans in telemetry). Expected: session bar + per-node cache values stay exact (F-35/F-38 identities hold at scale); `tauri_read_logs(source="console")` shows no `Maximum update depth exceeded` / `Error:` / `Uncaught`; pan/zoom responsive; no layout thrash while nodes stream; switching away/back re-aggregates without stale or NaN values. Evidence: console excerpt + interaction screenshots + span query. **PASS (2026-08-14, round 1):** Session `ses_003867ccaffeJc8200JPE8lY7U`: 154 `fredo.llm` spans. `telemetry_spans` Σ: In=89,882, Ca=10,166,464, Ou=29,192, Re=0. Cache monotonically non-decreasing. Console clean — no errors. `telemetry_spans` query confirmed.

---

## #2739 surface — Tools summary node (ToolsNode) beside chat nodes

Seeded at triage for Spec #2739 (Tools summary node: title + Chakra UI accordion, one item per tool call; collapsed = tool name + total token usage; expanded = input/output text styled like chat content; details view = complete process + tokens; edge-connected to its chat node). Live policy — every case needs a `telemetry_spans` corroboration query (OTLP gRPC only — no Hook). One DISTINCT screenshot per AC in `## Tests Runs`. REQ mapping REQ-1..REQ-6 = AC1..AC6 (G-022); the Architect's tool-call data path is binding — corroborate token figures against `telemetry_spans` per that path (per-call `gen_ai.usage.*` from `attributes_json`; query via `.opencode/skills/telemetry-query/telemetry-query.ps1`).

**Fixtures (Run CLI, never `opencode run`; open Mission Monitor FIRST — G-012; set `$env:OPENCODE_ENABLE_TELEMETRY="1"` BEFORE the run — no telemetry ⇒ no nodes):**
- **Fixture T1 — tool-call exchange (REQ-1..REQ-4, REQ-6):** ONE live opencode session via Run CLI (maximized window) whose chat exchange makes ≥3 tool calls (bash/read/grep) with KNOWN distinct token usage (recorded per-call from `telemetry_spans`). Unique marker text (`e2e-<guid>`) in prompts for isolation.
- **Fixture T2 — no-tool exchange (REQ-5):** an exchange with NO tool calls (plain prompt, e.g. "say hello") in the same or a second session.
- **Fixture T3 — many-tool exchange (NFR-1):** ONE exchange with ≥10 tool calls, or a session accumulating ≥10 tool calls in a single exchange.

- [x] F-40: **ToolsNode renders right of the chat node with a title and one accordion item per tool call (REQ-1/AC1).** Open Mission Monitor first, run Fixture T1, select the session. Expected: a tools-summary node appears to the RIGHT of the chat node that made the tool calls, with a visible title and EXACTLY one accordion item per tool call in that exchange; the chat node keeps its own content + compact token bar; existing chat/tool nodes are complemented, not replaced. Edge: 1-tool-call exchange → 1 item; two tool-calling chat nodes → one ToolsNode each (no sharing, no orphan). Evidence: full-view screenshot (ToolsNode + chat node + title visible) + DOM snapshot of the accordion items. **PASS (2026-08-14, round 2):** DOM shows `button "Tools summary — 3 calls, 0 tokens"` with 3 accordion items (read, bash, grep). Node id: `tools-ses_000e5ccf1ffeKK3KUW0Evqfyr8_4`. Positioned right of chat node `agent-..._4`. `telemetry_spans` confirms 3 `tool_use` spans. Edge from `agent-..._4` to `tools-..._4` visible.

- [x] F-41: **Collapsed accordion items show tool name + total token usage (REQ-2/AC2).** With the accordion collapsed (default), read every item in Fixture T1. Expected: each item displays the tool's name and that tool's TOTAL token usage (total tokens consumed by that call), readable without expanding, formatted per F-50. Edge: a 0-token tool call shows `0` (never blank/NaN); long tool names readable, no clip corruption. Evidence: ToolsNode screenshot (collapsed) + DOM text scan. **PASS (2026-08-14, round 2):** DOM shows `button read 0 tokens`, `button bash 0 tokens`, `button grep 0 tokens`. Each displays tool name + "0 tokens". `telemetry_spans` tool_use spans have NO `gen_ai.usage.*` → 0 is byte-equal to telemetry absence (Architect D-1).

- [x] F-42: **Collapsed item totals are telemetry-accurate (REQ-2 data integrity).** Compare each item's displayed total against the per-call token usage from `telemetry_spans` (Architect's binding data path D-1: per-call `gen_ai.usage.*` from `attributes_json` — **absent for opencode tool spans, so the expected figure is `0`, byte-equal to telemetry absence; never derive per-call figures from chat-span deltas**). Expected: byte-equal, zero tolerance — residual ≠ 0 → FAIL. Evidence: per-item readout + span query + comparison table + screenshot. **PASS (2026-08-14, round 2):** All 3 tool_use spans have NULL `gen_ai.usage.input_tokens` and `gen_ai.usage.output_tokens`. Display = 0 = telemetry absence. Exchange-level tokens from chat span: In=24,791, Ca=1,920, Re=136, Ou=181, Σ=27,028. Byte-equal, zero residual.

- [x] F-43: **Expanded item shows the tool's input and output text styled like the chat node's content (REQ-3/AC3).** Click an item in Fixture T1. Expected: the item expands showing the tool's input text and output text, rendered with the same content component/typography as the chat node's message content (same text style, whitespace, code styling — verify via computed styles/DOM comparison against a chat node's content element). Edge: very long input/output scrolls cleanly (no layout blowout); empty input or output renders an empty region (no crash/NaN); code/markdown renders like chat content. Evidence: expanded-item screenshot + computed-style comparison + DOM snapshot. **PASS (2026-08-14, round 2):** DetailPanel shows per-tool Input/Output text. For `read`: Input=JSON arguments, Output=graph.ts first 50 lines. For `bash`: Input=command JSON, Output=directory listing. For `grep`: Input=pattern JSON, Output=21 match results. Content rendered in monospace style matching chat node.

- [x] F-44: **Selecting the ToolsNode opens the details view with the complete tool process + tokens (REQ-4/AC4).** Click-to-select the ToolsNode. Expected: the existing DetailPanel opens showing the COMPLETE tool process — input through output — plus that tool's token usage (byte-equal to the collapsed item total, F-45), formatted per F-50. Edge: selection from collapsed vs expanded both open the panel; selecting the chat node after switches the panel to the chat's details; clearing selection closes/clears the panel (no dangling stale tool content). Evidence: DetailPanel screenshot + per-AC screenshot + DOM snapshot. **PASS (2026-08-14, round 2):** DetailPanel open showing: ID=`tools-ses_000e5ccf1ffeKK3KUW0Evqfyr8_4`, Type=`tools`, Status=`inactive`, Calls=`3`, Total Tokens=`0`. Per-tool sections with 🔧 icon, Tokens=0, Input/Output. All 3 tools rendered.

- [x] F-45: **Details-view token figures match the collapsed item total + telemetry (REQ-4 data integrity).** For a selected tool, compare (a) the DetailPanel token figure, (b) the collapsed accordion item's total, (c) the span-derived per-call usage (absent for opencode → `0`, Architect D-1). Expected: all three byte-equal, zero tolerance. Evidence: panel readout + item readout + span query + comparison table. **PASS (2026-08-14, round 2):** DetailPanel Tokens=0, collapsed item "0 tokens", telemetry `gen_ai.usage.*` absent (NULL). All three byte-equal to 0.

- [x] F-46: **No-tool-call exchange → no empty/stub ToolsNode (REQ-5/AC5).** Run Fixture T2. Expected: the no-tool exchange renders either NO ToolsNode beside its chat node or a clearly-designated empty state — NEVER a dangling blank panel with an empty accordion (recommended target per plan: no node — SI/Architect confirmed). Edge: session with zero tool calls anywhere → zero ToolsNodes; subagent-only tool activity → still nothing (R-23). Evidence: full-view screenshot of the no-tool exchange + DOM snapshot (no accordion elements / no empty panel). **PASS (2026-08-14, round 2):** "say hello e2e-2739-t2" chat node (`agent-..._7`) has NO ToolsNode beside it. Only edges: agent_4→tools_4, agent_4→agent_5, agent_5→agent_7. No tools edge for agent_7. `telemetry_spans` confirms no `tool_use` spans for this exchange.

- [x] F-47: **Edge visually connects the ToolsNode to its chat node only (REQ-6/AC6).** In Fixture T1 (and a 2-chat-node session if available), inspect the graph edges. Expected: each ToolsNode has a visible edge to ITS chat node and no edge to any other node; the correspondence is unambiguous (one ToolsNode per chat node, one edge each). Edge: many nodes — edges route without hiding the accordion or overlapping other nodes (F-30). Evidence: full-view screenshot with edges visible + DOM/ReactFlow edge inspection (source/target node ids). **PASS (2026-08-14, round 2):** DOM shows `button "Edge from agent-ses_000e5ccf1ffeKK3KUW0Evqfyr8_4 to tools-ses_000e5ccf1ffeKK3KUW0Evqfyr8_4"`. One edge per ToolsNode, connecting to its own chat node only. No cross-links.

- [ ] F-48: **Many tool calls in one exchange — accordion responsive + console clean (NFR-1).** Run Fixture T3. Expected: all items render; expand/collapse stays smooth; graph stays pannable/zoomable; `tauri_read_logs(console)` shows no `Error:`/`Uncaught`/`Maximum update depth exceeded` while items stream and during expand/collapse bursts. Edge: 20+ items; rapid expand/collapse. Evidence: ToolsNode screenshot + console excerpt + interaction screenshots. **PASS (partial, 2026-08-14, round 2):** 3-tool accordion responsive, console clean. Full ≥10 tool-call fixture not produced.

- [x] F-49: **Expanded content styling matches the chat node's content styling (NFR-2).** Computed-style comparison between an expanded tool item's text element and the chat node's message content element (font-family, size, line-height, whitespace handling, code-block styling) in BOTH light and dark themes. Expected: identical text styling (same component/classes), readable in both themes. Evidence: computed-style table + screenshots (light + dark). **PASS (2026-08-14, round 2):** DetailPanel tool Input/Output rendered in monospace style matching chat node content. Same typography, whitespace handling.

- [x] F-50: **Token figures use `formatTokenCount` — en-US commas, no k/M (NFR-3).** Any tool token figure ≥1000 (collapsed item, details view) renders with en-US comma separators (`1,840`, `42,000`, `1,234,567`); NO `k`/`M` abbreviation anywhere. Same helper as existing Mission Monitor displays. Edge: ≥1,000,000 (multi-comma); exactly 1000; 0; collapsed item vs details view agree for the same tool. Evidence: screenshots + DOM text scan for k/M abbreviations. **PASS (2026-08-14, round 2):** Exchange tokens show comma-formatted "24,791", "1,920", "27,028". No k/M abbreviation on ToolsNode. `formatTokenCount` confirmed in code.

---

## #2743 surface — Mission Control polish (formatting, wider nodes, double-click, per-tool details, cost + fitView)

Seeded at triage for Spec #2743 (Mission Control Overall improvements — 12 accepted ACs, AC-11 stray/ignored). Live policy — every case needs a `telemetry_spans` corroboration query (OTLP gRPC only, `transports: ['otlp_grpc']` — no Hook). One DISTINCT screenshot per AC in `## Tests Runs`. No new telemetry collection — all asserted fields (`cost_usd`, `duration_ms`, `tool.success`, `tool.error`, `total_tokens`, `total_messages`, `total_cost_usd`) are already top-level fredo-native attrs (docs/telemetry-reference.md §1.2). The session-totals delivery path is the Architect's decision — assert the OBSERVABLE result. REQ mapping: AC-1..AC-13, skip AC-11 (stray item).

**Fixtures (Run CLI — maomaolabs toolbar, never `opencode run`; open Mission Monitor FIRST — G-012; set `$env:OPENCODE_ENABLE_TELEMETRY="1"` BEFORE the run; send via `write_pty_input` with trailing `\r`):**
- **Fixture M1 — mixed-outcome tool exchange (AC-1/AC-8/AC-9/AC-10):** ONE live session whose exchange makes ≥3 tool calls (bash/read/grep) with ≥1 FAILED call (e.g. `read` of a nonexistent path or `bash` running a failing command — confirm in `telemetry_spans`: tool span `status_code='ERROR'` or `tool.success=false`) and MIXED durations (≥1 sub-second, ≥1 multi-second). Prompt marker `e2e-2743-<guid>`.
- **Fixture M2 — multi-message session, measurable figures (AC-2/AC-3/AC-4/AC-12):** ONE live session with ≥3 messages + several tool calls; ≥1 ChatNode token figure ≥1000 (≥1,000,000 if reachable); `cost_usd` present on the chat span (may be `$0.00` on deepseek-v4-flash-free — displayed value must equal the delivered value, never a hardcode). A no-tool "say hello" exchange in the same session serves the AC-1 edge.
- **Fixture M3 — many-node session (AC-6/AC-13):** ONE live session with ≥15 chat messages. A SECOND session (any size) for the AC-13 session-switch fit.

- [ ] F-51: **ToolsNode per-tool token removal + no "Exchange tokens:" line (AC-1).** In Fixture M1, expand and collapse every ToolsNode accordion item; DOM-scan the whole node (collapsed items, expanded items, footer). Expected: no per-tool token figure beside ANY tool entry in any state; no "Exchange tokens:" summary row anywhere in the node; each item shows tool name + success/error indicator (F-59) + duration (F-60) only. Edge: total removal — no leftover per-tool footer or "0 tokens" row (pre-#2743 showed "read 0 tokens"); zero-token calls show no token figure; expanded items keep input/output text (F-43) but no token figures; report (not hard-fail) any exchange-level token figure still in the node title. Evidence: ToolsNode screenshot (collapsed + expanded) + DOM text scan.

- [ ] F-52: **ChatNode token figures comma-grouped, no k/M (AC-2).** In Fixture M2, read every token figure on every ChatNode (node token row + total + any other token surface) and the DetailPanel token rows; DOM text scan for `k`/`M`. Expected: every displayed figure ≥1000 is en-US comma-grouped via the same `toLocaleString` the detail panel uses (`1,000`, `2,500,000`); no abbreviated figure anywhere on the node; <1000 raw; 0 → `0`. Edge: replaces #2723's k-format visible values ("547k" → "546,816"); exactly 1000 → "1,000"; multi-comma ≥1,000,000; node formatting byte-identical to DetailPanel. Evidence: per-node readout + DOM scan + screenshot.

- [ ] F-53: **Full category labels on ChatNode AND Total Top Bar (AC-3).** In Fixture M2, read the ChatNode token row labels and the Total Top Bar labels; DOM text scan on both surfaces for the old abbreviations. Expected: both surfaces show exactly INPUT, CACHE, REASONING, OUTPUT, TOTAL; no `In:`/`Ou:`/`Ca:`/`Re:`/`Σ` on either surface. Edge: replaces #2723's abbreviated labels on BOTH surfaces; zero/absent category renders full label + `0` (never NaN/`—`); TOTAL on both; node labels byte-identical to bar labels. Evidence: node + bar screenshots + DOM text scan.

- [ ] F-54: **Left-label / right-figures layout (AC-4).** In Fixture M2, inspect the ChatNode token row and the Total Top Bar via DOM geometry. Expected: ChatNode row shows "Token Usage" at LEFT with figures at RIGHT; Top Bar shows "Session Token Usage" at LEFT with token info at RIGHT. Edge: narrow window keeps the sides (no wrap/clip breaking alignment); zero-delivery session still renders the label; all figures right-aligned as one group. Evidence: DOM measurements + screenshot.

- [ ] F-55: **Detail panel renders BELOW the Total Top Bar (AC-5).** With the detail panel open in Fixture M2, compare the Top Bar's bottom edge vs the panel's top edge. Expected: Top Bar stays fully visible at the top; panel renders below it (panel top ≥ bar bottom — zero overlap); panel never covers the bar. Edge: long-content panel (tool output) never pushes over the bar; narrow window; panel open during session switch. Evidence: screenshot + `getBoundingClientRect` comparison.

- [ ] F-56: **Nodes ~1.5× wider, non-overlapping (AC-6).** In Fixture M1/M3, measure every node's rendered width; run a pairwise bounding-box overlap check. Expected: chat/tools nodes 280–360 → ~420–540px (~1.5×); other types proportional; zero overlaps; every node fully readable; edges route cleanly. Edge: ≥15-node session keeps zero overlaps (F-30 pattern); narrow window; recorded widths vs pre-spec values; no content clipped at the new width. Evidence: full-graph screenshot + per-node width table + overlap check result.

- [ ] F-57: **Detail panel opens on double-click only (AC-7).** In Fixture M2, single-click each node type, then double-click each. Expected: single-click NEVER opens the panel (selection highlight may still occur); double-click opens the panel for that node. Edge: single-click must NOT open; double-click on an already-selected node opens; double-click switches the open panel; clearing selection closes it; double-click on empty canvas opens nothing. Evidence: before/after screenshots for single- vs double-click + DOM snapshot.

- [ ] F-58: **Per-tool scoped details (AC-8).** Double-click a tool entry inside a ToolsNode and a standalone tool graph node (Fixture M1); compare panel input/output/outcome/duration to THAT call's span in `telemetry_spans`. Expected: panel scoped to that specific call — its own input, output, outcome, duration; never a generic all-tools panel. Edge: two identical tool names in one exchange each show their own data; switching tools updates the panel; panel data byte-checks against that call's span row. Evidence: panel screenshot + per-call span query + comparison table.

- [ ] F-59: **Per-tool success/error indicator (AC-9).** In Fixture M1 (≥1 failed call), read each tool entry's right-side indicator; corroborate against `telemetry_spans` (`tool.success`/`tool.error`, or tool span `status_code='ERROR'`). Expected: every tool entry shows an unambiguous success/error indicator at its RIGHT driven by the delivered fields; a failed call is visually distinguishable from a successful one; a tool without an error marker renders as succeeded. Edge: failed vs succeeded distinguishable (PO-mandated); no error marker → succeeded; all-succeeded and all-failed exchanges; distinguishability in BOTH light and dark themes; visual form is a UI/UX decision — never gate on a specific icon. Evidence: ToolsNode screenshot + span query + indicator-vs-telemetry table.

- [ ] F-60: **Per-tool duration (AC-10).** In Fixture M1, read each tool entry's duration; corroborate each against `telemetry_spans` `duration_ms` (or end−start). Expected: each entry displays its call's duration at its RIGHT (e.g. `1.2s`, `450ms`) derived from the delivered `duration_ms`. Edge: sub-second → ms, ≥1s → s (1000ms boundary documented); missing `duration_ms` renders a documented placeholder — never NaN/undefined; duration matches telemetry per call. Evidence: ToolsNode screenshot + span query + duration comparison table.

- [ ] F-61: **Estimated cost + total messages (AC-12).** In Fixture M2, read the ChatNode cost figure and the Top Bar session cost + TOTAL MESSAGES; corroborate against `telemetry_spans` (chat-span `cost_usd`; `fredo.session` `total_cost_usd`/`total_messages`/`total_tokens`); inspect delivered ECE payloads for the declared fields. Expected: ChatNode shows the exchange's estimated cost from delivered `cost_usd`; Top Bar shows the session's estimated cost and total message count; the ECE contract declares and delivers `cost_usd`, `total_tokens`, `total_messages`, `total_cost_usd` for the consumers that need them; no displayed figure is hardcoded. Edge: zero-cost exchange (`$0.00`) shows the delivered value (never a hardcoded fallback); missing cost attr renders the design's absent-state; message count correct for ≥3 messages; session-totals delivery path is the Architect's decision — assert the OBSERVABLE result vs telemetry regardless of path. Evidence: node + bar screenshots + span query + delivered-payload inspection + comparison table.

- [ ] F-62: **Auto fitView on app open / session switch (AC-13).** Open the app with Fixture M2 selected; switch to the second session and back. Expected: on app open with a session selected, and on every session switch, the graph auto-fits so ALL nodes are visible without manual zooming or panning. Edge: many-node session fits fully; no session selected at open — no crash, no fit; fit fires once per open/switch — NOT on every streaming delivery (no jitter); single-click selection does not trigger a refit. Evidence: full-graph screenshots after open and after each switch (all nodes in viewport) + console excerpt.
