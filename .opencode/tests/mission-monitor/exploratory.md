# Mission Monitor exploratory tests

- [ ] E-2766 deep+wide: Combine four nesting levels with five or more innermost siblings; zoom out and inspect for overlap, clipping, freeze, or detached nodes. Record screenshot, DOM bounding boxes, console logs, and telemetry_spans receipt.
- [ ] E-2766 streaming: Observe a tool completing while response streams; verify the tool remains above the response and failing tool outcomes remain in chronological order.
- [ ] E-2766 orphan: Exercise an unresolved parent and verify the node is skipped rather than mis-placed; record the exact telemetry receipt.
- [ ] E-MM-1 (round 2): A new session row renders at sidebar index 0 within seconds of the first LLM span while MM is open — live streaming confirmed on R2A/R2B; arm canvas watchers (not row-list watchers) for SubagentNode witnessing.
- [ ] E-MM-2 (round 2): The FeatureStore `timestamp` of the node-creating init delivery IS the UI-appearance moment (± one React frame) — NFR-HOTP measurable without MutationObserver (promoted to event-persistence smoke S-7). DOM witnessing lags by poll cadence (R2B sighting 03:27:28.4 vs true appearance ≈03:27:17.8).
- [ ] E-MM-3 (round 2): Mission Monitor's window state persists across dev-instance cold restarts — it was found already open with the restored session list; dance steps assuming "MM closed after restart" must verify `#window-mission-monitor` presence first.
- [ ] E-MM-4 (round 2): `.react-flow__edge` elements expose NO id/data-id attributes in this ReactFlow build — NFR-DUPE receipts must key on node-id sets + edge counts.
- [ ] E-MM-5 (round 2): The agent node subtitle shows the drive's model (`build · muse-spark-1.2-contributor-free`) — a free receipt that the Run CLI model selection took effect.
- [ ] E-2770-1 Streaming burst mid-reflow: during fixture B's drive, a burst of deliveries lands while a measured-height reflow is in flight — sample `.react-flow__node` `style.transform` values with SHORT SYNCHRONOUS snippets (G-055 cadence; never delayed async JS); after settle, zero AABB overlap with ≥24px clearance persists. A transient mid-frame state is recorded, not failed — only the settled state gates.
- [ ] E-2770-2 Panel resize during reflow: resize the Mission Monitor panel while a reflow runs; fit-view honors the minZoom clamp and `zoomOnDoubleClick` records (G-040 family — read geometry in layout space, never zoom-scaled rects); graph stays collision-free.
- [ ] E-2770-3 Restore-from-persistence rebuild: after session switch away/back AND after app restart, the stress tree rebuilds to the SAME position set (compare parsed `translate()` reads per G-055) — no drift across restore paths.
- [ ] E-2770-4 Compact L3+ collapsed vs expanded: the nested distinction (3px inset stripe + nested accent) survives both card states; the state toggle changes the measured height, which triggers the R-8 reflow without overlap and without a render loop. Round 2 (FIXB2): the overlap half verified with nested fixture-l3 cards present — 10 pairs / 0 overlaps / min clearance 24px, stable across a full session-switch rebuild (switch to FIXB 24-node graph and back → identical 5-node position set, aria + stripe unchanged); compact card does not exist yet (depth ≥ 3 unreachable with the l2/l3-only fixture set — see FM-15). Tooling note: the embedded tool-call accordion inside the full-variant nested card could NOT be toggled by the driver (4 real CDP clicks + synthetic pointer sequences on the `chakra-accordion__itemTrigger`, state stayed closed; unit tests ChatNode.test.tsx:598/ToolCallAccordionItem.test.tsx verify the click-toggle, so this is driver input-injection, not a product defect) — the full-variant height-change reflow receipt therefore remains unexercised live; a synthetic pointer sequence additionally triggered one uncaught `TypeError: Cannot read properties of null (reading 'document')` at reactflow.js:1145 (20:38:57Z, driver-artifact suspected — real clicks never produced it; app healthy before/after). Round 3: compact card still does not exist live — both depth-3 drives (FIXB3/FIXB3b) were poisoned by the plugin parent-attribution wedge before any node could render (see FM-20); the compact collapsed/expanded states therefore remain unexercised, and the accordion-toggle driver limitation above stands as the named tooling constraint for any height-change reflow leg.

---

## RTDB migration (#2788) — unscripted probes

- [ ] E-2788-1 Patch storm: fire a rapid burst (≥20 patches, same key + mixed keys) inside one ~30ms flush window; verify coalescing merges without loss (final row state = last patch per field), `seq` monotonic, no duplicate deliveries, no dropped keys. **ROUND 1: PASS (live).** Burst 1: 18 concurrent emit processes (8+4 same-key mutations + 6 distinct keys) — default query coalesced same-key mutations into one delivery per window (insert@2 carrying merged fields, highest seq); flush:0 delivered individually; identical-content mutations correctly suppressed by the content-no-op gate (no churn, no lost final state). Burst 2: 6 distinct-value mutations same key — flush:0 seq 3→8 individually ~10ms apart; default coalesced to @5+@8; final row state correct (SQLite + delivery stream agree).
- [ ] E-2788-2 Replay-while-live: subscribe with `replay: true` while events are actively arriving — observe the snapshot/live boundary: no gap, no duplicate application, graph consistent after settle. **ROUND 1: partial receipt — the live tester session was streaming while subscriptions with replay were registered (retention/ac4b-2/flush queries); snapshot inserts arrived, then live patches, no duplicate application observed at the delivery level; graph-settle observation blocked by F-33.**
- [ ] E-2788-3 Malformed span at ingest: inject a garbage/partial span (missing fields, wrong types, missing `ended_at`) — ingest loop survives, row skipped-or-defaulted with a surfaced count, never a crash or silent-empty. **ROUND 1: PASS (live, HTTP leg).** Malformed JSON POST to :4318/v1/traces → receiver survived (subsequent valid posts + health 200); no row written from garbage; backfill log counters (skipped_malformed/skipped_unrecognized) exist and reported 0 on the healthy corpus.
- [ ] E-2788-4 Query args exploration: args matching zero rows (legit empty), args on deep payload paths, mixed envelope+payload args — distinguishable outcomes: mount validation error vs runtime zero-match. **ROUND 1: partial — mount-vs-runtime distinction covered at the validation layer (hard errors vs registered query returning no rows for a non-matching filter is the zero-match path; e.g. the non-matching subscriber in the backend's eviction test receives nothing). Deep-payload arg legs not exercised live this round.**
- [ ] E-2788-5 Eviction race: a patch lands for a key in the same window the retention sweep evicts it — observe the final state (remove wins / patch re-inserts); no crash, no resurrected stale row, `seq` stays monotonic. **ROUND 1: NOT EXERCISED (prune observation pending — see FM-29).**
- [ ] E-2788-6 Restart mid-flush: kill the app during a write-behind batch — relaunch, verify SQLite integrity (WAL recovery), no partially merged row visible via replay, `seq` continuity intact. **ROUND 1: NOT TARGETED — two app restarts occurred with the DB healthy afterwards (WAL intact, rows consistent, seqs continuous across restarts via MAX(seed)), but no deliberately-timed mid-batch kill.**
- [ ] E-2788-7 (PROMOTED round 1 → FM-33) MM mount on a large canonical corpus wedges the webview (replay flood; 3/3 reproductions, 25+ min no recovery). Details in FM-33. **ROUND 3: mount criterion PASS on the async-drain fix (8a521fc) — see FM-33 round-3 receipt for the 4-mount timeline.**

---

## RTDB migration (#2788) — round 3 findings

- [ ] E-2788-r3-1 (NEW, round 3) Live-path renderer saturation with MM open on a ~42k-row corpus under sustained agent traffic: with Mission Monitor mounted, every live row-mutation batch bumps the epoch → a full O(rows) derivation + 360-row sidebar re-render. Under a continuously streaming agent session (the tester's own opencode drive), the webview JS thread enters repeated full-core spins (18-19 CPU-s per 15s wall, ~120% of one core); during bursts, heavy webview operations (screenshots, DOM snapshots, console reads) time out while the main thread stays responsive (fredo.exe Responding:True throughout — the "Not Responding" signature did NOT return) and the backend stays healthy. Observed on boots 2/3/4/5; bursts align 1:1 with live-traffic density (idle when the agent sleeps, spin when it streams). NOT a mount-freeze regression (mounts are clean — FM-33 round 3) and NOT blocking any AC (the settled UI is correct; interactive probes slip through between bursts), but it bounds MM's interactive smoothness on large corpora during long missions. Evidence: CPU timelines in `.opencode/tmp/2788/` (cpu-monitor output in the round-3 verdict), tool-timeout signatures (bridge 2s request timeouts during bursts). Candidate directions for a future spec (NOT this one): incremental derivation keyed by touched sessions, sidebar virtualization, or derivation memoization per selected session. **ROUND 4 (deletion build @ 12be946, 45.4k rows): RESIDUAL PERSISTS — SEVERE under an actively-streaming agent, now documented as the ARCHITECTURE.md known-limitation note.** The P6.1 note landed in the RTDB section per the round-4 disposition (adopt (b): document, no mitigation in-spec). Observed: MM mount drains bounded (108s/185s, recovered, main thread healthy — FM-33 ROUND 4); session-selection spins ~5/~5/~11 min; ONE non-recovering ≥13-min renderer wedge (boot 1) requiring the full G-046 Down→Up recovery — bridge alive (2s request timeouts, no WS_SERVER errors), backend healthy, JS thread never yielded. Amplifier identified: the round-4 tester's own continuous OTLP traffic (every shell command lands tool_use rows) keeps per-batch derivations re-triggering during any pause-laden verification loop — bursts no longer align with "idle when the agent sleeps" because the agent never sleeps during a verification round. All state recovered after restarts; zero console errors; zero data loss.

**ROUND 5 (spec/2788 @ f8add9a, 47.8k rows): RESIDUAL PERSISTS — consistent with the softened note.** Two boots this round, both with the tester's own opencode session streaming continuously (1,682 spans/5min during bursts): boot 1 — the dispatch-era instance was found WEBVIEW-WEDGED before any MM open (all JS evals timing out, Rust healthy, idle instance) — a wedge WITHOUT the streaming aggravator, never recovered over ~2 min → G-046 Down→Up (new data point: the wedge class is not strictly MM/traffic-gated); boot 2 — MM mount drain COMPLETED bounded (~10 min under near-zero traffic, no restart; full render — FM-33 ROUND 5), then interactive use under the continuously-streaming session re-saturated the webview for multi-minute stretches (heavy evals/DOM/screenshots time out; 1+1 slipped through between bursts; backend healthy throughout; closing MM unblocked the renderer instantly). The softened ARCHITECTURE.md:114 note ("Drains typically recover, but at least one ≥13-minute non-recovering webview wedge was observed under an actively-streaming agent") now matches observed reality on BOTH halves: drains did typically recover this round, and the non-recovering wedge class reproduced (boot 1's found-wedged instance + round 4's). Non-blocking, documented; candidate directions unchanged.
- [ ] E-2788-r5-1 (NEW, round 5) Dev Mode stream viewer pins at the mutation log's 512-entry cap: the FM-34 fix (version-driven recompute) works below the cap, but the retained accumulator (useDevModeStream.ts:102-110) uses seenCount-offset bookkeeping that breaks at the cap — ROW_MUTATION_LOG_CAP = 512 (StreamContext.tsx:153), eviction splice(0, length-512) (:172-173) pins length at 512 while shifting contents, so start = min(seenCount=512, length=512) = 512 and the loop for(i=512; i<512) never re-examines the array: the newest entry (shifted into index 511 by the eviction) is NEVER consumed. Expected: the viewer tracks post-cap mutations too (the id-set already de-dupes — the seenCount fast-path is the only thing that breaks). LIVE-proven on boot 2 (f8add9a): two stepper emits applied to the row store (Stepper Probe replayed rows 2→3, row-store epoch 42→43) while the viewer stayed pinned at exactly "512 events" (newest rendered row #512 @ 23:39:56 — a drain mutation predating both emits). Same visible symptom class as FM-34 but only AT the cap; viewer-only (debug tool): store semantics, epoch, MM derivation, replay all unaffected. NOT blocking any AC (FM-34's round-4 failing scenario is fixed and verified live; the Fix Plan retained the accumulator as byte-identical). Fix direction: when version changes and mutations.length === CAP, re-scan backwards from the end by id instead of trusting the seenCount offset. Evidence: r5-devmode-512-pinned.jpeg + r5-stepper-epoch43.jpeg (uploaded to the round-5 verdict).
