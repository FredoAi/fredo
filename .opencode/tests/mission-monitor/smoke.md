# Mission Monitor smoke tests

- [ ] S-2766 app/feature: Dev instance serves the required spec commit, Mission Monitor opens, and the Run CLI window launches only after Mission Monitor is mounted.
- [ ] S-2766 console: Read the webview console before and after each streaming fixture; no Error, Uncaught, or Maximum update depth exceeded.
- [ ] S-2766 live receipt: Every live visual fixture has a marker-resolved provider session id and a telemetry_spans tree matching the rendered node set 1:1.
- [x] S-2766 app/feature — exercised round 2 (#2768): instance served spec/2768 @ the origin tip; MM open before each Run CLI drive (strict order).
- [x] S-2766 console — exercised round 2: zero Error/Uncaught/duplicate-key entries across all interactions (only pre-round 00:09 UTC entries in the buffer, prior era).
- [x] S-2766 live receipt — exercised round 2: R2A (`ses_faf58aae4ffeA5TEEA31QbFMni`, 3 nodes/2 edges) and R2B (`ses_faf4b16cbffefWVYwzXt1GTqwO`, 2 nodes/1 edge) trees matched rendered nodes 1:1.
- [x] S-2770 fixture doctrine (G-080): **PASS round 1** — real Run CLI drives only, Muse Spark 1.2 Free selected via /models before the first prompt (footer receipt: `Build · Muse Spark 1.2 Free OpenCode Zen`), minimal trees, MM open before every launch (`#window-mission-monitor` verified pre-launch — ECE contracts registered).
- [x] S-2770 marker receipts (G-076): **PASS round 1** — FIXA first prompt embeds `fredo-2770-FIXA-20260830` (root `ses_fabe4ecffffeaWF2HQftbvwijI`), FIXB embeds `fredo-2770-FIXB-20260830` (root `ses_fabcf1e07ffeDpHMeSWXayFT8R`); both roots resolved by marker query and MM titles the sessions by the first prompt. Markers never reach children (children joined via parent task-span attributes).
- [x] S-2770 windows-only waits (G-083): **PASS round 1** — leaf prompts used PowerShell `Start-Sleep -Seconds 3`; no POSIX sleep anywhere.
- [x] S-2770 completion gating (G-073) + one-fixture-per-launch (G-074): **PASS round 1** — both fixtures store-gated (idle record + completed session spans for EVERY session in each tree before any DOM assertion); terminal closed and relaunched between FIXA and FIXB.
- [x] S-2770 NEW (round 1 recipe finding, promoted from FM-15/FM-20 experience): **Nested-delegation drive recipe via pipeline agents.** Prompt-only nested dispatch is UNRELIABLE on Muse Spark 1.2 Free (4/4 failures — children run the leaf work themselves). The reliable recipe: root calls task with `subagent_type self-improver` (task prompt: "telemetry fixture, not pipeline work; do NOT run pipeline-state; call task once with subagent_type developer, prompt: reply DONE-LEAF") — the mid's dedicated system prompt makes it comply (verified: `ses_fabd6d81bffe…` → `ses_fabd6bbd9ffe…`, depth 2). Depth 3 remains impossible without restoring #2762-style fixture agents in opencode.json (see FM-20 blocker).
