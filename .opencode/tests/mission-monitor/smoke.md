# Mission Monitor smoke tests

- [ ] S-2766 app/feature: Dev instance serves the required spec commit, Mission Monitor opens, and the Run CLI window launches only after Mission Monitor is mounted. **ROUND 4: PASS (deletion build).** Dev-env Up ×2 verified fail-closed on `spec/2788 @ 12be946` (origin tip, tree clean); desktop launcher rendered (Settings/Mission Monitor/Query Viewer/Run CLI/Stepper Probe); MM opened on both boots; no Run CLI launch needed this round (no live fixture drive — the round-3 marker session's persisted corpus served as the live graph source).
- [ ] S-2766 console: Read the webview console before and after each streaming fixture; no Error, Uncaught, or Maximum update depth exceeded. **ROUND 4: PASS** — error-level console read returned ZERO entries across both boots and all interactions (see S-2788-3 ROUND 4).
- [ ] S-2766 live receipt: Every live visual fixture has a marker-resolved provider session id and a telemetry_spans tree matching the rendered node set 1:1. **ROUND 4: PASS (no new fixture — persisted-corpus receipt).** Round-3 marker `fredo-2788-AC1R3-0901` re-resolved via telemetry_spans (root `ses_fa0cda737ffeQMlQvpvHodBOkw`, child `ses_fa0cd74e1ffeF9nYHkT1Mc5myg`, task span 22:59:03→23:05:48Z = 405s) and its restored graph rendered 2 nodes / 1 edge — agent node + SubagentNode `Subagent · general 6m 45s` — matching the telemetry tree 1:1 on the deletion build.
- [x] S-2766 app/feature — exercised round 2 (#2768): instance served spec/2768 @ the origin tip; MM open before each Run CLI drive (strict order).
- [x] S-2766 console — exercised round 2: zero Error/Uncaught/duplicate-key entries across all interactions (only pre-round 00:09 UTC entries in the buffer, prior era).
- [x] S-2766 live receipt — exercised round 2: R2A (`ses_faf58aae4ffeA5TEEA31QbFMni`, 3 nodes/2 edges) and R2B (`ses_faf4b16cbffefWVYwzXt1GTqwO`, 2 nodes/1 edge) trees matched rendered nodes 1:1.
- [ ] S-2770 fixture doctrine (G-080): fixtures are real Run CLI drives at MINIMAL viable volume on a FREE model (select Muse Spark 1.2 Free — never leave the default paid model selected); Mission Monitor mounted BEFORE any fixture launch (ECE contracts register only at mount — a fixture launched first delivers into unregistered contracts).
- [ ] S-2770 marker receipts (G-076): fixture A's first user prompt embeds `fredo-2770-FIXA-<date>`; fixture B's embeds `fredo-2770-FIXB-<date>`; resolve the real (provider-minted) session id by querying spans for the marker — never key receipts on invented labels; markers never reach child sessions (join via the parent's task-span child-session attributes).
- [ ] S-2770 windows-only waits (G-083): fixture prompts use PowerShell `Start-Sleep -Seconds 3` in leaf tasks to keep spans observable — never POSIX `sleep`.
- [ ] S-2770 completion gating (G-073) + one-fixture-per-launch (G-074): a fixture is COMPLETED only when the telemetry store has its idle record and a completed session span (query via `.opencode/skills/telemetry-query/telemetry-query.ps1`), never the PTY done-marker alone; between fixtures close the terminal and relaunch — a prompt submitted mid-turn queues into the same conversation and contaminates the session; assert DOM only on COMPLETED sessions whose telemetry agrees at the same instant.
- [ ] S-2770 round-3 recipe finding (promoted 2026-08-30, FM-20): driving a depth-3 delegation tree (root→l2→l3→l4) with the current opencode plugin POISONS the root's llm-span attribution — the plugin stamps `session.parent_id` (a grandchild session id) on the PRIMARY root's `fredo.llm` spans during concurrent 3-level delegation (2/2 depth-3 drives: FIXB3 2/4 spans, FIXB3b 2/2; the round-2 depth-2 root was clean 0/3; a full G-046 Down→Up did NOT clear it). The adapter's `is_subagent_session` then flags the root as subagent and Mission Monitor's excludePayload contracts drop the root's chat/tool events pre-buffer → the session renders nothing and never appears in the sidebar. Before ANY depth-3 live drive: query the root's llm spans for `"session.parent_id"` presence EARLY (first 2 llm spans after the marker resolves); if contaminated, STOP — the drive cannot produce rendered evidence; report the plugin defect instead of re-driving.
- [ ] S-2770-R6 real-path depth-3 smoke (round 6, ST-5): ONE Run CLI launch, marker `fredo-2770-L3REAL-<date>` in the FIRST user prompt, chain root (`build`, primary) → product-owner → self-improver → developer (leaf runs a permitted bash wait — NO configured real agent allows `Start-Sleep` since fixture agents were removed; use the G-075 authorized equivalent, e.g. `node -e setTimeout(()=>{},3000)`). Preflight order: dev instance on spec tip → Mission Monitor mounted → Run CLI launch → free model via /models (root model only; subagent models are config-pinned) → submit. Gates: EARLY root-attribution probe (0/N root llm spans stamped — FM-20), completion via `telemetry_spans` (4/4 `fredo.session` OK+ended, 0 open spans). Receipts: task-span chain child_session_id == dispatcher ids; child `session.parent_id` == dispatcher; `feature_mission_monitor_events` rows for the L3 session id under composite-parent keys (L2/L1/root); root re-key copy PRESERVES the inner `compositedChildSessionId` stamp (ST-3). DOM: 1 agent node + 3 subagent cards, L3 compact (`[L3]` chip, single summary line `N tools · M nested · K tok`, aria `Subagent (nested) · <name> · level 3`), staircase Δy=64/level in layout space, 0 duplicate node/edge ids. EXERCISED round 6 (2026-08-31, marker `fredo-2770-L3REAL-0831`, root `ses_fa8b7aecaffeKZxFowCV8vhvgR`): ALL gates and receipts green — see the round-6 `## Tests Runs`.

---

## RTDB migration (#2788) — smoke

- [ ] S-2788-1 (#2788) RTDB boots: app starts clean with the canonical tier active; Mission Monitor mount registers its query subscriptions (post-Phase-4 the first delivery arrives without the v1 contract path). **ROUND 1: PASS (boot half).** Canonical tier active at boot: `chat_rows`/`tool_use_rows`/`agent_session_rows` in fredo.db with the spec'd DDL; one-shot marker `rtdb.backfill.completed = 2026-09-01T16:03:00Z` (startup backfill completed, log receipt `spans_read=15450 … skipped_malformed=0`); retention knobs seeded (`rtdb.retention_days`=7, `rtdb.max_rows`=100000). MM-mount half blocked by F-33. **ROUND 3: PASS (both halves).** Four boots (corpus 41.7k canonical rows) all started clean; MM mounted and registered its subscriptions on every boot — first delivery ~2ms after subscribe return (async command), terminal markers received, sidebar 360+ rows repopulated from replay on every boot. **ROUND 4: PASS (both halves, deletion build @ 12be946).** 2 fresh boots (corpus 45,432 canonical rows: 20,368 chat + 24,157 tool_use + 907 agent_session); knobs + one-shot backfill marker unchanged; MM mounted and registered subscriptions on both boots; sidebar restored 366 rows (boot 1) / 369 rows (boot 2) from replay.
- [ ] S-2788-2 (#2788 AC4) Subscribe/emit sanity: a trivial valid query returns rows after one `fredo emit` event; the telemetry store receives the span (telemetry-query receipt). MOCK-VS-REAL: verify extraction paths against `telemetry_spans` before trusting mock-derived rows. **ROUND 1: PASS.** Valid query + replay returned the emit-created row (retention-1 insert delivery at subscribe); emit flows land rows (see FM-27); extraction verified against real span shapes (F5) per the mock-vs-real rule. **ROUND 4: PASS (deletion build).** The full SKILL.md flow set (chat init→response via `--file`, tool_use pair, agent_session pair, stepper probe, parse regression) landed typed rows with correct extraction: chat `e2e-r4-chat-1|e2e-r4-chat-1_1` seq 1→2 state=response, KeepFirst userMessage, tokens 10/20 from the file payload; agent_session camelCase `totalTokens` → typed columns 4321/7/`opencode` (matches the pinned mock-shape test `ingest_event_maps_cli_mock_payloads_to_rows`, ingest.rs:1955); Stepper Probe counters 0→1 + epoch 1→2 live; MM rendered the node (USER → RESPONSE → token bar 10/0/0/20/30). See FM-32 for the Recipe-3 doc-shape finding.
- [ ] S-2788-3 (#2788) Console clean after ingest cutover: no Error / Uncaught / Maximum update depth exceeded during an emit flow and a live drive. **ROUND 1: PARTIAL — backend logs clean across all legs (no ERROR entries, receiver/prune/backfill healthy); the webview console is unreadable during the F-33 wedge (MM open). Console reads on the desktop were healthy before the first MM open.** **ROUND 3: PASS.** Console read at error level returned ZERO entries on boot 5 after the full round's legs (emit flows, live drive, close/reopen ×2, restarts, prune) — no Error / Uncaught / Maximum update depth exceeded on the row path. **ROUND 4: PASS (deletion build, 2 boots).** Console error-level read returned ZERO entries after the full round's legs (2 boots, MM mounts, ~10 emit flows, session selections, window opens, replay drains, DevMode/Konami). Backend telemetry_logs carried zero rtdb/ingest/flush ERROR entries (only `fredo::otlp` tool_result records of the agents' own failed tool calls, pre-existing class).
- [ ] S-2788-4 (#2788) Fixture doctrine carried: MM mounted before fixture launch, free model (G-080), marker receipts (G-076), PowerShell waits (G-083), completion gating (G-073), one fixture per launch (G-074) — the ordering reason becomes query-subscription registration at mount (replaces the ECE-contract rationale in S-2770). **ROUND 1: doctrine not exercised live — no fixture drive (F-33). Observed deviation for future rounds: MM mount is currently the wedge trigger, so "MM mounted BEFORE fixture launch" cannot be satisfied safely until F-33 is fixed.** **ROUND 3: PASS — doctrine fully exercised.** The AC1R3-0901 drive: MM mounted BEFORE Run CLI launch; free model (Muse Spark 1.2 Free via /models); marker in the FIRST user prompt resolved the real session ids via telemetry_spans; completion gated on the store (2/2 `fredo.session` OK+ended), not the PTY marker; one fixture per launch; node waits used `node -e "setTimeout(...)"` (G-083 windows-safe pattern).
- [ ] S-2788-5 (#2788 Phase 5) Post-deletion smoke: re-run S-2766 rows + S-2788-1..3 with v1 components deleted; zero-warning builds; telemetry-query skill intact. **ROUND 1: PENDING (Phase 5 deliberately gated on this round's verdict).** **ROUND 4: PASS WITH 1 REGRESSION FINDING (spec/2788 @ 12be946, 2 fresh boots).** Deletion verified live (grep receipts zero production v1 references; contract module/opencode.rs/adapters-otlp.rs + 7 FE files gone; EventBus reduced to `emit_row_delivery_batch`; CLI nits b/c fixed); zero-warning builds per the dev's round-4 receipts (`cargo check` clean, `cargo test rtdb` 173/173, `pnpm build` clean, vitest 669/669, goldens unmodified — the running binary IS the build); telemetry-query skill intact (18-row re-hash EXACT, marker session identical); S-2766 rows + S-2788-1..3 re-run green (see their ROUND 4 receipts). **REGRESSION FOUND: Dev Mode's row-mutation stream is frozen at its mount-time snapshot (E-2788-r4-1/FM-34) — post-mount mutations never render (live-proven: 6+ store mutations applied while Dev Mode stayed at "1 event").** Orphan observation: the `contract_events` SQLite table remains in fredo.db as historical data (zero code references — data artifact, not a code-deletion gap).
