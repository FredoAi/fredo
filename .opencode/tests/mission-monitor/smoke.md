# Mission Monitor smoke tests

- [ ] S-2766 app/feature: Dev instance serves the required spec commit, Mission Monitor opens, and the Run CLI window launches only after Mission Monitor is mounted.
- [ ] S-2766 console: Read the webview console before and after each streaming fixture; no Error, Uncaught, or Maximum update depth exceeded.
- [ ] S-2766 live receipt: Every live visual fixture has a marker-resolved provider session id and a telemetry_spans tree matching the rendered node set 1:1.
- [x] S-2766 app/feature — exercised round 2 (#2768): instance served spec/2768 @ the origin tip; MM open before each Run CLI drive (strict order).
- [x] S-2766 console — exercised round 2: zero Error/Uncaught/duplicate-key entries across all interactions (only pre-round 00:09 UTC entries in the buffer, prior era).
- [x] S-2766 live receipt — exercised round 2: R2A (`ses_faf58aae4ffeA5TEEA31QbFMni`, 3 nodes/2 edges) and R2B (`ses_faf4b16cbffefWVYwzXt1GTqwO`, 2 nodes/1 edge) trees matched rendered nodes 1:1.
- [x] S-2770 fixture doctrine (G-080): fixtures are real Run CLI drives at MINIMAL viable volume on a FREE model (select Muse Spark 1.2 Free — never leave the default paid model selected); Mission Monitor mounted BEFORE any fixture launch (ECE contracts register only at mount — a fixture launched first delivers into unregistered contracts). Exercised round 5 (FIXB5): MM mounted pre-launch, `/models` → `Muse Spark 1.2 Free` selected via PTY before the first prompt, 15/15 llm spans `muse-spark-1.2-contributor-free`, Σ cost $0, minimal 2-branch tree as instructed.
- [x] S-2770 marker receipts (G-076): fixture A's first user prompt embeds `fredo-2770-FIXA-<date>`; fixture B's embeds `fredo-2770-FIXB-<date>`; resolve the real (provider-minted) session id by querying spans for the marker — never key receipts on invented labels; markers never reach child sessions (join via the parent's task-span child-session attributes). Exercised round 5 (FIXB5): marker `fredo-2770-FIXB5-2026-08-30` in the FIRST user prompt; session row titled with the marker. CAVEAT learned: the free model relays the marker text into its task dispatch instructions, so the marker ALSO matches CHILD session spans — root resolution must additionally require `agent: "build"` (no `session.parent_id`), not marker-match alone (naive marker query picked a fixture-l2 child first).
- [x] S-2770 windows-only waits (G-083): fixture prompts use PowerShell `Start-Sleep -Seconds 3` in leaf tasks to keep spans observable — never POSIX `sleep`. Exercised round 5: 2/2 leaf `fredo.tool.bash` spans 3219ms/3230ms.
- [x] S-2770 completion gating (G-073) + one-fixture-per-launch (G-074): a fixture is COMPLETED only when the telemetry store has its idle record and a completed session span (query via `.opencode/skills/telemetry-query/telemetry-query.ps1`), never the PTY done-marker alone; between fixtures close the terminal and relaunch — a prompt submitted mid-turn queues into the same conversation and contaminates the session; assert DOM only on COMPLETED sessions whose telemetry agrees at the same instant. Exercised round 5 (FIXB5): single launch, single prompt, no queued input; DOM asserted only after 7/7 `fredo.session` OK+ended + 0 open spans store query.
- [ ] S-2770 round-3 recipe finding (promoted 2026-08-30, FM-20): driving a depth-3 delegation tree (root→l2→l3→l4) with the current opencode plugin POISONS the root's llm-span attribution — the plugin stamps `session.parent_id` (a grandchild session id) on the PRIMARY root's `fredo.llm` spans during concurrent 3-level delegation (2/2 depth-3 drives: FIXB3 2/4 spans, FIXB3b 2/2; the round-2 depth-2 root was clean 0/3; a full G-046 Down→Up did NOT clear it). The adapter's `is_subagent_session` then flags the root as subagent and Mission Monitor's excludePayload contracts drop the root's chat/tool events pre-buffer → the session renders nothing and never appears in the sidebar. Before ANY depth-3 live drive: query the root's llm spans for `"session.parent_id"` presence EARLY (first 2 llm spans after the marker resolves); if contaminated, STOP — the drive cannot produce rendered evidence; report the plugin defect instead of re-driving. Round 4 (FIXB4, plugin fix 2846283 temporal-origin guard): the probe ran as prescribed (first root llm spans landed, mid-drive) — CLEAN, and stayed clean through completion: root `ses_faa339a39ffeLs1UZ50whYhZMK` 0/15 llm/tool spans carry `session.parent_id`; all 21 children's stamped parent == task dispatcher (21/21 MATCH). The probe is now the standard early gate for every depth-≥3 drive. Round 5 (FIXB5, fresh boot on 0bea948): gate ran EARLY (root's first 2 llm spans landed mid-drive) — root `ses_fa9f3a8c9ffevzBu0q2H4VMCNy` (agent build) CLEAN, 0/3 llm spans stamped `session.parent_id`; 12/12 child llm spans stamped with parent == task dispatcher (6/6 exact-match). NOTE: a naive marker-LIKE query also matches child sessions whose task instructions relay the marker text — always resolve the root by `agent: "build"` + null parent, then run the gate on THAT id.
