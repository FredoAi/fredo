# app-dock — Smoke

- [x] S-1: App window renders — `tauri_webview_dom_snapshot(type="structure")` returns a non-empty `<body>` (**PASS** — DOM snapshots throughout the rounds returned non-empty element sets; the Fredo desktop rendered FREDO logo band, clock, settings, launcher)
- [x] S-2: No console errors — `tauri_read_logs(source="console", lines=50)` shows no `Error:`/`Uncaught`/`Maximum update depth exceeded` (**PASS** — console read after every leg across rounds 1 & 2; zero of the three signatures)
- [x] S-3: Dock surface reachable — open ≥1 window (launcher grid / desktop toolbar); the dock renders its open-app entries (Sidebar default: left-edge rail `[data-testid="app-dock"]` / `role="region" aria-label="Open applications"` → `list` → `listitem` per open app) (**PASS** — opening Mission Monitor/Query Viewer rendered the dock region → list → listitem with per-window `aria-label` entries; empty-gate verified: 0 windows → no dock)
- [x] S-4: Telemetry Settings accessible — gear/nav opens the settings dialog with sections visible (**PASS** — `button[aria-label="Settings"]` opens ProfileSettingsModal; nav sections Companion/Appearance/Fredo Setup/Telemetry visible)
- [x] S-5: Screenshot captured — `tauri_webview_screenshot(format="jpeg", quality=80, filePath=".opencode/tmp/<issue>/e2e/smoke.jpeg")` succeeds (**PASS** — screenshots saved to `.opencode/tmp/2848/e2e/` throughout, e.g. `F4-sidebar-resting-visible.png`, `F6a-bottom-reveal-maximized.png`, `F3-boot-restart-bottom.png`)
- [x] S-6: Settings → Appearance shows the Dock position field — open Settings (`[aria-label="Settings"]`) → Appearance section; a **Dock position** control renders with **Sidebar** and **Bottom bar** options, default **Sidebar** on an existing install (**PASS, round 1 spec/2848 @ 12afa697** — `chakra.select[aria-label="Dock position"]` with options `Sidebar (left edge)`[selected]/`Bottom bar (bottom-center)`, rendered beneath ThemingSettings in ProfileSettingsModal's Appearance content; **re-confirmed round 2 spec/2848 @ cde149d9** — Dock position select read `value:"bottom"`, options Sidebar/Bottom, token-styled)
