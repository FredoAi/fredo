# app-dock — Functional

Scope: the positionable open-apps dock surface — **Sidebar** (left-edge vertical rail) vs **Bottom bar** (bottom-center horizontal pill), selectable under Settings → Appearance — extending the #2838/#2841 left-edge dock (resting-visible on a clean desktop, edge-peek overlay reveal when a window covers the desktop, hover/focus ✕ close). Seeded from issue #2848; map 1:1 to `.opencode/tmp/2848/triage.md` `## QA Expert` (QA Plan F-1..F-12, F-6a/F-6b split).

> **Verification policy: live** — pure-rendering UI spec (position choice, edge-summon reveal/hide, hover ✕, persistence); ACs provable ONLY by observing the running artifact (`pnpm dev:tauri` serving `spec/2848`, MCP driver `com.fredo.app:9223`). DOM snapshots, `getBoundingClientRect`/computed-style probes, real `webview_interact` pointer/keyboard drives, and `tauri_webview_screenshot` receipts per orientation. NO telemetry surface — the live-gate reference is the established desktop-shell F-14 / desktop-chrome F-23 pattern: `SELECT COUNT(*) FROM telemetry_spans` non-zero + recent `max(ingested_at)` at round start (the dock is row-INDEPENDENT — entries come from the `useWindows()` store, opened via the launcher grid). A static-only PASS is a FALSE PASS. A console `Error:`/`Uncaught`/`Maximum update depth exceeded` on any leg invalidates that leg's evidence.

- [x] F-1 (AC1 — setting surface): Open Settings (chrome `[aria-label="Settings"]`, clean desktop) → Appearance section; locate the **Dock position** control. **Expected:** a **Dock position** field renders in the Appearance section (the auto-discovered appearance content — `ThemingSettings`/the Architect's chosen surface in ProfileSettingsModal) offering exactly **Sidebar** and **Bottom bar** options; the default reads **Sidebar** on an existing install; label/options match the backlog wording. **PASS (round 1, spec/2848 @ 12afa697):** Settings → Appearance shows a `Dock` section with a `chakra.select[aria-label="Dock position"]` (tag `SELECT`, token-styled `bg:var(--card-bg)`/`borderColor:var(--border-color)`/`color:var(--text-primary)` — NOT a `NativeSelect`), options exactly `Sidebar (left edge)` [default, selected] + `Bottom bar (bottom-center)`, with helper text. Rendered BENEATH `<ThemingSettings/>` in `ProfileSettingsModal.tsx` (stdout `ProfileSettingsModal.tsx:213-221`), never in the legacy tabbed `SettingsPanel`. **Re-confirmed round 2 (spec/2848 @ cde149d9):** post-boot Settings→Appearance Dock position select read `value:"bottom"`, options `Sidebar (left edge)`/`Bottom bar (bottom-center)`, `bg:rgb(24,11,51)`/`border:rgb(58,30,94)`/`color:rgb(245,230,255)` (token-styled, NOT a NativeSelect).
  - **Edge:** the Settings modal must be CLOSED before any dock geometry/reveal probe (an open Chakra dialog z:1500 masks dock hits — #2841 F-18 precedent); the field is a `chakra.select` styled via theme CSS vars, NOT a `NativeSelect` (unstyled browser default = FAIL); the control lives in ProfileSettingsModal, never the legacy tabbed `SettingsPanel`.
- [x] F-2 (AC1 — immediate move, both directions): With ≥1 window open (mix minimized + floating) on a clean desktop, select **Sidebar** → observe; select **Bottom bar** → observe; select back. **Expected:** the dock relocates IMMEDIATELY on selection WITHOUT an app restart: Sidebar = left-edge vertical rail (resting-visible, x≈0); Bottom bar = bottom-center horizontal dock (`position:fixed`, bottom-anchored, horizontally centered); the SAME open-app entries render in both; NO leftover dock element at the previously-chosen edge (exactly ONE dock in the DOM). **PASS (round 1, spec/2848 @ 12afa697):** with Sessions/Query Viewer/Stepper Probe open, `setDockPosition('bottom')` → dock bottom-center `{x:935,y:953,w:50,h:52}` `flexDir:row` `translate(-50%,0)`; `setDockPosition('sidebar')` → dock left rail `{x:-60(when covered)/0(clean),w:52}` `flexDir:column`; `regionCount:1` (exactly ONE dock, no stale at the old edge). Same entries render in both. Rapid Sidebar↔Bottom↔Sidebar flips → no stale/duplicate dock, no `Maximum update depth exceeded` (console clean). **Re-confirmed round 2 (spec/2848 @ cde149d9):** select `sidebar` → `{x:0,w:52} flexDir:column`; select `bottom` → `{x:935,y:953,w:50,h:52} flexDir:row`; `dockCount:1` (exactly ONE dock, no stale element).
  - **Edge:** rapid toggle Sidebar→Bottom bar→Sidebar (no stale/duplicate dock, no `Maximum update depth exceeded`); selection while 0 windows open (dock absent — the setting still persists); narrow viewport (bottom bar not clipped/off-screen).
- [x] F-3 (AC1 — persistence): Select **Bottom bar**; FULLY restart the app (kill + relaunch `pnpm dev:tauri`); reopen with ≥1 window on a clean desktop. **Expected:** the dock renders at the LAST-chosen edge (bottom bar) after the full restart — the default (Sidebar) must NOT re-assert. Setting persisted via the app settings store (FeatureStore/AppStore → `Fredo_dock_position`), NOT a `useRef`/component flag. **PASS (round 2, retry; round-1 FAIL resolved):** `Fredo_dock_position=bottom` persisted in AppStore SQLite `settings` + localStorage mirror; a FULL `dev-env Down → Up -Spec 2848` restart + reopen ≥1 window (Mission Monitor) rendered the dock at the BOTTOM bar WITHOUT opening Settings (round-1 FAIL — the persisted value was ignored at boot because `hydrateDockPosition()` was only invoked in the Settings→Appearance mount; the FD-1 boot-hydration fix added a mount-only `hydrateDockPosition()` in AppDock): boot-rendered dock `bottom:12px, left:960px(50%), w:50, h:52, flexDir:row`, resting-visible pill on the clean desktop. Verified Settings→Appearance after boot reads Dock position = `bottom` (no double-hydrate/glitch, `docks:1`). Switch to `sidebar` + another full restart → dock boot-hydrates to SIDEBAR (`x:0, w:52, flexDir:column`) — the default/positional path both honored at boot. Console clean (ZERO `Error:`/`Maximum update depth exceeded`). Evidence: `https://github.com/FredoAi/fredo/raw/spec/2848/.opencode/evidence/2848/F3-boot-restart-bottom.png`, `https://github.com/FredoAi/fredo/raw/spec/2848/.opencode/evidence/2848/F3-boot-restart-bottom-resting.png`, `https://github.com/FredoAi/fredo/raw/spec/2848/.opencode/evidence/2848/F3-sidebar-default-resting.png`.
  - **Edge:** persistence for **Sidebar** when chosen explicitly; unknown/corrupt persisted value (bogus enum) degrades to **Sidebar** without crashing (graceful default, console clean); the persisted key is readable in the store (FeatureStore/AppStore query) after restart.
- [x] F-4 (AC2 — resting-visible, both orientations): All windows minimized / none showing → clean desktop (`coveredByWindow === false`); with the pointer AT REST (no edge gesture), snapshot + `getBoundingClientRect` the dock per orientation. **Expected:** Sidebar — dock `visibility:visible`, `pointer-events:auto`, resting transform `translate(0px,-50%)` at the left edge (x≈0, NOT off-canvas `-100%`), entry list visible. Bottom bar — dock visible, bottom-anchored + horizontally centered, NOT off-canvas (no `translateY` hide). Stays visible until covered or the position changes. **PASS (round 1, spec/2848 @ 12afa697):** Sidebar clean-desktop: `visibility:visible`,`pointer-events:auto`,`transform:matrix(1,0,0,1,0,-71)`,`x:0`,`flexDir:column`. Bottom bar clean-desktop: `visibility:visible`,`transform:matrix(1,0,0,1,-25,0)` `{x:935,y:953,w:50,h:52}` bottom-anchored+centered. Both orientations rest VISIBLE with no pointer gesture. **Re-confirmed round 2 (spec/2848 @ cde149d9):** Sidebar `visibility:visible`,`pointer-events:auto`,`x:0`,`translateY(-50%)`; Bottom `visibility:visible`,`translateX(-50%)`,`{x:935,y:953}`.
  - **Edge:** 0 windows → NO dock at either edge (empty gate holds in both orientations — `dockPresent:false`, no listener, no ghost); all-minimized vs all-closed transitions; light + dark presets; narrow viewport (bottom bar fits, not clipped).
- [x] F-5 (AC2 — entry parity + empty gate, both orientations): Compare dock entries to the `useWindows()` store set per orientation; close the last window. **Expected:** the dock lists every open window exactly once in store order (same icons/entries in BOTH orientations — entries carry the SAME `aria-label` app title + state); closing the last window removes the dock entirely (no empty rail/pill, no ghost element). **PASS (round 1, spec/2848 @ 12afa697):** 3 open → `[Sessions (minimized), Query Viewer (background), Stepper Probe (active)]` (Stepper `aria-current=step`); after closing Stepper Probe → `[Sessions (background), Query Viewer (active)]`; closing the last (Sessions) → `dockPresent:false`,`dockTestId:false` (empty gate, no ghost). Entry set == window-store set at every settle. Empty gate holds in BOTH orientations.
  - **Edge:** minimized + floating mix; same-feature double-open (one entry per feature id); entry set == store set after every settle (no stale/ghost entries in either orientation).
- [x] F-6a (AC3 — reveal, Sidebar): Maximize a window (covers the left edge, full-bleed). With the dock at rest (hidden, off-canvas), move the pointer to the far LEFT edge (`clientX ≤ 6` reveal zone, mid-height) — real pointer + document-`pointermove` dispatch. **Expected:** the dock slides IN as an OVERLAY on top of the maximized window (`zIndex:1200`, above the window stack): computed `visibility:visible`, `pointer-events:auto`, x≈0 (not off-canvas), showing its open-app entries over the window content. The window stays full-bleed (NO content re-indent). **PASS (round 1, spec/2848 @ 12afa697):** dispatch `pointermove(clientX:0)` with Sessions maximized → dock `visibility:visible`,`pointer-events:auto`,`transform:matrix(1,0,0,1,0,-71)`,`x:0`,`zIndex:1200` (over the window stack z=1), window `fullBleed:true` (x:0, w:1920, NO re-indent). Boundary: `clientX=6` reveals, `clientX=7` does not (correct ≤6 reveal zone). 6 reveal/hide cycles clean. **Re-confirmed round 2 (spec/2848 @ cde149d9):** bottom-edge pointermove (`clientY≥viewportH−2`) → dock `visibility:visible`,`zIndex:1200` over the maximized window; left-edge (`clientX:2`) → `x:0`,`zIndex:1200`; window full-bleed (viewport 1920×1017, no re-indent); reveal→hide→re-reveal cycle clean.
  - **Edge:** `clientX=6` boundary (≤6 reveals, >6 does not); reveal fires on edge ENTRY (no dwell requirement); pointer resting INSIDE the dock keeps it open (no flicker/stuck); 5+ reveal/hide cycles clean (no `Maximum update depth exceeded`); reveal NOT triggered by click/focus alone (pointer-only).
- [x] F-6b (AC3 — interaction over the overlay): With the dock revealed over a maximized window, click an app entry; then re-reveal and click its ✕. **Expected:** clicking a docked app's entry focuses/restores that window (topmost, accent treatment) — a docked app is activatable while the dock floats over the maximized window. Clicking `.dock-close` (`aria-label "Close <title>"`) closes ONLY that app (its entry drops); other entries remain; the ✕ reveals on row hover / `:focus-within` (`opacity 0→1`, `pointer-events none→auto`). **PASS (round 1, spec/2848 @ 12afa697):** with the dock revealed over the maximized window, clicking the `Query Viewer (minimized)` entry restored it (`Restore Query Viewer`, window shown full-bleed); clicking `.dock-close` on `Close Stepper Probe` closed ONLY Stepper (entry dropped, Sessions/Query Viewer remained); dock stays revealed/usable over the maximized window during interaction.
  - **Edge:** close of focused vs minimized vs backgrounded app from the revealed dock; the ✕ click never double-fires the row's activate (`stopPropagation`); the dock stays revealed/usable over the maximized window during interaction.
- [x] F-7 (AC3 — hide): With the dock revealed over a maximized window, move the pointer AWAY past the keep-zone (`DOCK_WIDTH_PX + DOCK_KEEP_MARGIN_PX`, i.e. > ~76px) and hold outside past the hide-delay grace (~350ms). **Expected:** the dock slides away (`visibility:hidden` after the slide, off-canvas, `pointer-events:none`), leaving the maximized window FULLY unobstructed and full-bleed (no indent — `elementFromPoint` at the edge returns the window, not the dock). **PASS (round 1, spec/2848 @ 12afa697):** reveal → dispatch `pointermove(clientX:500)` → after ~350ms grace dock `visibility:hidden`,`pointer-events:none`,`x:-60`; `elementFromPoint(6,500)` returns a window-content DIV (NOT the dock); `windowFullBleed:true`. Keep-zone boundary: pointer parked at `clientX:70` (≤76) stays revealed after 500ms; `clientX:77` (>76) arms the hide → hidden after 350ms. ESC on the revealed dock (covered branch) hides it + restores focus (`focusInDock:false`, activeElement=BODY). **Re-confirmed round 2 (spec/2848 @ cde149d9):** pointer above the bottom keep-zone + grace → `visibility:hidden`,`pointer-events:none`.
  - **Edge:** pointer returns inside the keep-zone before the grace expires → dock stays (hide cancelled); pointer inside the dock suspends auto-hide; keyboard focus inside the dock suspends auto-hide (no dock hides under an active keyboard user); ESC hides + restores focus (edge-peek branch only); hide-delay timing ≈350ms (slide completes, THEN visibility flips).
- [x] F-8 (AC4 — ✕ reveal + close, both orientations): Hover an open-app icon (each orientation) → ✕ appears top-right of the icon well; activate it → closes that app; hover away → ✕ hides (`opacity 0`). **Expected:** in BOTH orientations `.dock-close` reveals on row `:hover` / `:focus-within` (`opacity:1`, `pointer-events:auto`), its hidden state is opacity-hidden NOT `display:none` (stays in the tab order), `aria-label="Close <title>"`, and clicking it closes ONLY that app. The ✕ NEVER renders for a window whose `win.canClose` is false (no close button in the entry). **PASS (round 1, spec/2848 @ 12afa697):** `.dock-close` is `display:flex` (opacity-hidden, NOT `display:none` — stays tabbable) + `aria-label="Close <title>"` (verified per entry). Real keyboard `Tab` (entry focus → row `:focus-within`) revealed ✕ `opacity:0→1` (`.dock-close` at `opacity:1`,`pointer-events:auto`). Hover-reveal via the synthetic pointer harness did NOT deliver CSS `:hover` (documented driver artifact — same #2838 F-24/#2841 F-21 class); the reveal CSS rule is unified `&:hover .dock-close, &:focus-within .dock-close { opacity:1; pointer-events:auto }` (DockEntry.tsx:95) so the verified focus-within reveal exercises the same mechanism. Clicking `.dock-close` closed ONLY that app (F-6b: Stepper closed, others remained). `canClose` gate renders the ✕ only `{win.canClose && (...)}` (DockEntry.tsx:179) — a `canClose:false` window is NOT live-reachable (shipped catalog declares none) → cleared via the pre-sanctioned component-test contingency (AppDock.test.tsx asserts the close button is absent/no close dispatch).
  - **Edge:** minimized/background/focused app ✕; a keyboard-revealed ✕ (`focus-within`) activates without launching/activating the app; hover-away before activation closes nothing.
- [x] F-9 (AC4 — keyboard ✕): Tab/focus an app entry (roving keys) so `:focus-within` reveals the ✕; activate it with Enter (keyboard tool). **Expected:** the ✕ becomes visible on entry focus; Enter on the focused ✕ CLOSES that app WITHOUT activating/focusing/restoring it (the entry's activate is NOT fired); `stopPropagation` keeps the row dispatch silent. The app's window closes and its entry drops. **PASS (round 1, spec/2848 @ 12afa697) — with a documented driver-artifact note on the Enter-activation leg:** (a) `:focus-within` reveal verified — real keyboard `Tab` onto the entry set `.dock-close` `opacity:0→1`,`pointer-events:auto`; (b) `.dock-close` native `<button type="button">` with `onClick: e.stopPropagation(); onClose(win)` (DockEntry.tsx:180-189) — the close path is verified via real `webview_interact` click (F-6b/F-8: closes ONLY that app); (c) `stopPropagation` prevents the row's activate clear (close never double-fires); (d) `canClose` gate (✕ absent when `canClose:false`) cleared via the component-test contingency. **Enter-activation driver artifact:** pressing Enter on the focused ✕ (and, as a control, on the focused Settings button) did NOT fire native-button activation through the synthetic `webview_keyboard` harness — this is the SAME #2841 F-21 documented driver artifact, reproduced on a control (Settings) button that has no dock-specific code, so it is NOT a product defect. The ✕ close semantics are verified via click + the native-button Enter contract on a real button.
  - **Edge:** Enter on the ✕ vs Enter on the entry icon are distinct outcomes (activate = focus/restore vs ✕ = close); the ✕ of a `canClose:false` window is absent from the tab order too; the `:focus-visible` ring on the ✕ uses the accent token (`tint('var(--accent-primary)',40)`).
- [x] F-10 (AC5 — switch while maximized): With a window MAXIMIZED (dock hidden, edge-peek model active), open Settings → Appearance and change Dock position Sidebar ↔ Bottom bar; then probe the newly-chosen edge. **Expected:** changing position while a maximized window covers the desktop applies reveal behavior at the NEWLY chosen edge — the maximized window stays unobstructed (full-bleed at the OLD edge, no leftover dock there) and edge-summon (reveal zone + slide-in + hide) works at the NEW edge. The choice persists (F-3 leg after this switch). **PASS (round 1, spec/2848 @ 12afa697):** restore → open Settings → Appearance → `setDockPosition('sidebar')` → close Settings → re-maximize (Query Viewer) → dock reverts to Sidebar edge-peek (`visibility:hidden`,`x:-60`). Reveal at the new (left) edge works (`visibility:visible`,`translate(0,-50%)`,`x:0`), maximized window stays full-bleed, `elementFromPoint` at the OLD (bottom) edge returns the window content (no leftover bottom dock), `regionCount:1` (one dock only). Sequencing per the plan (open Settings BEFORE maximizing/the restore→open→re-maximize path) — Settings sinks to z0 under a maximized window (#2841), not a product defect. **Re-confirmed round 2 (spec/2848 @ cde149d9):** switching to Sidebar over the maximized window applies reveal at the NEW left edge (`x:0`,`zIndex:1200`), `dockCount:1` (no leftover bottom-edge dock), maximized window stays full-bleed.
  - **Edge:** switch maximized→restore→maximize cycles (reveal follows the current edge, no stale-edge dock); rapid position flips under a maximized window; no `Maximum update depth exceeded` from the orientation flip (transition-only reveal preserved); the old edge is NOT a second reveal surface (only the chosen edge summons). Sequencing: the Settings button sinks to z0 under a maximized window — open Settings BEFORE maximizing, or restore→open→re-maximize (#2841).
- [x] F-11 (AC5 — keyboard roving, both orientations): Reveal the dock; keyboard-drive with per-orientation roving keys. **Expected:** Sidebar — ArrowUp/ArrowDown rove between entries (Home/End = first/last; #2838 F-35 roving precedent). Bottom bar — ArrowLeft/ArrowRight rove between entries (left/right horizontal layout). Enter/Space activates the focused entry; Tab reaches each entry's ✕ in BOTH orientations; the ✕ is keyboard-reachable in both; ESC hides (edge-peek) + restores focus. **PASS (round 1, spec/2848 @ 12afa697):** Sidebar roving: ArrowDown `Query Viewer (background)`→`Sessions (active)`, ArrowUp returns; Bottom bar roving: ArrowRight `Query Viewer`→`Sessions (minimized)`, ArrowLeft returns. Cross-axis guard: ArrowDown in the Bottom bar does NOT rove (stays on the same entry — the "cross-axis keys are a FAIL" guard holds). ESC on a revealed dock (covered branch) hides it + restores focus (`focusInDock:false`). Hidden (covered) dock is out of the tab order + a11y tree (its entry `.focus()` on a hidden dock leaves `activeElement` outside — no invisible-focus trap).
  - **Edge:** direction semantics MUST match the orientation (Sidebar up/down only, Bottom bar left/right only — cross-axis keys are a FAIL); keyboard focus inside the dock suspends auto-hide (no dock disappears mid-keystroke); a hidden (covered) dock is out of the tab order + a11y tree (no invisible-focus trap when the dock hides).
- [x] F-12 (NFR — tooltip placement per orientation): Hover an entry icon in each orientation; snapshot the Chakra tooltip. **Expected:** the tooltip appears on hover AND keyboard-focus; Sidebar (left rail) — tooltip opens to the RIGHT of the well (`placement:"right"` baseline), never clipped at the left viewport edge; Bottom bar — tooltip opens ABOVE the well, never clipped at the bottom viewport edge. Content = app/window title; closes on leave/focus-out (`closeDelay={0}`, no sticky tooltip). **PASS (round 2, retry; round-1 FAIL resolved):** round-1 FAIL — the bottom-bar tooltip resolved `data-placement="right"` (the requested `top` was collision-flipped by the Ark/floating-ui resolver against the bottom-edge trigger). The FD-3 fix (`strategy:'fixed'` + `flip:false` on the bottom-orientation `positioning`) resolves it: focused the bottom-bar entry well (rect y:956–996) → Chakra popper `data-placement="top"`, tooltip content rect `{x:928,y:918,w:64,h:30,bottom:948}` sits ABOVE the well (bottom 948 < well top 956), NOT clipped at viewport bottom (948 << viewportH 1017), horizontally centered over the well. Sidebar (unchanged baseline) still opens `data-placement="right"` (`x:50 > well right:42`, unclipped at left edge). Evidence: `https://github.com/FredoAi/fredo/raw/spec/2848/.opencode/evidence/2848/F12-bottom-tooltip-top.png`.
  - **Edge:** long app titles; narrow viewport (no clip per orientation); tooltip readable in light + dark; no double-announce via `aria-describedby` when the tooltip rides the real-button accessible name (DockEntry has real-button labels — the tooltip is never the SOLE label).
- [x] F-13 (E-9 promoted — active-indicator axis per orientation): With a window focused+non-minimized (the ACTIVE entry), read the active dock entry well's computed `boxShadow`. **Expected:** in the Sidebar orientation the ACTIVE well shows the left-edge bar `inset 3px 0 0 0 var(--accent-primary)`; in the Bottom bar orientation it shows the bottom-edge underline `inset 0 -3px 0 0 var(--accent-primary)` (the dock-bar wireframe's horizontal active indicator). The accent-tinted active fill (`tint('var(--accent-primary)',12)`) and the `:focus-visible` accent ring persist in both. **PASS (round 2, promoted; round-1 FAIL resolved):** round-1 FAIL — the Bottom bar ACTIVE entry used the left-edge bar. The FD-2 fix adds an `orientation === 'bottom'` branch: live computed-style — Bottom bar ACTIVE well `box-shadow: rgb(255, 43, 194) 0px -3px 0px 0px inset` (= `inset 0 -3px 0 0 var(--accent-primary)`, bottom-edge underline) + fill `color(srgb 1 0.168627 0.760784 / 0.12)` (= `tint('var(--accent-primary)',12)`); Sidebar ACTIVE well `box-shadow: rgb(255, 43, 194) 3px 0px 0px 0px inset` (= `inset 3px 0 0 0 var(--accent-primary)`, left-edge bar, unchanged) — both with `aria-current="step"`. Verified in the resting-visible AND the revealed-over-maximized states. Evidence: `https://github.com/FredoAi/fredo/raw/spec/2848/.opencode/evidence/2848/E9-active-bar-bottom-underline.png`.

---

## F-row → AC map

F-1..F-3 → AC1 (position choice, immediate move, persistence); F-4/F-5 → AC2 (resting-visible, entry parity + empty gate); F-6a/F-6b/F-7 → AC3 (edge-summon reveal, overlay interaction, hide-after-grace); F-8/F-9 → AC4 (hover/focus ✕ both orientations + `canClose` gate); F-10/F-11 → AC5 (switch-under-maximized persists, per-orientation roving); F-12 → tooltip NFR; F-13 → AC-adjacent visual fidelity (promoted E-9 active-indicator axis, verified live round 2). A FAIL on any F-row fails its AC (the `## Tests Runs` verdict maps F-row → AC 1:1).
