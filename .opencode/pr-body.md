## Test Plan

- [x] Search: No copilot/claude references in Rust source (AC-1) — PASS
- [x] Search: No copilot/claude references in UI source (AC-1) — FAIL: useMissionMonitor.ts line 96
- [x] Search: No copilot/claude references in JSON config (AC-1) — PASS
- [x] UI: SetupWizard single OpenCode flow, no provider selection (AC-5) — PASS
- [x] UI: RunCliPanel hardcodes opencode, no provider toggle (AC-6) — PASS
- [x] UI: RunCliSettings no provider toggle (AC-6) — PASS
- [x] Search: OTLP comments reference OpenCode (AC-7) — PASS
- [x] Type: EventSource enum casing consistency (AC-8) — FAIL: 3 files use PascalCase
- [x] Search: Rust EventSource references OpenCode (AC-9) — PASS
- [x] Search: Documentation references OpenCode (AC-10) — FAIL: 3 docs have copilot/claude
- [x] File: claude-hooks.json deleted (AC-11) — PASS
- [x] File: fredo-plugin.ts exists and references OpenCode (AC-11) — PASS
- [x] Config: pnpm-workspace.yaml references marketplace-plugin (AC-12) — PASS
- [x] Search: Rust setup commands use opencode fields (AC-2/3/4) — PASS
- [x] Search: Terminal commands resolve opencode binary (AC-6 backend) — PASS

## Requirements Coverage

| Requirement | Test File | Status |
|-------------|-----------|--------|
| AC-1 (Rust) | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-1 (UI) | `tests/opencode-replacement.spec.ps1` | FAIL |
| AC-1 (JSON) | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-2 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-3 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-4 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-5 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-6 (UI) | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-6 (Rust) | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-7 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-8 | `tests/opencode-replacement.spec.ps1` | FAIL |
| AC-9 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-10 | `tests/opencode-replacement.spec.ps1` | FAIL |
| AC-11 | `tests/opencode-replacement.spec.ps1` | PASS |
| AC-12 | `tests/opencode-replacement.spec.ps1` | PASS |

## Test Results

- Tests added: 1 script (58 assertions)
- Tests passing: 51
- Tests failing: 7

## Bugs Discovered

### Bug 1: EventSource enum PascalCase/camelCase mismatch (AC-8) — CRITICAL
**Files affected:** `DevMode.tsx`, `useDevModeStream.ts`, `SpatiotemporalManifold.tsx`
**Description:** The Rust `EventSource` enum uses `#[serde(rename_all = "camelCase")]`, which serializes as `"hook"`, `"otlpGrpc"`, `"otlpHttp"`. But the DevMode components use PascalCase string literals (`'Hook'`, `'OtlpGrpc'`, `'OtlpHttp'`), which are incompatible with the TypeScript type definition `EventSource = 'hook' | 'otlpGrpc' | 'otlpHttp'`.
**Impact:** TypeScript compilation fails with 9+ errors. At runtime, EventSource comparisons will always fail because the serialized values from Rust won't match the PascalCase strings in the UI.
**Fix:** Replace `'Hook'` with `'hook'`, `'OtlpGrpc'` with `'otlpGrpc'`, `'OtlpHttp'` with `'otlpHttp'` in all three files. Also update `SOURCE_LABELS` and `SOURCE_COLORS` keys.

### Bug 2: useMissionMonitor.ts still references "claude" (AC-1)
**File:** `apps/ui/src/features/mission-monitor/hooks/useMissionMonitor.ts` line 96
**Description:** Comment contains "claude-sonnet-4.6" — the model name should reference a generic format or be removed.
**Impact:** Violates AC-1 (no claude references in source code).
**Fix:** Change comment to reference "opencode" or use a generic model name.

### Bug 3: Documentation still has copilot/claude references (AC-10)
**Files affected:** `docs/ARCHITECTURE.md` line 409, `docs/SCOPE.md` line 74, `docs/FOLDER_STRUCTURE.md` line 129
**Description:** Migration context tables still say "Original Copilot/Claude plugin" and reference `copilot-plugin/` directory.
**Impact:** Violates AC-10 (all documentation references OpenCode).
**Fix:** Update migration tables to say "Original AI CLI plugin (superseded)" or similar. Remove reference to `copilot-plugin/` directory which no longer exists.

---
*Generated by @fredo-tester*
