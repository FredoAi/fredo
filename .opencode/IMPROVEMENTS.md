# Process Improvements

Living document. Human-maintained.

## How This File Works

### Sections
- **Active**: Guardrails agents MUST follow today — backed by a prompt, script, or pipeline step.
- **Archived**: Former Active entries now baked in, one-time insights, or guardrails no longer in effect.
- **Retro Log**: Per-spec summaries appended automatically by the Retro-Analyst (via bash).

### Who Writes What
| Section | Author | Trigger |
|---------|--------|---------|
| Retro Log | Retro-Analyst | After code review + e2e (automatic) |
| Active | Human | After reviewing completed spec's Retro Log + metrics.json |
| Archived | Human | When an Active guardrail is baked in or outdated |

### Promotion Flow (Human-Driven)
1. After a spec completes, read metrics.json → `reviewer_issues`, `architect_issues`, `top_failure`
2. Read the new Retro Log entry for that spec
3. Check existing Active entries for duplicates or overlaps
4. For each issue that looks like a lasting pattern:
   - Recurring? (appears in multiple Retro Log entries or metrics across specs)
   - Actionable? (can become a prompt, script, or pipeline step)
   - Not already captured? (check Active table)
   - Yes to all three → write an Active row:
     `| <date> | Spec #N | <guardrail> | <evidence from metrics> |`
5. Periodically review Active → move baked-in entries to Archived

### Metrics → Improvement Signal
| metrics field | What it tells you |
|---------------|-------------------|
| `reviewer_issues` | Capsule contract gaps, pattern violations, missing key_files |
| `architect_issues` | Decomposition flaws, missing REQ coverage, forbidden_changes gaps |
| `top_failure` | Systemic failure category — recurring across specs |
| `retries` | High retry count = unclear capsule contract or wrong patterns |

## Active

| Date | Trigger | Change | Justification |
|------|---------|--------|---------------|
| 2026-06-03 | Spec #116 | Architect must rebase spec branch onto latest main before creating capsules | Spec #116 branch created before #108 merged to main. tauri.conf.json still had GGUF resources, lib.rs ordered models_dir after resource_dir, PR #117 conflicted after #109 merged. Rebase at spec-init catches all three. |
| 2026-06-09 | Spec #141/#142 | Reviewer must verify Coder PRs don't touch forbidden_changes via capsule contract check during review | Architect added 11 Rust files to TypeScript-only spec branch; Reviewer didn't catch it before merge. Reviewer now checks capsule forbidden_changes against PR diffs in step 2-4. |
| 2026-06-11 | Spec #174 | Phase 3a must verify issue closed, PR merged, no leftover drafts before declaring spec complete | Issue #174 left OPEN after merge; human had to ask about it. Planner Phase 3a step 5 now verifies all four checks. |
| 2026-06-11 | Pipeline | Reviewer delegates automated DOM-based e2e to the e2e-tester sub-agent via the `task` tool; Reviewer owns retry/escalation, e2e-tester owns DOM inspection + evidence | All status transitions were broken (missing read:project auth scope). Added dev-tauri-manager.ps1 for persistent dev instance, tauri-e2e skill for DOM test patterns. Reviewer dispatches `task subagent_type="e2e-tester"` and reads its PASS/FAIL table to drive the retry loop - no more inline DOM mechanics in Reviewer. |
| 2026-06-11 | Pipeline | Stale branch cleanup integrated into pipeline: Reviewer FYI scan, Planner Phase 3a targeted delete | clean-stale-branches.ps1 rewritten to use gh issue view --json state instead of brittle project API. Now cleans spec branches, feat branches, and worktrees for completed specs. |
| 2026-06-15 | Spec #181 | Architect must complete Research Phase (Step 1b) before designing spec — trace real data flows, cite file:line, produce Domain Model | #181 root cause: Architect designed capsules without understanding OpenCode SDK event model (message.updated has no content, text in message.part.updated). Result: 12+ bug-fix cycles across 6 follow-up specs. |
| 2026-06-15 | Spec #181 | After 2 failed e2e cycles, Planner escalates to ARCHITECTURE ESCALATION — stop patching symptoms | #181 had 8+ bug-fix cycles patching a broken foundation. Planner Phase 3b now triggers escalation at cycle 2 with RCA template + proposed redesign. No more dispatching until human approves new direction. |
| 2026-06-15 | Spec #215 | Architect must generate contract files (contract.rs/contract.ts) for multi-capsule specs — Coders implement against typed stubs | #93 frontend used non-existent backend types (caught at review). #215 capsule AC conflicted with real SDK behavior. Contract stubs + compiler catch type mismatches before review. |
| 2026-06-15 | Pipeline | Capsules are tracked as sub-issues under the backlog parent - not as comments | Single-issue + comments model had no per-capsule status, no Projects visibility, no progress bars. Sub-issues give individual tracking, labels, assignees per capsule while keeping backlog as single source of truth. |
| 2026-06-22 | Specs #265/#275 | Unify replay and live event processing - replay mode (buildGraphFromEvents) must use the same subscription processor as live mode, or be eliminated entirely | #275 required separate fixes for both paths; subagent nodes only work in live mode. Dual-path creates maintenance trap where every new node type needs 2 implementations. |
| 2026-06-22 | Audit | Pipeline scripts auto-log failures to `.opencode/state/script-errors.jsonl` via `_Common.ps1` - agents never call a logger directly | Every pipeline script wraps its body in `Invoke-WithLogging`. Nonzero exits (internal throws OR external `gh`/`cargo`/`pnpm` failures) auto-write JSONL entries with source, message, exit_code, branch, issue. `retro-append.ps1` surfaces spec-scoped counts during retrospective. Zero agent-prompt edits required. |
| 2026-06-22 | Audit | Reviewer trusts Coder's verification comment when diff aligns; only re-investigates mismatches or `[ ]` (blocked) ACs | Defense-in-depth without duplicated labor. Reviewer used to re-derive every AC from scratch; now accepts `[x]` + diff confirmation, focuses review effort on discrepancies. |
| 2026-06-22 | Audit | Tests run once per spec - at Reviewer's final coherence check (step 1b), not pre-merge per-PR | Coder's per-PR build/test remains the commit gate. Reviewer no longer runs `cargo test` + `pnpm test:run` twice (pre-review + post-coherence). One post-coherence run gates main PR readiness. |
| 2026-06-22 | Audit | Architect no longer duplicates Reviewer's EARS coverage check - Reviewer step 0b is the single gate | Architect's step 5c was redundant with Reviewer 0b. Removing it eliminates duplicated work; Reviewer catches coverage gaps before any PR review proceeds. |
| 2026-06-25 | Spec #311 | Architect must include ECE contract registration wiring in capsules that touch FredoFeatureClass — `eventContracts` on features are NOT auto-registered with the Rust engine; features must call `registerEventContracts()` at mount | Spec #311: `registerEventContracts()` was exported but never called — discovered by e2e-tester after 4+ wasted cycles. Fix applied mid-session (commit a6df8bb) on spec branch. Engine silently drops unmatched events (per AC-R6.1), making this a silent failure mode. |
| 2026-06-25 | Spec #311 | `fredo emit` CLI args use lowercase state and hyphenated provider — PascalCase state and underscore provider produce silent failures with no error message | Spec #311 e2e-tester spent 3+ cycles debugging CLI arg format. `fredo emit --state Init` silently fails; `--state init` works. `--provider open_code` silently fails; `--provider open-code` works. Both the e2e-tester prompt and the fredo-cli-events skill now include this format warning. |
| 2026-06-25 | Specs #295/#303/#311/#318 | ECE `streamFields` must use 2-level paths only (e.g. `['payload', 'state']`) — 3-level paths like `['payload.info.text']` silently strip to `{state: ...}` in ContractEngine deliveries | Every ECE spec encountered this: #295 subagent fields lost, #303 feature migration payloads empty, #311 e2e payloads `Some(Null)`, #318 chat-node payload stripped. ContractEngine flattens beyond 2 levels. Features must extract sub-fields in feature code, not via ECE field paths. |
| 2026-06-25 | Specs #295/#303/#311/#318 | Coder verification comments must use `--body-file`, not PowerShell `@""@` heredoc — Coders freeze on heredoc syntax and fail to post verification | 4 consecutive ECE specs had Coders freeze mid-implementation trying to post verification comments via `gh issue comment --body @""@`. The heredoc syntax confuses the LLM (unclosed quote, nested at-signs). `--body-file` with a temp file is predictably reliable. |
| 2026-06-25 | Specs #311/#318 | Reviewer must close capsule sub-issues after merging approved PRs — Reviewer prompt step for this exists but was missed in 2 consecutive specs | Both #311 and #318 had capsule sub-issues left OPEN after their PRs were merged to the spec branch. The Reviewer prompt line 102-104 instructs closure, but it's buried in prose. Promoted to explicit checklist item with `[ ]` checkbox visibility. |

| 2026-06-29 | Specs #295/#303/#311/#318/#326/#327/#339 | `backlog-create.ps1` may fail with `gh project item-create --url` flag — when it does, `project-status.ps1` fails for the entire spec lifecycle because the issue was never added to the project | 7 consecutive specs had `project-status.ps1` errors "Issue #X not found in project". Root cause: `backlog-create.ps1` line 28 uses `gh project item-create 1 --owner FredoAi --url $issueUrl` which intermittently fails with "unknown flag: --url". When backlog-create fails, the issue never enters the project. Agents should verify project membership via `gh project item-list 1 --owner FredoAi` before depending on project-status. Fixed `project-status.ps1` limit from 100→500 to handle large projects but the --url flag in backlog-create remains the root cause. |

| 2026-06-29 | Specs #303/#311/#318/#326/#327/#339 | `workspace-cleanup.ps1` fails with "'C:/Code/fredo' is a main working tree" when run from the main worktree instead of a linked worktree | 6 consecutive specs had `workspace-cleanup.ps1` fatal errors because the script attempted to remove the main worktree path. Fixed: script now detects and skips the main worktree via `git rev-parse --show-toplevel` comparison. |

| 2026-07-01 | Pipeline | All agents: `doom_loop: deny` prevents infinite tool retry loops; MCP tools isolated per agent role; every agent prompt includes an "Available Tools" manifest with MUST NEVER lists and a "fall back to bash" recovery rule | Reviewer got stuck retrying `Write` tool (denied by `edit: deny`). opencode PRs #30508/#31861 confirm `doom_loop` default `ask` hangs subagents on permission rejections. Coder heredoc freezes (specs #295/#303/#311/#318) and OTLP token count loops (spec #369) share the same root cause: model retries wrong tool instead of pivoting. Three-layer harness: (1) opencode.json permission hardening, (2) prompt-level tool manifests, (3) IMPROVEMENTS.md guardrail for retro-analyst monitoring. |

| 2026-06-30 | Specs #265/#369 | Architect must verify OTLP span attribute keys against real span data before designing adapter-to-frontend payload mapping | Two specs (both root cause `no_upfront_research`) had OTLP payload mapping failures. #265 designed capsules around fictional event types; #369 assumed Hook and OTLP payloads shared the same shape — they don't (nested vs flat). Architect research step 7 now includes concrete OTLP validation: inspect real spans, trace adapter→ECE→frontend path, verify token attributes exist. Coder anti-pattern #5 covers the frontend fallback pattern. Reviewer checklist now includes "OTLP payload path" verification. |

| 2026-06-30 | Spec #369 | Coder must build ReactFlow edges in a separate pass AFTER all nodes are created — not interleaved with node creation | Spec #369 edges rendered initially (20 edges) but disappeared after all nodes reached completion. Root cause: `nodeOrder` reordered entries across graph rebuilds, putting child nodes before parents — the interleaved `nodeList.some()` parent check failed. Fix: build all nodes first (pass 1), then build all edges (pass 2) using a `Set` of node IDs. Coder anti-pattern #6 with Wrong/Right examples. Reviewer checklist now includes "Edges & graph state" verification. |

| 2026-07-07 | Specs #295/#303/#311/#318/#326/#327/#339/#347/#356/#361/#369/#382/#498 (fixed) | `sub-issue-create.ps1` `addSubIssue` GraphQL mutation fails with `"Argument 'issueId' on InputObject 'AddSubIssueInput' has an invalid value"` — capsules are created but never linked as parent/child sub-issues | 50+ errors across 20+ specs (spec #295 through #498). The `gh issue view --json id` returns issue global node IDs but the `addSubIssue` mutation rejects them. Fixed in spec #498 retro: script no longer throws on mutation failure — child issue creation succeeds, the mutation failure is logged as a warning, and the script returns the child issue number successfully (exit code 0). Agents should still verify sub-issue linkage via `gh issue view <N> --json subIssues` after capsule creation. When the mutation fails, post the capsule as a fallback comment on the parent issue. The `capsule-get.ps1` script used by Reviewer for capsule lookup relies on proper sub-issue linkage. |

| 2026-07-01 | Specs #265/#369/#382 | Architect must verify Hook event payload shapes against real opencode events — `fredo emit` mock events and real agent events have DIFFERENT payload structures | Three specs failed because adapters and frontend extraction assumed mock event shapes that don't match real opencode events. #265: designed capsules around fictional `SubagentStart` event. #369: assumed Hook (nested `{info, part}`) and OTLP (flat `gen_ai.usage.*`) payloads had identical shapes — they don't. #382: `fredo emit` mock events use `UserPromptSubmit` with `properties.text`, `turnInputTokens`, `session.next.tool.*` — but real opencode events use `chat.message` with `output.message.parts[0].text`, `info.tokens.input`, `session.created` with `parentID`. The `properties.info.parentID` path NEVER exists in opencode events — the real parent-child relationship is at `tool_response.metadata.parentSessionId` in PostToolUse task events. Architect Step 1b extended with Hook event verification: extract samples from `~/.fredo/event-dump.jsonl`, compare mock vs real fields, document differences in Domain Model. Coder Anti-pattern #7 covers mock-vs-real payload mismatch in extraction code. |

| 2026-07-02 | Specs #108/#124/#275/#407 | Architect: Every source file MUST belong to exactly one capsule. Contract files are the sole exception — commit them to the spec branch before dispatching Coders; capsules reference them read-only. | `cross_capsule_conflict` / `cross_capsule_dependency` is the top failure in 4 specs. #108: build.rs omitted from allowed_files. #124: T2 imported T3-created files. #275: root cause `cross_capsule_dependency`. #407: `contract_407.rs` assigned to TWO capsules (Capsule 1 and 2 both implemented MetricCollector methods), REQ-1 and REQ-17 duplicated across capsules. Architect prompt updated: (a) step 3c commits contract file to spec branch before capsule creation, (b) step 4 enforces exclusive file ownership, (c) capsule rules warn against same-file-in-two-capsules. Reviewer checklist now includes a `File overlap` check to catch this during EARS coverage (step 0b). |

| 2026-07-02 | Specs #124/#318/#326/#407 | Architect: No EARS requirement may appear in more than one capsule's `requirement_ids` — decompose shared requirements into separable sub-requirements or combine capsules | 4 specs had duplicated REQs across capsules. #124: REQ-2.1/2.2/2.3/2.4/6.2 duplicated. #318: REQ-20 duplicated. #326: REQ-C1 duplicated. #407: REQ-1 and REQ-17 duplicated. Reviewer step 0b catches this post-hoc, but prevention at Architect design time avoids rework. Capsule rule added: when a REQ involves both a component AND its integration/wiring, either (a) split into separable sub-requirements (REQ-1a: component impl, REQ-1b: integration wiring), or (b) assign the whole REQ to one capsule and have the other capsule reference it as a dependency via key_files.

| 2026-07-02 | Specs #108/#124/#275/#407/#408 | Architect: Infrastructure auto-permit files (Cargo.toml, tsconfig.json, package.json, etc.) that MULTIPLE capsules would modify MUST be pre-committed to the spec branch or assigned to a single capsule owner | `cross_capsule_dependency` / `cross_capsule_conflict` now at 5 specs. #408: Capsules B and C both added `tracing = "0.1"` to Cargo.toml because each needed the dependency for compilation. Since Cargo.toml is auto-permitted for ANY capsule (architect.md line 192), this created a merge conflict that had to be resolved post-hoc. Fix: Architect prompts updated — when new dependencies are introduced, either (a) pre-commit the Cargo.toml change to the spec branch (same as contract files), or (b) designate exactly ONE capsule as the infrastructure owner with Cargo.toml in its allowed_files + acceptance criteria. Other capsules list it only in key_files. This is upstream of the existing "exclusive file ownership" rule — that covers source files; this covers shared infrastructure files.

| 2026-07-03 | Specs #457/#466 (audit) | Architect Step 10 retro-analyst dispatch is MANDATORY — every completed spec requires a Retro Report comment AND an improvement PR. No spec is "complete" without retro-analyst completion. | Audit of last 4 specs found: #457 has Retro Log entry + Retro Report comment but NO improvement PR; #466 has Retro Log entry but NO Retro Report comment and NO improvement PR. Only #431 and #440 had full retro coverage. The Architect prompt (Step 10) previously said "Also dispatch" — implying optional. Now reads "MUST dispatch ... MANDATORY GATE — SPEC NOT COMPLETE WITHOUT THIS" with explicit verification steps. Planner Phase 3a.5 now verifies Retro Log entry exists. Root cause: weak language in architect.md made retro-analyst dispatch seem optional. |

| 2026-07-03 | Spec #369 (audit) | Reviewer `closed_as` in metrics.json MUST match the Retro Log closure status — data integrity between metrics and retro log is critical for cross-spec analysis | Metrics audit found: Retro Log line 225 says #369 was "Closed as abandoned" but metrics.json shows `passed: true` with no `closed_as` field (defaults to `merged_to_main` in analysis scripts). This makes cross-spec filtering unreliable — the abandonment pattern is invisible to metrics-based queries. The Reviewer owns metrics.json (see "Constraints" section line 326 in reviewer.md). The retro-append.ps1 script should validate closed_as against the Retro Log's stated outcome, or the Reviewer should explicitly set closed_as to the resolved state. |
| 2026-07-07 | Spec #461/#478/#498 (updated) | Architect Bug Fix Mode: After creating the capsule sub-issue (`sub-issue-create.ps1`), MUST verify it was linked to the parent bug issue. If the `addSubIssue` mutation fails (chronic bug affecting 20+ specs — now handled gracefully by the script: warning logged, child issue number returned), post the capsule as a fallback comment on the bug issue AND reference the comment number in the Coder dispatch. Never dispatch a Coder without capsule tracking — the Reviewer step 0b (EARS coverage check) depends on it. | Spec #461 and #478 both had Coders dispatched without tracked capsule sub-issues. #478 had 3 sequential Coders dispatched without any sub-issue tracking. The `sub-issue-create.ps1` script fix (spec #498 retro) eliminates the hard-throw but agents must still verify linkage and post fallback comments when the mutation fails. Architect Bug Fix Mode step 5 now includes a fallback when sub-issue linking fails. |

| 2026-07-07 | Bug #478 (e2e cycles) | All agent prompts must explicitly distinguish MCP function tools from shell commands — never present MCP tool calls in ` ``` ` code blocks or as command-line syntax. e2e-tester prompt now has a **CRITICAL — MCP vs Shell tools** block in Available Tools that: (1) labels `tauri_*` as function tools, NOT shell executables, (2) bans code-block formatting for MCP calls, (3) bans source-code fallback when MCP calls fail. | e2e-tester prompt wrote `tauri_driver_session start` and `tauri_manage_window(...)` in markdown code blocks → LLM attempted as shell commands → "not found" → spiraled into reading source code (Cargo.toml, Rust plugin files, websocket.rs) trying to reverse-engineer the MCP protocol instead of calling the MCP tool directly. Caused stalled/failed e2e cycles across multiple bugs including #478 (final e2e "blocked by missing MCP tools"). This is a chronic infrastructure issue: pipeline agents misidentify MCP tools as shell executables when they appear in code-block format. |

| 2026-07-07 | Specs #303/#318/#326/#466/#478 (e2e gaps) | E2e testers MUST restart the dev instance when the webview freezes or MCP tools hang — never fall back to telemetry DB evidence, code inspection, or mock event data as a substitute for visual DOM verification. The e2e-tester now has a webview freeze recovery procedure: Stop → Start → WaitForReady → reconnect MCP → retry failing ACs up to 3 times. The tauri-e2e skill has a dedicated "Webview Freeze Recovery" section. | Multiple specs had e2e gaps where the e2e-tester fell back to DB evidence (#303: dev instance unavailable, #318: timed out waiting for Vite, #326: process PID dead, #466: e2e not completed, #478: MCP tools missing). The Architect accepted DB evidence as substitute for visual verification, defeating the purpose of e2e testing. The `dev-environment` skill previously instructed the e2e-tester to "report E2E BLOCKED: dev instance not running" instead of restarting. Webview freezes are a runtime environment failure — the correct response is to fix the runtime (restart dev), not bypass the visual test with DB data. |

| 2026-07-07 | Spec #498 (audit) | Specs with zero user-observable ACs (performance, internal refactors, cleanup) MUST still run e2e as regression smoke test — verify the app shell renders, core features work, and no console errors. | Spec #498 was a performance audit with no visual changes; e2e was skipped because there were no user-observable ACs. Without regression testing, internal changes can silently break user-facing features. The Reviewer now dispatches e2e-tester with "regression" mode when no user-observable ACs exist. The e2e-tester regression checklist: (1) app window renders, (2) no console errors, (3) Mission Monitor accessible, (4) Telemetry Settings accessible, (5) screenshot of main view. |

| 2026-07-07 | Spec #498 (audit) | Coder must NEVER commit directly to `main` — always verify `git branch --show-current` before committing. If on `main`, abort and report the error. | Commit `22d3cb6` (12 files, dev-env refactor) was committed directly to main by a Coder agent at 20:07 and lost at 20:56 when main was reset to `origin/main` — the commit was never pushed. The Coder has no guardrail against committing to the wrong branch. Coder prompt now requires a branch check before every commit. A `.opencode/scripts/pre-commit.ps1` guard script blocks commits to `main` locally. |

| 2026-07-07 | Spec #498 (audit) | Retro-analyst MUST NOT run on specs where `passed_e2e: false` — Architect checks metrics before dispatching, retro-analyst gates itself in Step 0. | Running retro on incomplete specs produces noisy metrics and Retro Log entries with indeterminate e2e status. Architect Step 10 now checks `passed_e2e` from metrics.json before dispatching retro-analyst; `passed_e2e: false` → report e2e failure to Planner without retro. Retro-analyst Step 0 aborts if e2e not passed. |

## Archived

| Date | Trigger | Change | Justification |
|------|---------|--------|---------------|
| 2026-05-26 | Spec #79 | [Superseded] Architect must not dispatch Coder/Reviewer directly | Pipeline model changed: Architect now dispatches Coders (step 8) and Reviewer (step 10) via task tool. The Planner dispatches only the Architect. |
| 2026-06-09 | Spec #142 | [Pattern] Lightweight ChakraProvider test pattern: use createSystem(defaultBaseConfig, { disableLayers: true, preflight: false, globalCss: {} }) | jsdom parses Chakra's ~2000 CSS token lines per render; lightweight system eliminates all CSS injection while keeping Chakra context for compound components. Not a process guardrail — useful coding pattern. |
| 2026-06-02 | Spec #93 | Cross-capsule type mismatch in check_model_files required T2 retry | T2 frontend assumed non-existent backend command types; Planner should include explicit API types in capsule acceptance criteria |
| 2026-06-02 | Spec #102 | Single-capsule visual refactor — first-pass review found 4 issues | Reviewer caught hardcoded colorPalette, missing shadow, error styling, fontFamily — all fixed in 1 retry |
| 2026-06-03 | Spec #108 | Task #110 capsule omitted build.rs from scope, CI failed on first attempt | Build script had cross-capsule dependency not captured in capsule; Coder retry fixed by switching to CI env var |
| 2026-06-03 | Spec #124 | T2 imported T3-created files — cross-capsule dependency required merge-order rebase | PR #130 needed rebase after #131 merged; 2 bugs found in first-pass review |
| 2026-06-09 | Spec #141/#142 | Planner needs fallback dispatch authority when subagent session DB is down | Without subagents, Planner had to become Coder violating role boundaries. Mitigation: detect dispatch failures early and flag to human. |
| 2026-06-11 | Spec #173 | Mutation testing at crate scale impractical for pre-merge CI | cargo-mutants mutates entire crate; narrow scope with --file flag necessary but still slow (24s per mutant) |

| 2026-06-30 | Spec #361 | [Pattern] React refs (useRef) do not survive component unmount — use module-level state for data that must persist across mount/unmount cycles. Delete+insert is not atomic — prefer UPDATE for SQLite upserts. Fire-and-forget async in persistence effects causes race conditions — serialize with `await` when order matters. | Spec #361 bug: `deletedSessionIdsRef` (useRef<Set<string>>) lost on unmount → deleted sessions resurrected on panel reopen. `persistDelivery()` used delete+insert (not atomic) + fire-and-forget (no await) → raced with `deleteSessionFromStore`. Fix: module-level `deletedSessionIds` Set + atomic `featureStoreUpdate` + serialized `await persistDelivery` loop. Coder prompt now includes explicit Wrong/Right examples for each anti-pattern. |
## Notes

Pipeline scripts intentionally merged/removed in SDD v2 (2026-05-26):
- `task-create.ps1` — not needed; task creation is inline in Architect prompt
- `pr-merge.ps1` — merged into `pr-review.ps1` (approve action squashes + deletes branch)
- `spec-finalize.ps1` — replaced by Planner Phase 3a steps (merge, close, clean)
- `labels-setup.ps1` — simplified to 2 labels managed via `gh` CLI directly

## Retro Log

| Date | Spec | Result | Note |
|------|------|--------|------|
| 2026-06-09 | #141 | 7/7 merged, 0 bugs | All capsules first-pass approved; pre-existing adapter_tests.rs CI failures unrelated |
| 2026-06-09 | #142 | 3/3 merged, 0 bugs | All capsules required tsconfig exclusion fix for CI typecheck compatibility |
| 2026-06-11 | #181 | 4/4 merged, 0 bugs | All capsules first-pass approved; 1 pre-existing test failure (SessionStart?node) unrelated |
| 2026-06-13 | #212 | 1/1 merged, 0 bugs | Single-capsule spec; first-pass approved, clean implementation |
| 2026-06-11 | #199 | 4/4 merged, 0 bugs | All capsules first-pass approved and merged; clean EARS coverage, zero retries |
| 2026-06-11 | #205 | 1/1 merged, 0 bugs | Single-capsule delta filter fix — first-pass approved and merged |
| 2026-06-15 | #215 | 1/1 merged, 0 bugs | Single capsule with 8 REQs; 1 Coder retry (test fix for text part time.end exemption); spec AC2 conflicts with SDK behavior for user text parts - text parts accepted without time.end (intentional fix cc200df) |
| 2026-06-15 | #181 | Did not pass e2e — closed after 12+ cycles | 4 initial capsules 0 bugs merged; 8+ bug-fix cycles across #198 #199 #205 #209 #212 #215; filter proven (11 events/exchange), ChatNode renders user text, but thinking/response text lost in turn system. Root cause: (1) no upfront research on OpenCode SDK event model — message.updated has no content, text in message.part.updated; (2) incremental patches on fragile foundation; (3) complex turn-based state machine error-prone. Guardrail: after 2 failed e2e cycles, escalate to architecture review — stop patching. |
| 2026-06-16 | #221 | 3/3 merged, 0 bugs | All capsules first-pass approved and merged. Incremental graph reducer, ChatNode awaiting state, and incremental counters clean implementations. E2E verified: counters display, data pipeline persists, full test suite passes (201 UI + 108 Rust). Coherent cross-capsule design with clear separation of concerns. |
| 2026-06-17 | #252 | 1/1 merged, 6 bugs fixed live | EventSubscription system + ChatNode-only Mission Monitor. 6 live bugs (isReplay guard, nodeMap rebuild, replay overwrite, flashing, session_id path, ghost sessions). 17 files +637/-1166. Rust adapter session_id fix - multi-terminal sessions now work. Note: Planner bypassed Architect/Coder/Reviewer loop for all 6 bugs - fixed directly on spec branch via live debugging (vibecoding). Process gap: Phase 3b assumes Architect owns bug fixes, but live DOM debugging needed Planner intervention. |
| 2026-06-22 | #265 | Did not pass e2e - abandoned after 2 cycles | Root cause: Architect designed capsules around fictional SubagentStart event without researching OpenCode SDK agent/subtask part types. 3 capsules built on wrong foundation, re-planned into #275/#276. |
| 2026-06-22 | #275 | 6/6 merged to main, 5 bugs fixed over 4 cycles | Live mode subagent nodes work (INPUT/OUTPUT sections correct). 3 separate infinite re-render bugs fixed (onNodesChange edges dep, refreshSessions events dep, auto-select sessions dep). Replay mode still shows subagents as ChatNodes - dual-path gap. All E2E manual via Planner+Tauri MCP. |
| 2026-06-22 | #276 | 0/0 merged - never dispatched | Deferred CE improvements (simplify event processing + trim user prompt display). Low priority, never reached. |
| 2026-06-22 | audit | Workflow audit - dead scripts removed, _Common.ps1 added, Reviewer delegates e2e to e2e-tester | Deleted: log-error, script-error-summary, gate-check, pre-review-check, ci-health-check, spec-status, validate-domain-model (zero callers). Added _Common.ps1 with Invoke-WithLogging - every pipeline script wraps its body; failures auto-log to state\script-errors.jsonl. Collapsed dups: Architect no longer re-runs project-status Coding or EARS coverage (Reviewer gates); Reviewer runs tests once at coherence check; Reviewer delegates e2e DOM inspection to e2e-tester subagent. |
| 2026-06-25 | #303 | 3/3 merged, 0 bugs | ECE backend engine + TS infra + feature migration all first-pass approved. 79 ECE-specific Rust tests pass. 3 pre-existing Rust failures (opencode adapter), 1 pre-existing TS failure (layoutVersion). Draft PR merge gating caused re-merge: gh pr merge silently skipped draft PRs — must mark ready first. E2E blocked: dev instance unavailable on spec branch (needs Rust recompile). |

| 2026-06-29 | #339 | 2/2 merged, 0 bugs | FeatureStore + Mission Monitor SQLite migration both first-pass approved. All 14 EARS requirements implemented. 200/200 Rust tests, 190/190 UI tests pass, TypeScript clean. Capsule A: FeatureStore backend (860 lines Rust + 129 lines TS client); Capsule B: Mission Monitor migration (remove sessionStorage.ts, add persistence.ts, wire SQLite through useDeliverySessions). Delete fix verified: trash icon permanently removes sessions from SQLite, no localStorage resurrection. |

| 2026-06-30 | #361 | 1/1 merged, 0 bugs. 1 bug-fix cycle (PR #367) for cross-mount persistence. All 6 ACs pass after fix. | Single-capsule session delete fix. Original PR #364 passed review but failed E2E: delete+insert upsert in persistDelivery raced with deleteSessionFromStore; useRef-based deletedSessionIdsRef lost on unmount; fire-and-forget async persisted deliveries without serialization. Fix PR #367: module-level deletedSessionIds Set + atomic featureStoreUpdate instead of delete+insert + serialized persistDelivery. Key takeaway: React refs do not survive unmount — cross-mount state needs module-scoped tracking. |

| 2026-07-01 | #369 | 4/4 merged, 1 bug (PR #380 fix). 7/9 ACs pass after 2 e2e cycles; 2 ACs still fail (OTLP token counts=0, edges disappear on completion). Closed as abandoned. | Four capsules: ECE init-before-end fix, OTLP adapter payload mapping, FeatureStore idempotent insert, frontend graph building + d3-force layout. Key failures: (1) OTLP `gen_ai.usage.*` attributes mapped by adapter but frontend extraction path mismatch — token counts stuck at zero. (2) ReactFlow edges render initially but disappear when all nodes complete — edge building interleaved with node creation; nodeOrder reordering puts children before parents. Root cause: `no_upfront_research` — OTLP span attribute names and Hook/OTLP payload shape differences not verified before designing. Guardrails added: Architect research step 7 (multi-transport payload verification), Coder anti-patterns #4 (content merging), #5 (OTLP payload path), #6 (ReactFlow edge preservation), Reviewer checklist OTLP + edges verification. |
| 2026-07-01 | #382 | 5/5 merged, 1 bug. 4/5 capsules first-pass; Capsule A needed 1 retry. 4 E2E cycles before 7/7 ACs passed. Initial retro (4 caps) had E2E blocked by libclang; follow-up capsule (session-splitting fix) brought total to 5. | ECE transport+eventType filtering, adapter correlationId bridging, contract declarations with transports/eventTypes, payload extraction normalization + edge validation + streaming noise filtering, session splitting fix (childToParentSession map). 198/198 UI tests pass. Key achievement: ECE filtering by transports + eventTypes eliminates duplicate agent/subagent nodes from dual Hook+OTLP transport and excludes message.* streaming noise from node-creation contracts. Key lesson: `fredo emit` mock events and real opencode events have completely different payload structures. Capsule sub-issues (#384-#387) created but not linked as parent/child (pre-existing `sub-issue-create.ps1` addSubIssue mutation bug). |

| 2026-07-01 | #396 | 3/3 merged, 0 bugs. 2/3 first-pass; Capsule C 1 retry (IPC param). 3 E2E cycles (Vite import, SettingsPanel/ProfileSettingsModal mismatch, final wiring PR #405). Telemetry tracing: SpanStore + SpanCollector + telemetry-query skill + TelemetrySettings UI. | Architect targeted SettingsPanel.tsx but actual settings dialog is ProfileSettingsModal.tsx (in forbidden_changes). Required e2e-cycle capsule to wire Telemetry into correct modal. Key lesson: trace FULL component tree when adding to existing UI surfaces. Strengthened Architect Step 1b.3; added Settings UI Hierarchy to AGENTS.md; added Reviewer UI surface checklist item. |

| 2026-07-02 | #407 | 4/4 merged, 0 bugs. 2/4 capsules first-pass; Capsule 1 needed 1 retry (active_sessions leak, flush_all data safety), Capsule 2 needed 2 retries (scope violation, REQ-2 bugs, cross-capsule conflict). E2E: all user-observable ACs PASS. | Telemetry metrics (Phase 2): MetricCollector + MetricBuffer + SpanStore telemetry_metrics table + TelemetrySettings UI Metrics section + telemetry-query metric recipes. Top failure: cross_capsule_conflict — contract_407.rs assigned to 2 capsules; REQ-1 and REQ-17 duplicated. Architect issues: contract file not pre-committed before capsule dispatch, same file in 2 capsules' allowed_files. Reviewer caught scope violation (infrastructure/mod.rs). Guardrails added: exclusive file ownership rule, contract file pre-commit step, REQ duplication prevention, Reviewer checklist file overlap check. |
| 2026-07-02 | #408 | 5/5 merged, 0 bugs. All capsules first-pass. E2E: 6 passed, 1 indeterminate (AC-10 trace context â€” test harness gap). | Telemetry Phase 3: structured logging with tracing crate ecosystem. LogCollector + LogBridgeLayer + telemetry_logs table. ~80 eprintln! sites migrated. Logging controls added to Telemetry Settings UI. Top failure: cross_capsule_dependency (Cargo.toml modified by 2 capsules). |

| 2026-07-02 | #431 | 1/1 merged, 1 bug (PR #437 fix). 5/5 ACs passed initial e2e, but visual bug found manually: NativeSelect dropdowns + purge button colors wrong in both themes. | One-shot TelemetrySettings design polish: Card layout + semantic tokens + confirmation dialog. Reviewer false-positive: flagged Chakra Dialog.Backdrop rgba as REQ-7 violation (outside scope). Post-merge bug (PR #437): NativeSelect renders native `<select>` that ignores theme tokens — replaced with `<chakra.select>` + CSS variables. Purge button `variant="outline" colorPalette="red"` silently broken by global CSS `button[data-variant="outline"]: { borderColor: 'var(--border-color)' }` in system.ts — fixed with `variant="solid"` + explicit CSS vars. Key lesson: global CSS overrides can silently break Chakra's `colorPalette`; NativeSelect doesn't inherit theme tokens. |

| 2026-07-03 | #440 | 8/8 PRs merged, 0 bugs, 5/5 capsules first-pass. E2E: AC-1 overlap 90% reduced, AC-6 click selection fixed, AC-4 Y-layering PASS. Tool/File nodes not testable (data limitation). | Mission Monitor node overlap fix + full node functionality audit. forceCollide with level-based collision radii (agent=180px, subagent=180px) resolved persistent overlap. ReactFlow v11 `selectNodesOnDrag={false}` (PR #455) enabled click-to-select. Issue: `event-dump.jsonl` grew to 1.1GB/3M lines — agents must use telemetry DB instead. Key lesson: ReactFlow v11 changes `selectNodesOnDrag` default to `true` — must explicitly set `false` for click-to-select UX. |
| 2026-07-03 | #457 | 1/1 merged, 0 bugs | One-shot cleanup: removed event-dump.jsonl (~1.1 GB) and all dump.rs references. All agent prompts + AGENTS.md + ARCHITECTURE.md updated to reference telemetry DB (telemetry_spans table). Zero warnings on cargo check + pnpm build. |
| 2026-07-03 | #466 | 1/1 merged, 0 bugs. 1/1 first-pass. E2E not completed (diagnostic logging approach). Improvement PR: #472 (comprehensive workflow audit — mandatory retro-analyst gate, IMPROVEMENTS.md fixes, 2 new Active guardrails). | Single-capsule fix for Mission Monitor subagent node output extraction failure (follow-up to #382 session-splitting). `extractAgentReply()` didn't match real opencode subagent payload paths — 2 PRs (#469 + #471) added diagnostic console.debug logging + 3 new extraction paths (`part.text`, `state.output`, bare `text`). Diagnostic approach: logging designed to surface actual payload structure at runtime. Retro audit: spec had Retro Log entry but NO Retro Report comment and NO improvement PR — retro-analyst dispatch was skipped. Root cause: Architect Step 10 used optional-sounding language ("Also dispatch"). Fixed in PR #472: made MANDATORY GATE with Planner verification. Cross-spec: 2 of 4 recent specs (#457, #466) lacked full retro coverage. 346 total script errors analyzed — dev-tauri-manager.ps1 (102, 29.5%) and sub-issue-create.ps1 (56, 16.2%) dominate. |
| 2026-07-04 | #473 | 1/1 merged, 0 bugs | Single-capsule deletion: removed OTLP transport from Mission Monitor (frontend only). Stripped `isOtlpPayload()`, all `gen_ai.*` branches in payload extraction, token post-processing loop, and OTLP test cases. Chat-node contract `transports` now `['hook']` only (was `['hook', 'otlp_grpc', 'otlp_http']`). All 9 EARS requirements implemented, zero retries. E2E took 3 cycles: first on main branch, then spec branch with `fredo emit` transport mismatch, finally real opencode agent run via opencode-cli-runner skill. Key takeaway: deletion-only capsules with precise domain model (file:line annotations) achieve one-shot completion on implementation, but e2e verification of Hook-only transport required real agent execution (mock events don't surface transport-layer gaps). |
| 2026-07-05 | #478 | 3/3 merged, 0 bugs. 3 PRs across 3 e2e cycles (PR #481: 1/6 ACs, PR #482: 3/6 ACs, PR #483: edge + subagent filtering fix). Final e2e blocked by missing MCP tools. | Bug fix: Mission Monitor session splitting regression. Root cause: chicken-and-egg deadlock between `sessionDeliveries` filtering (requires mapping) and `childToParentSession` mapping population (requires deliveries). Fix: synchronous side-effects during render (useMemo) instead of useEffect. Pipeline gap: no capsule sub-issues created — Architect Bug Fix Mode attempted `sub-issue-create.ps1` but `addSubIssue` mutation failed (chronic bug). 3 Coders dispatched sequentially without capsule tracking. The `childToParentSession` mapping is populated from two sources: `session.created` events and PostToolUse `task` events, but both paths need to fire BEFORE session sidebar filtering consults the map. |



| 2026-07-07 | #498 | 4/4 merged, 0 bugs | First exploratory performance audit â€” extended research + profiling before capsule design. 7 root causes identified and fixed: StreamContext deliveries[] cap (5K), TTL (300s), childToParentSession cap (1K), processedMappingIds cap (10K), Mission Monitor incremental graph rebuild, Home.tsx throttle (200ms), ECE completed buffer cleanup (5min), OpenCodeAdapter map eviction (10K), SpanCollector stack pop, RunCliState output_buffer cap (10MB). All 4 capsules first-pass, 0 retries, one-shot. sub-issue-create.ps1 fixed to stop throwing on addSubIssue mutation failure (50+ false-positive errors across 20+ specs eliminated). Exploratory audit pattern documented in AGENTS.md, performance guardrails in ARCHITECTURE.md.
| 2026-07-08 | #509 | 2/2 merged, 0 bugs | Both capsules first-pass, one-shot. Adapter-level sessionId rewrite replaces frontend mapping. E2E: all 5 user-observable ACs passed. Key lesson: solve data transformations at the source, not the consumer. |
| 2026-07-08 | #509 (bug fix) | 1 fix PR merged (PR #516). 11+ spurious SubagentNodes → 1. Root cause: adapter-level agent filter too broad — `session.updated` handler populated `child_to_parent` for ALL child sessions including internal OpenCode build/plan tool-execution agents. Fix: two-layer agent-name filter (adapter + frontend belt-and-suspenders). Key lesson: verify agent identity when filtering by parentID — not all child sessions are user-requested subagents. |
| 2026-07-08 | #523 | 2/2 merged, 0 bugs | Both capsules first-pass, one-shot, e2e passed. ECE compositing replaces adapter-level sessionId rewriting: relationship metadata convention + ECE relationship registry + cross-session compositing in ContractEngine. Capsule A (PR #530): ECE relationship registry; Capsule B (PR #527): OpenCode adapter cleanup (removed #509 rewrite code). +900 lines (engine.rs +230, tests.rs +450, opencode.rs +134/-584). 320/320 Rust tests pass. Key lesson: solve data transformations at the right architectural layer — delivery-level compositing (ECE) is more robust than event-level rewriting (adapter) when timing gaps exist. |

